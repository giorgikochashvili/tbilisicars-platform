kind = "web"
previewPath = "/crm/"
title = "Tbilisi Cars CRM"
version = "1.0.0"
id = "artifacts/crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 22444
name = "web"
paths = ["/crm/"]

[services.development]
run = "pnpm --filter @workspace/crm run dev"

[services.env]
PORT = "22444"
BASE_PATH = "/crm/"

[services.production]
build = ["pnpm", "--filter", "@workspace/crm", "run", "build"]
publicDir = "artifacts/crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
