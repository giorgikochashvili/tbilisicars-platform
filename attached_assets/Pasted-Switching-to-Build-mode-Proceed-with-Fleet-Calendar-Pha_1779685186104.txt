Switching to Build mode.

Proceed with Fleet Calendar Phase 1C exactly according to the plan.

Hard scope:
Change only:
artifacts/crm/src/pages/FleetCalendar.tsx

Do not change any other files.

Required changes:

1. Dashboard → Fleet Calendar region continuity
- Read the existing Dashboard-selected region from localStorage key "dashboard-region".
- Valid Dashboard values are: "All", "Tbilisi", "Kutaisi", "Batumi".
- Map "All", missing, invalid, or localStorage errors to Fleet Calendar city value "all".
- Map "Tbilisi", "Kutaisi", "Batumi" directly to the Fleet Calendar city value.
- This should happen only as the Fleet Calendar initial city value.
- Do not write to localStorage from FleetCalendar.
- Do not add DB persistence.
- Do not change Dashboard.tsx.

2. Compact vehicle rows
- Remove the vehicle availability/status badge and city label from Fleet Calendar vehicle row left label area.
- Keep only single-line compact model + license plate text, e.g. "Forester FF246K", "Outback KX844KK".
- Reduce vehicle row height so more rows fit in the board.
- Keep booking bars comfortably clickable.
- Remove unused Badge import / vehicle status color constants only if they become unused.
- Preserve group header rows.
- Preserve VehicleDetail opening on vehicle row click.
- Do not remove vehicle status from API or other CRM pages.

3. Datetime-aware display conflict detection
- Update Fleet Calendar conflict detection only.
- Use pickupDateTime/dropoffDateTime when available.
- Fall back safely to existing pickupDate/dropoffDate only if full datetime is missing.
- A booking ending at 10:00 and another booking starting at 13:15 on the same vehicle/date must NOT be marked as conflict.
- Exact boundary touch is not a conflict:
  previous.dropoffDateTime <= next.pickupDateTime means no conflict.
- True overlap remains conflict:
  previous.dropoffDateTime > next.pickupDateTime
  AND previous.pickupDateTime < next.dropoffDateTime
- This must be display-only.
- Do not change backend availability/conflict validation.
- Do not change booking creation/update logic.
- Do not update DB, booking status, audit logs, or workflows.

Preserve:
- 60-day view
- internal board scrolling
- Group by Model/Category
- Brand+Model group headers
- historical bookings
- BookingDetail click
- VehicleDetail click
- color/overdue logic
- city filter UI behavior after initial load
- date navigation
- sticky header/left column

Strict exclusions:
- No API changes.
- No DB migration.
- No new dependencies.
- No booking mutation changes.
- No status/action workflow changes.
- No pricing/availability/payment/accounting changes.
- No parking/service changes.
- No website/SEO/Google Ads changes.
- No right-click actions.
- No drag/drop booking movement.
- No persistent model order customization.

After implementation:
- Run CRM typecheck/build check as appropriate.
- Confirm exact files changed.
- Confirm no excluded files/logic were touched.
- Report manual test steps:
  1. Dashboard selected region carries into Fleet Calendar
  2. vehicle rows are compact and more rows fit
  3. same-day non-overlapping bookings no longer show conflict
  4. true overlapping bookings still show conflict