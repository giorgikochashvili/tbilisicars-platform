You are updating ONLY customer-facing UI copy and presentation in the public booking flow.

STRICT RULES:
- Do NOT change architecture
- Do NOT change routing
- Do NOT change API contracts
- Do NOT change booking flow logic
- Do NOT change form validation behavior except simple helper copy
- Do NOT touch backend in this task
- ONLY polish customer-facing text, labels, and price presentation
- Keep layout structure intact unless a tiny inline text adjustment is necessary
- No breaking changes

GOAL:
Clean up the booking page/customer-facing pricing and insurance copy so it looks more professional, more trustworthy, and no internal/admin wording is exposed.

FILES TO CHECK FIRST:
- artifacts/website/src/pages/Booking.tsx
- any small related presentational helper/component used only by Booking.tsx for summary blocks

--------------------------------------------------
1. HIDE INTERNAL DISCOUNT NAMES
--------------------------------------------------

Current issue:
Customer-facing booking summary shows internal/admin discount names such as:
"ALL APRIL TEST", etc.

Required:
- NEVER show internal discount names anywhere in the public booking flow
- Replace discount presentation with a clean customer-facing format only

If a discount exists, show pricing in this style:

- Base rental row:
  show price-per-day × number of days
  show original subtotal
  original subtotal should appear visually muted / struck through if discounted

- Discount row:
  label should simply be "Discount"
  show percentage if available
  show discount amount in money terms
  DO NOT show discount campaign name

- Final rental row:
  clearly show the discounted rental total

Apply this consistently in all booking flow summary/review/side pricing blocks on the website.

--------------------------------------------------
2. INSURANCE COPY CLEANUP
--------------------------------------------------

Find customer-facing insurance text blocks.

Required changes:

A)
Where "Insurance" summary currently uses "Full Cover",
change it to:
"Full Insurance"

B)
Where pricing/summary text currently includes "excess",
remove "excess" entirely from the public booking flow summary copy.

C)
Where "Not covered" currently includes "Personal belongings",
replace that item with customer-facing text meaning:
- underbody damage caused during off-road use
- damage from incidents where the cause cannot be verified

Write this in clean professional English.
Do NOT use awkward wording like "unclear circumstances".

D)
Where deposit explanatory text currently says:
"Deposit is pre-authorised at pickup and fully refunded upon return of the vehicle in good condition."
change it to:
"Deposit is pre-authorised at pick-up and fully refunded upon return of the vehicle."

--------------------------------------------------
3. STANDARDISE "PICK-UP" WORDING
--------------------------------------------------

In customer-facing booking copy only:
- replace "pickup" with "pick-up"
- replace "vehicle pickup" with "vehicle pick-up"
where natural and visible to customers

Do NOT rename variables/functions/internal code names.
This is copy-only.

--------------------------------------------------
4. PHONE NUMBER HELPER COPY
--------------------------------------------------

Near the customer phone number field, add a short helper note telling users to include the country code / phone index.

Use concise professional copy such as:
"Please include your country code."

Do NOT change validation logic in this task.

--------------------------------------------------
5. REMOVE URGENCY/NOISE COPY
--------------------------------------------------

Remove this customer-facing line completely:
"Availability may change — secure your booking now"

--------------------------------------------------
6. TOTAL BLOCK CLEANUP
--------------------------------------------------

Where the total block currently says:
"Estimated Total"

change it to:
"Total Amount"

Also improve the deposit mention so the customer sees it clearly but subtly, for example:
"(+100€ deposit at pick-up)"
or equivalent dynamic currency-aware wording if the amount/currency is already dynamic in the UI.

Important:
- Do NOT hardcode EUR if the existing code already has dynamic currency support
- Keep the deposit note visually secondary to the main total
- Main focus should remain on the total amount

--------------------------------------------------
7. PAYMENT COPY CLEANUP
--------------------------------------------------

Where customer-facing payment method text says something like:
"Choose how you'd like to handle payment for your rental"
update it so it clearly references pick-up, for example:
"Choose how you'd like to handle payment for your rental at pick-up."

Also make sure any nearby explanatory payment copy uses "pick-up" wording consistently.

--------------------------------------------------
8. KEEP SCOPE SAFE
--------------------------------------------------

This task does NOT include:
- promo + discount stacking logic changes
- backend pricing rule changes
- API changes
- reservation code changes
- email/voucher changes

Those will be handled later.

DONE LOOKS LIKE:
- public booking flow no longer shows internal discount names
- discount pricing is shown cleanly as Base / Discount / Final rental
- insurance label says "Full Insurance"
- "excess" is removed from customer-facing booking summary
- insurance text is cleaner and more professional
- deposit text uses "pick-up"
- phone field shows country code helper note
- urgency line is removed
- "Estimated Total" becomes "Total Amount"
- deposit note is secondary and clean
- no layout breaks
- no logic changes
- no TypeScript errors

At the end, return a concise implementation summary with exact files touched.