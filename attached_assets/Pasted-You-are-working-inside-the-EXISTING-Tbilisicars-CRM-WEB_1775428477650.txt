You are working inside the EXISTING Tbilisicars CRM&WEB monorepo that was originally built in Replit. Do NOT redesign anything. Do NOT touch unrelated modules. Work ONLY on the broken AI Booking Importer / Voucher Extraction flow in CRM → Bookings → Import Voucher.

PRIMARY GOAL
Fully fix the voucher AI extraction so that uploading a REAL voucher image/PDF no longer always falls back to:
- extractionFailed: true
- extracted: {}
- unresolved fields:
  contactFullName, pickupLocation, dropoffLocation, pickupDatetime, dropoffDatetime

CURRENT CONFIRMED USER SYMPTOM
In Replit Preview:
- Upload succeeds
- UI moves to Review step
- But Review still shows:
  "Failed to extract data from file. Please fill in details manually."
- The same fallback state keeps happening repeatedly
- Prior attempts already changed some code but DID NOT fix runtime behavior

STRICT RULES
- Do NOT assume the cause
- Do NOT stop at code inspection
- Do NOT give a theoretical explanation only
- Do NOT fix side issues like pdftoppm/PDF tooling unless that is proven to be the actual blocker for the exact failing runtime path
- Do NOT modify unrelated CRM UI
- Do NOT modify booking creation logic
- Do NOT modify DB schema
- Do NOT modify unrelated AI modules
- Do NOT refactor architecture
- Do NOT stop until the actual Preview/runtime path is proven and the issue is fixed
- Keep manual fallback behavior intact for genuinely unreadable files
- Keep current endpoint contract/response shape intact

PROJECT-SPECIFIC CONTEXT
This project is a Tbilisicars CRM&WEB monorepo with:
- CRM frontend
- API server
- OpenAI integration wrapper
- voucher import route/service already built
Current flow already exists and must remain:
Upload → Review/Edit → Confirm

FILES IN SCOPE
- artifacts/api-server/src/routes/admin-voucher-import.ts
- artifacts/api-server/src/services/admin-voucher-import.service.ts
- lib/integrations-openai-ai-server/src/client.ts
- any exact runtime/workflow config that determines which API process Replit Preview is actually hitting
- artifacts/crm/vite.config.ts ONLY if needed to prove Preview → API routing

PHASE 1 — PROVE THE REAL RUNTIME PATH FIRST
Before changing logic, prove which exact API process Replit Preview is using right now.
You must verify:
1. Which frontend dev server/workflow powers Preview
2. Which API port/process Preview actually proxies /api requests to
3. Whether the code being edited is the code actually used by Preview
4. Whether stale workspace linking / stale build / wrong process is causing unchanged behavior

Do not assume Preview uses the same API process as an arbitrary shell command.
Prove it from runtime evidence.

PHASE 2 — ADD NARROW TEMPORARY RUNTIME LOGS ONLY TO THIS FLOW
Add temporary logs tagged with [voucher-import] only in the voucher extraction path.

Required logging:
A) Before OpenAI call:
- credential path selected (Replit proxy vs direct OPENAI_API_KEY)
- baseURL used
- model used
- mimeType
- base64 length
- whether uploaded file was image or PDF
- for PDF path: whether text extraction path or image fallback path is used

B) After OpenAI response:
- whether a response exists
- whether choices[0].message.content exists
- content type/shape
- raw content length
- first 300 chars of returned content

C) In parseExtractionJson:
- regex matched or not
- JSON.parse success/failure
- parsed key count
- non-null key count

D) In catch:
- err.name
- err.status
- err.message
- err.response?.data if available

Do NOT add noisy logs anywhere else.

PHASE 3 — REAL SELF-TEST AGAINST THE ACTUAL PREVIEW RUNTIME
Do not use a CRM form screenshot as the AI test file.
Use a REAL voucher / reservation confirmation image or PDF with visible booking data:
- customer name
- pickup date/time
- dropoff date/time
- pickup/dropoff location

If no real voucher file exists inside the workspace, STOP and ask me for the exact file path or uploaded file before proceeding.

Then:
1. Trigger the real extraction flow against the exact API process used by Preview
2. Capture the real server logs
3. Prove the exact failure point from logs, not guesses

Possible proven failure categories may include:
- Preview hitting different API process than expected
- stale code/runtime not using latest changes
- wrong OpenAI credential path at runtime
- unsupported OpenAI/proxy option
- model incompatibility in this exact runtime path
- OpenAI returning valid content in a shape parser does not handle
- parser failure on actual returned content
- PDF/image branch mismatch
But do NOT choose one until logs prove it.

PHASE 4 — APPLY ONLY THE MINIMAL CONFIRMED FIX
After proving the exact runtime failure point:
- implement only the smallest safe fix
- preserve manual fallback
- preserve endpoint response shape
- preserve Upload → Review/Edit → Confirm flow
- do not touch unrelated modules

PHASE 5 — RE-TEST UNTIL THE USER’S ACTUAL SYMPTOM IS GONE
After the fix:
1. Re-run with a REAL voucher
2. Confirm the Review step no longer always shows the generic extraction failure
3. Confirm extractionFailed becomes false when the voucher contains readable data
4. Confirm extracted fields populate when visible
5. Partial nulls are acceptable only for fields genuinely unreadable on the file
6. Remove temporary debug logs
7. Keep only minimal permanent error logging if useful

SUCCESS CRITERIA
The task is NOT complete unless all of these are true:
- Replit Preview runtime path was proven
- exact failure point was proven from logs
- fix was applied to the actual runtime path
- real voucher upload returns extracted data instead of always falling back
- no unrelated regressions were introduced

REQUIRED FINAL REPORT
Return a short exact report with:
1. exact runtime/API process Preview was using
2. exact confirmed root cause
3. exact file(s) changed
4. exact minimal fix applied
5. why the previous attempts did not change behavior
6. exact verification steps performed
7. exact sample successful API response
8. confirmation that temporary debug logs were removed