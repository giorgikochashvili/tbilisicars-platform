TBILISICARS CRM&WEB — Monitoring module patch

This is a LIVE production-connected project already fully built in Replit.
Do NOT re-architect anything.
Do NOT refactor unrelated files.
Do NOT change existing working flows outside the Monitoring module and the minimal related pickup/dropoff data flow needed for this patch.
Make only the exact changes below, surgically and safely.

Goal:
Improve the CRM Monitoring module so it shows the correct operational information for pickup/dropoff monitoring, customer behavior, quick access, and payment visibility.

Important rules:

* Preserve existing design language and dark UI styling.
* Preserve existing booking, payment, pickup, dropoff, and monitoring flows.
* Do not break Booking Detail, Dashboard, Bookings, Payment History, Handover/Return actions, or alerts.
* If a field/value already exists in DB/API, reuse it. Do not invent duplicate structures unless absolutely required.
* Keep code changes minimal and localized.
* If there is any existing enum/type for customer satisfaction, extend it carefully without breaking old records.
* Backward compatibility is required for existing records that already have Happy / Neutral / Sad.

Implement exactly these changes:

1. Monitoring expanded detail — remove unnecessary fields
   In the expanded Monitoring row/detail panel:

* Remove Pickup > Mileage from display
* Remove Pickup > Fuel from display
* Remove Drop Off > Mileage from display
* Remove Drop Off > Fuel from display

Important:

* This is a display change for Monitoring detail only.
* Do NOT remove mileage/fuel capture from pickup/dropoff forms or from stored operational data unless it is used nowhere else.
* Booking Detail / Handover / Return logic must continue working as before.

2. Add a new customer satisfaction option: Problem
   Extend the existing customer satisfaction options from:

* Happy
* Neutral
* Sad

to:

* Happy
* Neutral
* Sad
* Problem

Requirements:

* Problem must use a red angry/problem-style emoji or icon consistent with current UI.
* Styling should clearly distinguish it from Happy/Neutral/Sad.
* Old records must still render correctly.
* Existing Happy/Neutral/Sad records must not break.

3. Customer satisfaction must exist for BOTH pickup and dropoff
   Currently satisfaction is only captured on pickup.
   Change this so satisfaction is captured separately for:

* Pickup satisfaction
* Dropoff satisfaction

Requirements:

* Pickup flow keeps its current satisfaction requirement/behavior.
* Dropoff flow must also include customer satisfaction selection with the same 4 options:
  Happy / Neutral / Sad / Problem
* Store pickup satisfaction and dropoff satisfaction separately.
* Do NOT overwrite pickup satisfaction with dropoff satisfaction.
* If DB/API currently has only one shared field, extend carefully so both values can exist independently.
* Keep compatibility with old data.

4. Monitoring list/detail must show BOTH satisfaction values
   On the Monitoring page, each booking row and/or expanded detail must clearly show:

* Pickup satisfaction
* Dropoff satisfaction

Requirements:

* It must be obvious which one is pickup and which one is dropoff.
* If only pickup exists and dropoff is not yet recorded, show pickup and a clear empty/not-yet-returned state for dropoff.
* If neither exists yet, show a clean empty state.
* Do not clutter the row; if needed, keep compact badges in the row and more explicit labels in the expanded detail.
* Preserve current clean layout.

5. Reservation code must open the booking detail
   On the Monitoring page:

* Make the booking reference / reservation code clickable
* Clicking it must open/navigate to that exact booking’s detail page/view

Requirements:

* Reuse the existing CRM booking detail route/navigation pattern
* Do not create a new detail page if one already exists
* Use existing router/navigation utilities already used elsewhere in CRM

6. Customer phone number must open WhatsApp
   On the Monitoring page:

* Make the customer phone number clickable
* Clicking it must open WhatsApp chat using the phone number

Requirements:

* Use a proper WhatsApp deep link
* Normalize the number as safely as possible without damaging existing formatting
* If the number cannot be normalized perfectly, still use the stored number sensibly rather than hiding the action
* Open behavior should be practical for CRM staff use

7. Fix Monitoring payments block so it shows REAL payment records
   Current bug:
   A booking can already have a real payment record in Booking Detail / Payment History, but Monitoring expanded view still shows empty state like “No paid records.”

This must be fixed.

Requirements:

* Monitoring payments section must load and render the real booking payment record(s)
* If payment records exist, show them correctly
* Reuse the existing booking/payment source of truth
* Do NOT create a separate duplicate payment storage for Monitoring
* Keep the data consistent with Booking Detail Payment History

8. Monitoring payments empty-state logic
   Change the Monitoring payments block logic to this:

Case A:

* If booking has one or more payment records
* Show the real payment record(s)

Case B:

* If booking is already in pickup/delivered operational stage and there is NO payment record
* Show:
  “No payment record”

Important:

* Show “No payment record” only when there truly is no payment record
* Do not show false empty state when payments exist
* If current text says “No paid records.” replace it with the correct logic and wording above where appropriate

9. Keep current operational integrity
   Do NOT break:

* pickup action
* dropoff action
* payment creation
* payment summary
* booking detail modal/page
* dashboard operational cards
* alerts logic
* existing monitoring filters
* existing styling and spacing

10. Implementation quality expectations
    Please make the smallest safe set of changes across CRM/API/DB if needed.
    Before changing schema, first check whether pickup/dropoff satisfaction can already be represented with existing fields.
    Only add schema/API changes if truly necessary for separate pickup and dropoff satisfaction storage.

At the end, provide:
A) exact files changed
B) concise explanation of what was changed in each file
C) any DB migration or drizzle push required
D) quick manual test checklist for:

* pickup satisfaction
* dropoff satisfaction
* monitoring badges/details
* reservation code click
* WhatsApp click
* payment visible in monitoring
* true “No payment record” case

Do NOT do any unrelated cleanup.
Do NOT touch unrelated modules.
Do NOT redesign the app.
Implement only this Monitoring patch cleanly and safely.
