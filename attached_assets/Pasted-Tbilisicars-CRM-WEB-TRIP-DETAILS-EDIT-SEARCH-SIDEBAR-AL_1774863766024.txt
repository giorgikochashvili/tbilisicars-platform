Tbilisicars CRM&WEB — TRIP DETAILS / EDIT SEARCH SIDEBAR ALIGNMENT FIX (SURGICAL UI ONLY)

Continue from the current working state.

IMPORTANT:
- Do NOT change backend logic
- Do NOT change booking logic
- Do NOT change editSearch behavior
- Do NOT change filters logic
- Do NOT change summary logic
- Do NOT change step flow
- Do NOT touch unrelated modules
- Surgical layout fix only

CURRENT ISSUE:
In the left sidebar, inside the Trip Details / Edit Search block, the fields are visually misaligned and cramped.
The pickup/dropoff location fields and the pickup/return date-time fields do not sit cleanly.
The current two-column arrangement looks uneven and low quality in the narrow sidebar.

TARGET:
Refine the Trip Details / Edit Search block so the inputs are stacked vertically in one clean column, full width, in this exact order:

1. Pickup Location
2. Drop-off Location
3. Pickup Date & Time
4. Return Date & Time

DESIGN REQUIREMENTS:
- Each field should take full available width of the sidebar card
- Use a single-column layout inside this Trip Details block
- Maintain existing labels, required markers, values, and handlers
- Maintain existing DateTimePicker behavior
- Maintain existing "Done" button behavior for editSearch
- Keep the current dark premium styling
- Fix spacing so labels and inputs align consistently
- No cramped two-column layout inside the narrow sidebar
- Make the block feel clean, ordered, and easy to scan

IMPORTANT FUNCTIONAL REQUIREMENTS:
- Reuse the exact existing state and handlers
- Do NOT rewrite the edit search flow
- Do NOT change validation
- Do NOT change auto-open pickup/dropoff behavior unless required by current existing wiring
- Do NOT break responsive behavior

IMPLEMENTATION NOTES:
- This is primarily a layout/grid/spacing adjustment in the Trip Details sidebar block
- Replace the current cramped multi-column arrangement with a single vertical stack
- Preserve the rental duration display and Done action
- Ensure the DateTimePicker fields render at full width and align properly with other controls

VISUAL GOAL:
Instead of:
[ Pickup Location ] [ Drop-off Location ]
[ Pickup Date ]     [ Return Date ]

Make it:
[ Pickup Location ]
[ Drop-off Location ]
[ Pickup Date & Time ]
[ Return Date & Time ]

LIKELY FILE:
- artifacts/website/src/pages/Booking.tsx

DELIVERABLE:
Return:
1. Exact file changed
2. Short summary of layout adjustment
3. Confirmation that:
   - Trip Details fields are now stacked vertically
   - alignment is fixed
   - existing edit search behavior remains unchanged

MANUAL TEST:
- Open Step 1 booking page
- Click Edit Search
- Confirm fields are displayed in this order:
  1) Pickup Location
  2) Drop-off Location
  3) Pickup Date & Time
  4) Return Date & Time
- Confirm all fields align cleanly
- Confirm changing values still works
- Confirm Done still exits edit mode correctly