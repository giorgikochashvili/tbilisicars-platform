Fix #LOGOUT-1 — Reliable Admin Logout + CORS Hardening (NO BREAKING CHANGES)

Project context:
This is a live production monorepo (Tbilisicars CRM & WEB).
DO NOT change architecture.
DO NOT refactor unrelated logic.
DO NOT touch booking, pricing, fleet, or database logic.
Only apply minimal, surgical fixes for logout reliability and CORS safety.

----------------------------------------
PROBLEM SUMMARY
----------------------------------------

1. Admin logout is unreliable:
- Frontend uses raw fetch()
- Redirect happens even if logout fails
- Session may remain active on server

2. Backend CORS is strict:
- Requires CORS_ORIGINS env variable
- If not configured correctly, logout request may silently fail

3. System must be hardened, not changed

----------------------------------------
GOAL
----------------------------------------

Make logout 100% reliable:
- Always destroys session on server
- Always clears cookie properly
- Never redirects if logout fails
- Works in production behind nginx + HTTPS

----------------------------------------
TASK 1 — FIX FRONTEND LOGOUT FLOW
----------------------------------------

File:
artifacts/crm/src/hooks/use-auth.tsx

Requirements:

1. Replace raw fetch logout with safe logic:

- Use try/catch
- Do NOT use .finally() for redirect
- Redirect ONLY if logout succeeds

Implementation:

- Send POST request to:
  /api/auth/admin/logout
  with credentials: "include"

- If response is OK:
  → redirect to /crm/login

- If response fails:
  → DO NOT redirect
  → log error to console
  → optionally show error toast (if toast system exists)

IMPORTANT:
Do not reintroduce React Query mutation.
Keep implementation simple and explicit.

----------------------------------------
TASK 2 — HARDEN BACKEND LOGOUT
----------------------------------------

File:
artifacts/api-server/src/routes/admin-auth.ts

Update logout route:

Current:
router.post("/auth/admin/logout", async (req, res) => {
  await logoutAdmin(req.session);
  res.json({ message: "Logged out" });
});

Fix:

- Ensure session is properly destroyed
- Explicitly clear cookie

Required behavior:

router.post("/auth/admin/logout", async (req, res) => {
  req.session.destroy((err) => {
    if (err) {
      console.error("Logout error:", err);
      return res.status(500).json({ message: "Logout failed" });
    }

    res.clearCookie("connect.sid", {
      path: "/",
      httpOnly: true,
      sameSite: "lax",
      secure: process.env.NODE_ENV === "production",
    });

    return res.json({ message: "Logged out" });
  });
});

IMPORTANT:
- Cookie name must match session config
- Do NOT change session middleware config

----------------------------------------
TASK 3 — SAFE CORS CONFIG (NON-BREAKING)
----------------------------------------

File:
artifacts/api-server/src/app.ts

Current behavior:
Strict allowlist using CORS_ORIGINS

Modify safely:

1. If CORS_ORIGINS is defined:
   → use strict allowlist

2. If NOT defined:
   → fallback to safe dev/prod mode:
      origin: true

Implementation logic:

const allowedOrigins = process.env.CORS_ORIGINS?.split(",").map(o => o.trim());

app.use(cors({
  origin: (origin, callback) => {
    if (!origin) return callback(null, true);

    if (!allowedOrigins || allowedOrigins.length === 0) {
      return callback(null, true); // fallback mode
    }

    if (allowedOrigins.includes(origin)) {
      return callback(null, true);
    }

    return callback(new Error("Not allowed by CORS"));
  },
  credentials: true,
}));

IMPORTANT:
- Do NOT block requests if env is missing
- This must NOT break production

----------------------------------------
TASK 4 — DO NOT TOUCH ANYTHING ELSE
----------------------------------------

STRICTLY FORBIDDEN:
- No changes to booking logic
- No changes to pricing
- No changes to DB schema
- No changes to routes unrelated to auth/logout/CORS
- No UI redesign
- No refactoring

----------------------------------------
EXPECTED RESULT
----------------------------------------

After fix:

1. Logout:
   - Always removes session
   - Always clears cookie
   - Redirect only on success

2. /api/auth/admin/me after logout:
   → must return 401

3. System stability:
   - No regressions in CRM or Website
   - No breaking changes

----------------------------------------
VALIDATION (REQUIRED)
----------------------------------------

Show:

1. Updated logout frontend code
2. Updated backend logout route
3. Updated CORS config
4. Confirmation that:
   - logout works
   - /me returns 401 after logout