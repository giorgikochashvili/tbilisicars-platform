Plan only. Do not change code yet.

Task:
In CRM → Service & Maintenance → Add Service Record modal, add a new selectable service category named:

სხვა

Context:
Currently the “Service Categories *” list contains Georgian service categories such as:
- ზეთი & ფილტრები
- ხუნდები
- ნათურა
- ვიზუალური დაზიანება
- კარობკის ზეთი
and others.

User request:
Add “სხვა” as another selectable category in the same Service Categories list.

Requirements:
1. “სხვა” must appear as a normal selectable service category.
2. It must work the same way as all existing categories:
   - selectable in Add Service Record modal
   - saved with the service record
   - displayed in Service Records list/detail where category is shown
   - filter/category behavior should not break
3. It should fit the current Georgian category list style.
4. Do not change the modal design except adding this option.
5. Do not reorder all categories unless there is a clear existing ordering rule.
6. Prefer placing “სხვა” at the bottom of the category list.
7. Do not change service pricing/cost logic.
8. Do not change vehicle selection logic.
9. Do not touch unrelated CRM modules.
10. Do not touch website/SEO/email/PDF.
11. Do not touch OpenAPI/codegen/generated files unless you first prove this category source requires it.

Investigation required:
1. Find where service categories are defined:
   - frontend constant?
   - backend enum?
   - DB seed/data?
   - API response?
2. Confirm whether adding “სხვა” requires:
   - frontend-only change
   - backend change
   - DB migration
   - seed/update script
3. Inspect Service module files:
   - Add Service Record modal/category rendering
   - service creation submit payload
   - Service Records list/display
   - filters if categories are hardcoded there too
4. Identify exact files that need to change.
5. Confirm whether this can be done safely without DB migration and without OpenAPI/codegen.

Output required:
1. Exact files inspected.
2. Where the category list currently comes from.
3. Exact files that would change.
4. Whether “სხვა” can be added safely as one extra category.
5. Whether backend/API/DB/OpenAPI/codegen are needed.
6. Risk level.
7. Recommend Build only if the change is small and safe.