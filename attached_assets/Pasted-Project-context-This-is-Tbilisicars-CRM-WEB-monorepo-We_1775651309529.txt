Project context:
This is Tbilisicars CRM&WEB monorepo.
We are making a very small, controlled public-website-only change before launch.
The website is an SPA.
Customer cabinet functionality is NOT ready for public use yet, so it must disappear from the public website UI to avoid confusing users.

CRITICAL RULES:
- Do NOT refactor architecture
- Do NOT change backend
- Do NOT change API responses
- Do NOT change booking submission logic
- Do NOT change CRM login at /crm/login
- Do NOT change email service
- Do NOT change the 6-step booking flow
- Do NOT touch anything outside the exact items below
- Login.tsx file may remain in the codebase; only remove its public route/navigation exposure

Task:
Make only the following 4 targeted website changes.

==================================================
1) FILE: artifacts/website/src/components/Header.tsx
==================================================

Goal:
Remove all public Login/Cabinet access from the website header.

Required changes:
- Remove the desktop "Log in" dropdown completely
- Remove the mobile login section completely
- Keep only:
  - logo
  - navigation links
  - Book Now CTA
  - mobile burger menu
- Remove all login-related code that becomes unused:
  - loginOpen state
  - loginMenuRef
  - login dropdown button
  - dropdown menu content
  - mobile login rows
  - outside click useEffect
- Remove now-unused imports related to login dropdown:
  - useRef
  - useEffect
  - ChevronDown
  - User
  - Shield
- Keep the existing navigation links and Book Now button behavior unchanged
- Keep header layout stable on desktop and mobile

Important:
Do not redesign the header.
Do not change branding.
Do not touch Book Now CTA except preserving it.

==================================================
2) FILE: artifacts/website/src/App.tsx
==================================================

Goal:
Remove the public customer login route exposure.

Required changes:
- Remove:
  import Login from "./pages/Login";
- Remove:
  <Route path="/login" component={Login} />
- Keep all other routes unchanged
- Do not change router base logic
- Do not touch CRM routes or any other page imports

==================================================
3) FILE: artifacts/website/src/pages/Booking.tsx
==================================================

PART A — Step 4 customer info helper text

Find this exact text:
"Use your real email address so you can receive your booking confirmation, voucher, and securely access your personal booking cabinet."

Replace it with exactly:
"Use your real email address so you can receive your booking confirmation and voucher."

Do not change anything else in Step 4.

--------------------------------------------------
PART B — Success / confirmation result UI
--------------------------------------------------

In the success state render block (the UI shown when `result` exists after booking submission), remove all customer-cabinet/account-login related UI.

Remove completely:
1. The generated password / "Your Account" block
2. The "Existing Account" block
3. Any UI text in this success section that mentions:
   - cabinet
   - password
   - log in

After the change, the booking confirmation screen should still keep the existing overall success layout, but only include the booking-confirmation-related content.

Must remain visible in success state:
- Booking reference number
- Vehicle name
- Pickup details
- Dropoff details
- The normal “we will confirm shortly” messaging
- Make Another Booking button

Allowed to remain:
- The existing success header
- The trip summary / timeline
- Pickup instructions
- Contact/help section
- result.message line, as long as it stays unchanged and contains no cabinet/login/password wording
- Return to Home button, unless removing it is absolutely required (prefer not to change it)

Important:
- Do NOT change submit logic
- Do NOT change result type/interface unless TypeScript strictly requires it
- Do NOT change backend-generated `generatedPassword` behavior
- Do NOT change email sending behavior
- Do NOT remove the `generatedPassword` field from API/backend contracts unless absolutely necessary for compile reasons (prefer not to touch contracts)

==================================================
4) SCOPE CONTROL
==================================================

This is a surgical public-website cleanup only.
Do not touch:
- artifacts/api-server/*
- artifacts/crm/*
- email templates
- voucher generation
- auth backend
- customer login API
- database
- shared contracts unless compile strictly forces it

==================================================
5) AFTER EDITS — VERIFY
==================================================

After making the changes:
1. Run a TypeScript/build validation for the website package
2. Confirm there are no unused imports left in Header.tsx or App.tsx
3. Then return ONLY these three code excerpts:

A) Header.tsx navigation/header section
B) App.tsx route list
C) Booking.tsx success/result render block

Also briefly confirm:
- public /login route removed
- header no longer shows login/cabinet access
- booking confirmation no longer shows password/account/login UI
- CRM /crm/login untouched