TBILISICARS CRM&WEB — Service Module Investigation + UI Improvement Plan

We need to improve the CRM Service & Maintenance module, but first inspect the current code and provide a safe plan only. Do not implement yet.

Context:
The Service page currently lists service records in a table. It works, but each record feels too flat/basic. We want each created service record row to be clickable/expandable and show more detailed service information.

Also, there may be a bug: when a new vehicle is added to the system, that newly added vehicle may not appear or may not be selectable when creating a new service record.

This is a live production CRM. Do not change unrelated modules, booking logic, pricing, payments, parking, website, SEO, or deployment.

Please inspect the code and answer:

1. Service module structure
- Which files control the Service page UI?
- Which files control Add Service Record modal/form?
- Which backend routes/services provide service records?
- Which endpoint provides vehicle options for the service form?

2. Expandable service record detail
Plan a minimal UI improvement:
- Make service record rows clickable or add an expand button.
- Expanded area should show all already-available service details from existing API/data.
- Do not add DB fields unless absolutely necessary.
- Preserve existing table layout and actions.
- Must work on desktop and mobile.
- Must not break status dropdown or action menu.

Please identify what data fields are already available for each service record and what can be shown in the expanded detail.

3. New vehicle not appearing in Service form
Investigate why a newly added vehicle may not appear/select in Add Service Record:
- Is the vehicle list fetched from the correct endpoint?
- Is there pagination or limit?
- Is there a status filter?
- Is there a location/category/model filter?
- Is the dropdown searchable by plate?
- Does it use cached data that may not refresh?
- Does it only load vehicles on first page?
- Does the backend endpoint include recently created active vehicles?

4. Safety requirements
- Do not modify booking lifecycle.
- Do not modify fleet creation logic unless the bug proves it is needed.
- Do not change DB schema unless absolutely necessary.
- Prefer frontend/API query fix over large refactor.
- Keep changes surgical.

5. Output
Return:
- Exact files involved
- Root cause hypothesis for the new-vehicle selector issue
- Whether the issue is confirmed from code
- Minimal implementation plan
- Risk assessment
- Whether this should be BUILD or needs more clarification