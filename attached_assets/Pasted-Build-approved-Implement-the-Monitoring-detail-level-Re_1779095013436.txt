Build approved.

Implement the Monitoring detail-level Review Request action exactly according to the approved plan.

Change only:
artifacts/crm/src/pages/Monitoring.tsx

Goal:
Inside the Monitoring expanded row / booking detail panel, add a “Review request” action so staff can send the review request from the detail view too, not only from the row-level action.

Requirements:
1. Reuse the existing SendMailDialog / Review Request modal.
2. Do NOT create a second duplicate dialog.
3. Do NOT create a new endpoint.
4. Do NOT change backend/API/email logic.
5. Pass a callback into ExpandedRow so clicking the detail-level button calls the same existing flow:
   setMailRow(row)
6. Add emailEnabled prop if needed so the detail-level button can follow the same availability logic.
7. The action should be visible only for RETURNED bookings.
8. The button should be disabled if:
   - email sending is not configured
   - customer email is missing
9. Button label:
   Review request
10. Use the same styling as the existing Monitoring row action button as much as possible.
11. Place it at the bottom of the ExpandedRow detail panel, below the two-column details grid, right-aligned.

Tooltips / titles:
- If email is not configured:
  Email sending is not configured
- If no customer email:
  No customer email on file
- Otherwise:
  Send review request email

Hard protections:
- Do NOT touch website files.
- Do NOT touch API/backend.
- Do NOT touch database/schema/migrations.
- Do NOT touch pricing, payment, booking creation, public booking, PDF, email templates, sitemap, robots, SEO, or Google Ads.
- Do NOT change Trustpilot BCC logic.
- Do NOT expose TRUSTPILOT_BCC_EMAIL in frontend.
- Do NOT allow non-RETURNED bookings.
- Do NOT add automatic sending.
- Do NOT modify BookingDetail in this task.
- Do NOT edit any file except Monitoring.tsx.

After implementation:
- Run CRM typecheck/build check appropriate for this workspace.
- Report exact files changed.
- Confirm only Monitoring.tsx changed.
- Confirm backend/API/email files were not touched.
- Confirm the detail-level button reuses the existing SendMailDialog.
- Confirm no new endpoint/dialog was added.