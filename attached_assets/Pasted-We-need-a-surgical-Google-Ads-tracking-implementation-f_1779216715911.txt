We need a surgical Google Ads tracking implementation for the public website only.

This is a LIVE production TBILISICARS CRM&WEB platform. Do not make broad refactors or unrelated changes.

Goal:
Add Google Ads Booking Completed conversion tracking for real successful booking submissions.

Google Ads conversion data:
- Google Ads tag ID: AW-18043925820
- Conversion label: Sy7mCPP_5q8cELzqgZxD
- Full send_to: AW-18043925820/Sy7mCPP_5q8cELzqgZxD
- Conversion action name in Google Ads: Booking Completed
- Category: Submit lead form
- Value: fixed USD 1
- Count: One

Strict scope:
Modify/create only these files if possible:
1. Create artifacts/website/src/lib/google-ads.ts
2. Modify artifacts/website/index.html
3. Modify artifacts/website/src/pages/Booking.tsx

Do not modify:
- backend/API
- CRM
- database/schema/migrations
- pricing logic
- payment logic
- email/PDF generation
- booking creation logic
- booking UX/steps/validation
- artifacts/website/src/App.tsx
- artifacts/website/src/main.tsx

Implementation requirements:
1. Add Google Ads base tag to artifacts/website/index.html inside <head>.
2. Create artifacts/website/src/lib/google-ads.ts.
3. The utility must:
   - define Google Ads ID AW-18043925820
   - define conversion label Sy7mCPP_5q8cELzqgZxD
   - expose trackBookingConversion(bookingId: number, reference: string): void
   - no-op safely if window.gtag is unavailable
   - wrap all tracking logic in try/catch
   - never throw and never affect booking submission
   - use sessionStorage duplicate protection based on bookingId, fallback only if needed
   - send conversion with:
     send_to: "AW-18043925820/Sy7mCPP_5q8cELzqgZxD"
     value: 1.0
     currency: "USD"
     transaction_id: String(bookingId)

4. In artifacts/website/src/pages/Booking.tsx:
   - import trackBookingConversion from "@/lib/google-ads"
   - call it only after successful apiFetch("/api/public/bookings", ...)
   - call it before setResult(data)
   - do not change payload, validation, booking state, pricing, UI, or API logic

Important:
- Conversion must not fire on page view.
- Conversion must not fire on booking step navigation.
- Conversion must not fire on car selection.
- Conversion must not fire on quote calculation.
- Conversion must fire only after the server confirms booking creation.
- If Google script is blocked or fails, booking must still complete normally.

Return PLAN ONLY first:
- exact files to change
- exact change summary
- risk controls
- test commands
- manual verification steps
Do not implement yet.