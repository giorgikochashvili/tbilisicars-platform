Switching to Build mode.

Proceed with this Fleet Calendar UI polish according to the plan, with these exact safeguards.

Hard scope:
Change only:
artifacts/crm/src/pages/FleetCalendar.tsx

Do not change any other files.
No API changes.
No DB migration.
No new dependencies.
No booking/payment/accounting/parking/service logic changes.
No website changes.

Required changes:

1. Free / neutral plate color
- Change free/neutral vehicle plate color from orange to neutral white/soft foreground.
- Keep active/rented plate green.
- Keep upcoming/scheduled plate blue.
- Keep service wrench orange.
- Do not color the full row.

2. Model + plate inline layout
- On desktop/tablet where there is enough width, show model + license plate on one line.
- Example: Prius 4th AC531AC
- Model text may truncate, but plate should remain visible.
- On narrow mobile widths, keep a safe responsive fallback if needed.
- Do not let text overflow the sticky label column.

3. Reduce row/label height
- Reduce vehicle row height as much as safely possible while keeping the design readable.
- Target compact row height around 36px if it fits cleanly.
- Adjust booking bar height safely if needed so bars remain visually clean and clickable.
- Keep row design professional, not cramped or ugly.

4. Fix left label column bleed/overlap
- Booking bars must never visually cross over the left sticky model/plate column.
- Increase/adjust z-index so left sticky vehicle label cells and group/category label cells stay above booking bars.
- Reduce booking bar hover z-index if needed.
- Left label column must stay solid/opaque during hover and horizontal scroll.
- This must fix the issue where old/returned booking bars appear over the model/plate field.

5. Board/timeline zoom controls
- Add compact zoom controls near the top controls area:
  - minus
  - current percentage
  - plus
- Use steps: 80%, 90%, 100%, 110%, 120%.
- Default: 100%.
- Persist selected zoom in localStorage if safe.
- Do NOT use CSS transform scale on the whole page or scroll container.
- Implement zoom by adjusting effective timeline/day column width and related bar/date positioning.
- Sticky date header, sticky left column, horizontal scroll, and internal vertical scroll must continue working.
- Zoom must not affect the whole CRM page, sidebar, or unrelated layout.

6. Preserve existing behavior
- Default Group by Category remains.
- Category/model ordering remains.
- Service wrench logic remains.
- Wrench click still opens /crm/service?vehicleSearch=PLATE and must not open VehicleDetail.
- VehicleDetail click remains.
- BookingDetail click remains.
- Parking tooltip remains.
- Booking tooltip remains.
- Conflict/overdue logic remains.
- 60-day range remains.
- Mobile/tablet responsive behavior remains usable.

Important responsive rules:
- Desktop/tablet should use inline model + plate when there is enough width.
- Very narrow mobile can keep safe stacked/truncated behavior if inline would break layout.
- No body/page horizontal overflow.
- Calendar board remains the internal scroll surface.

After implementation:
- Run CRM typecheck/build check as appropriate.
- Confirm only FleetCalendar.tsx changed.
- Confirm no excluded logic/files were touched.
- Report manual verification steps:
  1. free plate is neutral/white, not orange
  2. model + plate inline on desktop/tablet
  3. row height is more compact
  4. booking bars no longer bleed over left label column
  5. zoom controls work from 80% to 120%
  6. sticky header/left column still work
  7. booking and vehicle clicks still work
  8. mobile/tablet remain usable