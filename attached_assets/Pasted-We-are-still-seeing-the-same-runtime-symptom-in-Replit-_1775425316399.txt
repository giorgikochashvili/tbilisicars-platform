We are still seeing the same runtime symptom in Replit Preview after your previous “fix” report.

CURRENT REAL SYMPTOM
- CRM → Bookings → Import Voucher
- Upload succeeds and UI moves to Review step
- But Review still always shows:
  “Failed to extract data from file. Please fill in details manually.”
- Required unresolved fields remain empty:
  contactFullName, pickupLocation, dropoffLocation, pickupDatetime, dropoffDatetime
- So the runtime behavior has NOT changed.

IMPORTANT
Do NOT give another theoretical explanation.
Do NOT say “this should be fixed”.
Do NOT stop at code inspection.
Prove the actual runtime path being executed in Replit Preview right now.

WORK ONLY ON THIS FLOW
- artifacts/api-server/src/routes/admin-voucher-import.ts
- artifacts/api-server/src/services/admin-voucher-import.service.ts
- lib/integrations-openai-ai-server/src/client.ts
- any workflow/run configuration that affects the API process in Replit Preview

GOAL
Determine why Preview still returns fallback even after the previous claimed fixes.

PHASE 1 — VERIFY ACTUAL RUNTIME, NOT JUST CODE
1. Confirm the API process used by Replit Preview is running the latest code.
2. Confirm whether changes inside lib/integrations-openai-ai-server/src/client.ts are actually being picked up by the running API process.
3. Confirm whether the API imports this library from source or from stale built output.
4. Confirm whether a rebuild/restart of the relevant workspace package is required in this monorepo for Preview to use the fix.
5. Confirm whether Preview is hitting the same local API process you are auditing.

PHASE 2 — ADD RUNTIME-PROOF LOGS ONLY IN VOUCHER EXTRACTION
Add precise temporary logs in the voucher extraction path and then use them to prove the exact failure point:
- before OpenAI client creation / call:
  - which baseURL is being used
  - whether AI_INTEGRATIONS_OPENAI_API_KEY is dummy
  - whether OPENAI_API_KEY fallback is selected
  - model name
  - mimeType
  - base64 length
- after OpenAI call succeeds:
  - content existence
  - content length
  - first 300 chars
- in catch:
  - err.name
  - err.status
  - err.message
  - err.response?.data
- in parseExtractionJson:
  - regex matched or not
  - JSON.parse success/failure
  - parsed keys count

These logs must be narrow and only for this flow.

PHASE 3 — SELF-TEST THE ENDPOINT FOR REAL
Do not stop at static reasoning.
Actually test the endpoint in the running Replit environment with a real uploaded voucher image or PDF and prove one of these:
- OpenAI call throws
- OpenAI returns empty/non-JSON
- parser fails
- stale code/build is being used
- wrong runtime config path is still active

PHASE 4 — APPLY ONLY THE CONFIRMED MINIMAL FIX
Only after proving the cause at runtime:
- fix the exact issue
- keep manual fallback behavior
- keep endpoint contract unchanged
- do not touch unrelated CRM/UI/booking/database logic

CRITICAL
If the previous claimed fix is correct in code but not reflected in runtime, then investigate stale build / workspace linkage / preview process / restart requirements and fix that exact problem.

RETURN FORMAT
Return a short exact report with:
1. confirmed runtime failure point
2. exact file(s) changed
3. whether stale runtime/build was involved
4. exact minimal fix applied
5. exact commands / steps used to verify in Replit Preview
6. exact sample response after successful extraction