You are implementing a safe CRM booking data cleanup in the existing Tbilisicars monorepo.

STRICT RULES:
- This is a LIVE production-connected project
- Do NOT change architecture
- Do NOT break booking creation
- Do NOT break CRM booking detail
- Do NOT remove useful data from storage unless it is clearly redundant and already mapped properly
- Do NOT refactor unrelated code
- Keep all changes surgical and minimal
- Preserve backward compatibility for existing bookings

GOAL:
Stop exposing ugly raw website metadata dumps in CRM/customer-facing booking presentation, while preserving the actual data cleanly and safely.

CURRENT PROBLEM:
Some bookings carry note-style raw text like:

[WEBSITE DATA] Nationality: Georgia Age: 31 WhatsApp: Yes (phone number above) Payment Method: Cash Insurance: Full

This is ugly, unstructured, and should not appear as a messy concatenated metadata dump in CRM booking presentation or customer-facing output.

BUSINESS GOAL:
- Keep the useful information
- Do NOT show it as a raw ugly dump
- Display/store it in a cleaner structured way if the system already has suitable fields
- If some values do not yet have proper dedicated fields, handle them safely without breaking the flow

IMPORTANT:
This task should FIRST determine what is already stored in structured fields vs what is only embedded inside notes.
Do NOT assume.
Inspect the actual flow before proposing changes.

CHECK THESE AREAS FIRST:
- website booking submission flow
- public booking create request payload
- API booking creation route/service
- any logic that appends [WEBSITE DATA] ... into notes
- CRM booking detail / overview rendering
- email/voucher rendering only if the same raw notes are reused there
- DB schema for booking/customer fields related to nationality, age, WhatsApp, payment method, insurance

LIKELY FILE AREAS TO INSPECT:
- artifacts/website/src/pages/Booking.tsx
- artifacts/api-server/src/routes/public-bookings.ts
- artifacts/api-server/src/services/admin-bookings.service.ts
- booking insert / mapping logic in api-server
- artifacts/crm/src/pages/BookingDetail.tsx or equivalent booking detail page/component
- lib/db schema for booking/customer
- any helper that parses/normalizes website booking notes

TASK BREAKDOWN:

--------------------------------------------------
1. TRACE THE SOURCE OF [WEBSITE DATA]
--------------------------------------------------

Identify exactly:
- where the [WEBSITE DATA] string is created
- which fields are concatenated into it
- where it is stored
- where it is displayed

Return the exact source file(s) and code path.

Do NOT propose fixes before confirming this.

--------------------------------------------------
2. AUDIT WHICH VALUES ALREADY HAVE PROPER STRUCTURED HOMES
--------------------------------------------------

For each of these values:
- Nationality
- Age
- WhatsApp opt-in
- Payment Method
- Insurance

Determine:
- is it already stored in a dedicated DB/API field?
- is it only embedded inside notes?
- is it already shown elsewhere in CRM in a structured way?

Goal:
avoid duplicate/ugly note dumps when the same data already exists elsewhere.

--------------------------------------------------
3. SAFE CLEANUP STRATEGY
--------------------------------------------------

Prefer the safest approach:

A)
If a value already has a proper structured field and is already visible in the CRM in a clean place:
- stop duplicating it into ugly notes

B)
If a value is useful but currently only exists inside the raw note dump:
- do NOT lose it
- propose the smallest safe improvement
- this may mean keeping it stored but not rendering the raw block directly
- or parsing/displaying it in a cleaner way if that is safer than a schema change

C)
Do NOT do a broad schema redesign in this task unless absolutely necessary.

--------------------------------------------------
4. CRM PRESENTATION CLEANUP
--------------------------------------------------

Clean the CRM booking presentation so staff do not see messy raw metadata blocks.

Preferred outcome:
- booking notes remain for true free-text notes
- website metadata is either:
  - shown in structured fields, or
  - hidden/cleaned/parsing-rendered safely

Do NOT remove genuine manual notes entered by staff or customers.

--------------------------------------------------
5. BACKWARD COMPATIBILITY
--------------------------------------------------

Existing old bookings may already contain the raw [WEBSITE DATA] note text.

Plan must explain:
- how old bookings will behave
- whether old raw notes will still exist in DB
- whether CRM display will hide or normalize them
- how to avoid data loss

--------------------------------------------------
6. OUT OF SCOPE
--------------------------------------------------

- No broad CRM redesign
- No risky DB migration unless truly required
- No unrelated booking flow changes
- No promo/discount logic changes
- No email/voucher redesign unless raw metadata is confirmed to leak there too

--------------------------------------------------
7. DONE LOOKS LIKE
--------------------------------------------------

- raw [WEBSITE DATA] dumps are no longer shown messily in CRM/customer-facing presentation
- useful metadata is preserved
- duplicate ugly note content is removed or hidden safely
- booking creation still works
- old bookings remain readable
- no broken booking detail
- no TypeScript/runtime errors

--------------------------------------------------
8. OUTPUT FORMAT
--------------------------------------------------

Return:
1. exact source of the [WEBSITE DATA] dump
2. which values already have structured homes and which do not
3. safest cleanup strategy
4. exact files to change
5. whether this should go directly to Build or needs a Revise step