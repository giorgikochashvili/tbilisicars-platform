# FIX REPLIT PREVIEW ONLY — verify ports, run chain, and make Preview work without affecting production

We are working on a LIVE production-connected project, but this task is ONLY about fixing Replit Preview in the new Replit account.

## Confirmed current audit facts
- API Server runs on port 8080
- Website runs on port 5000
- CRM runs on port 3000
- CRM uses root-relative /api/admin/... calls
- Website uses root-relative /api/... calls
- Replit dev API routing is currently ambiguous / not verified
- Production DigitalOcean behavior must remain untouched

## Mission
Make Replit Preview work correctly for:
- Website
- CRM
- API-backed browser requests

But do this with the smallest safe changes only.

## Strict rules
- Do NOT change backend business logic
- Do NOT change booking logic
- Do NOT change pickup/dropoff logic
- Do NOT change dashboard logic
- Do NOT change production deployment files or behavior
- Do NOT change package versions
- Do NOT refactor unrelated code
- Do NOT guess ports — verify current actual run/config first
- If a config already exists, merge minimally instead of replacing

## Required investigation first
Before editing anything, verify all of the following from the actual codebase:
1. Exact run commands used for API, Website, CRM in Replit
2. Exact PORT and BASE_PATH expectations in:
   - artifacts/api-server/src/index.ts
   - artifacts/crm/vite.config.ts
   - artifacts/website/vite.config.ts
3. Whether current Replit preview opens one app only or a combined workflow
4. Whether CRM and Website already have any dev proxy/server config
5. Whether .replit / workflows / run buttons already define port exposure rules

## Minimal fix goal
If the root cause is confirmed to be missing dev proxy/routing for /api requests, then apply ONLY the minimal fix needed so that:
- CRM browser requests to /api/... reach API port 8080 in Replit dev
- Website browser requests to /api/... reach API port 8080 in Replit dev
- Existing BASE_PATH behavior remains correct
- Production build behavior remains unchanged

## Preferred safe fix path
Prefer a DEV-ONLY Vite proxy in:
- artifacts/crm/vite.config.ts
- artifacts/website/vite.config.ts

Proxy:
- "/api" -> "http://127.0.0.1:8080"

Use:
- changeOrigin: true

If server config already exists, merge only the missing proxy section.
Do not remove existing settings.

## Also verify Preview entry behavior
If Replit Preview is opening the wrong app or wrong port, identify the minimal Replit-side adjustment needed.
But do NOT change Replit config unless it is truly necessary after verifying vite proxy alone is insufficient.

## Validation required
After the fix, verify all of the following:
- API still runs on 8080
- Website still runs on 5000
- CRM still runs on 3000
- Website preview loads
- CRM preview loads
- Browser /api requests succeed from Website
- Browser /api requests succeed from CRM
- No unrelated files changed
- No production behavior changed
- No build errors introduced

## Output format
Respond in this exact structure:

### Root cause
<confirmed cause only>

### Files changed
- <exact path>
- <exact path>

### Changes made
- <minimal exact change>
- <minimal exact change>

### Validation
<what was tested>

### Non-goals confirmed
- No backend business logic changed
- No production deployment behavior changed
- No unrelated files changed