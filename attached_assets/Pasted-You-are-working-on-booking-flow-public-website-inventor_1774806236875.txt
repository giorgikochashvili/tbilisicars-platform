You are working on booking flow + public website inventory.

CRITICAL:
- Do NOT break booking logic
- Do NOT break API contracts
- Fix UX + data consistency only

-----------------------------------
TASK 1 — CALENDAR POSITION FIX
-----------------------------------

Fix datepicker popup:

- Must open aligned to input
- Must not overflow container
- Must not be cut off
- Must have proper z-index

Use:
- portal rendering OR
- correct absolute positioning

-----------------------------------
TASK 2 — CALENDAR UI POLISH
-----------------------------------

Improve:
- spacing
- shadow
- border
- overall visual quality

Make it feel premium

-----------------------------------
TASK 3 — TIME PICKER REWORK
-----------------------------------

Replace current time list with:

Two selectors:

1. Hour (00–23)
2. Minutes (only):
   - 00
   - 15
   - 30
   - 45

Result format:
HH:MM

Reduce scrolling
Improve usability

-----------------------------------
TASK 4 — WEBSITE VEHICLE VISIBILITY FIX
-----------------------------------

ISSUE:
Vehicles exist in CRM but not showing on website

Fix pipeline:

1. Check:
   - vehicle isActive
   - vehicle locationId
   - model relation
   - pricing availability

2. Fix public inventory query:
   - Must filter by selected location
   - Must include correct region vehicles

Example:
If user selects:
"KUTAISI INTERNATIONAL AIRPORT"

Return ONLY Kutaisi vehicles

-----------------------------------
TASK 5 — REGION-BASED INVENTORY
-----------------------------------

Ensure website booking uses region logic:

- Tbilisi → only Tbilisi fleet
- Kutaisi → only Kutaisi fleet
- Batumi → only Batumi fleet

No cross-region leakage

-----------------------------------
TASK 6 — DEBUG ROOT CAUSE
-----------------------------------

Find why Kutaisi vehicles are not shown:

Check:
- location mapping mismatch
- wrong filter in API
- missing join (vehicle → location)
- incorrect availability logic

Fix WITHOUT breaking working regions