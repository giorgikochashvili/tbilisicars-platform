This is a LIVE production-connected CRM system. Do NOT refactor unrelated code. Do NOT change architecture. Do NOT change backend logic. Do NOT add new features.

We are working ONLY on mobile / responsive stabilization for existing CRM pages.

SCOPE
Fix the current mobile layout problems on these existing pages/components only:
1. Booking Detail dialog/page
2. Booking Detail → Overview Edit mode
3. Bookings page mobile layout
4. Dashboard mobile layout overflow

CURRENT PROBLEMS

A) Booking Detail mobile view
- Some bookings render correctly, others become visually broken depending on content length
- Customer / Vehicle / Booking Price / Pickup / Dropoff / buttons can overlap or appear mixed
- In some cases dropoff/pickup text breaks badly and becomes vertically fragmented
- WhatsApp icon / action button alignment can become inconsistent

B) Booking Detail → Overview Edit mobile
- Labels and fields overlap
- Pickup/dropoff fields collide
- Date/time inputs collide
- Location fields collide
- Price/currency fields collide
- The form is not usable on small screens

C) Bookings page mobile
- Layout must stay stable on small screens
- Filters must stack cleanly
- Cards/list rows must not overlap
- No horizontal overflow
- Existing information must remain readable and usable

D) Dashboard mobile
- There is horizontal overflow
- The page can scroll sideways
- Top controls (especially Customize + region tabs) likely cause width overflow
- The responsive layout must be reworked so the page never expands beyond viewport width

IMPORTANT RULES
- Do NOT redesign desktop layout
- Do NOT change booking logic
- Do NOT change data flow
- Do NOT change API behavior
- Do NOT introduce new UX features
- Only stabilize responsive/mobile layout of existing UI
- Preserve current desktop behavior unless a tiny directly related CSS/layout correction is absolutely required

STEP 1 — ANALYZE CURRENT COMPONENTS
Identify:
- Which components render Booking Detail read mode
- Which components render Overview Edit form
- Which components render mobile Bookings layout
- Which Dashboard rows/containers are causing horizontal overflow

STEP 2 — FIND THE REAL ROOT CAUSES
Check carefully for:
- bad flex/grid behavior
- min-width issues
- nowrap / break-all / overflow issues
- fixed widths that exceed viewport
- row layouts that should stack on mobile
- containers that are wider than screen because of controls/filters/tabs

STEP 3 — PLAN MINIMAL SAFE FIX
Before building, explain:
1. Which files/components will be changed
2. What is causing the Booking Detail mobile breakage
3. What is causing Overview Edit mobile breakage
4. What is causing Dashboard horizontal overflow
5. Exactly what responsive/layout fixes you will apply
6. Confirmation that desktop behavior will be preserved

STEP 4 — ACCEPTANCE CRITERIA
- Booking Detail mobile read mode is stable and readable across long and short data
- No overlapping labels/values/buttons/icons
- Overview Edit is fully usable on mobile
- Bookings page mobile has no broken stacking or horizontal overflow
- Dashboard mobile no longer scrolls sideways
- Desktop remains visually intact
- No backend/API changes
- No feature additions