You are working inside an EXISTING production car rental monorepo project.

Your task is to implement ONLY the changes described below.
Do not refactor unrelated parts.
Do not rename unrelated files.
Do not change business logic outside the requested scope.
Preserve the current dark premium design language of the CRM and the current public website architecture.

IMPORTANT:
- First inspect the existing implementation and reuse current UI patterns/components where possible
- Make changes carefully and consistently across the system
- Do not invent new architecture if an existing shared input/select/upload component already exists
- After changes, run a factual check for build/type errors and fix only issues caused by your changes
- This is a real project already built largely inside Replit, so do not disturb working logic outside the requested scope

==================================================
TASK A — CATEGORY DROPDOWN IN ADD/EDIT MODEL
==================================================

Problem:
In the Add Model / Edit Model modal, the Categories dropdown is too tall and not well contained visually.

Required result:
1. The Categories dropdown must become scrollable.
2. It must visually match the current CRM dark design.
3. The dropdown panel must have:
   - max-height
   - overflow-y auto
   - smooth scrolling
   - proper spacing
   - visually clean hover/selected states
4. It must not break the modal layout on smaller screens.
5. The selected value must remain clearly visible inside the field.
6. Keep the current category values unless they are already sourced from a shared enum/config.

Apply the same dropdown behavior/style to similar long selects in the CRM where appropriate, but ONLY where the same issue exists. Do not redesign unrelated short selects.

==================================================
TASK B — NUMBER INPUTS: REMOVE SCROLL-WHEEL VALUE CHANGES
==================================================

Problem:
Number inputs currently allow accidental value changes via mouse wheel / scroll while focused.
Example: Seats / Doors / Luggage and similar numeric CRM fields.

Required result:
1. Wherever similar numeric fields exist in the CRM, scrolling the mouse wheel must NOT change the value.
2. Keep the input as a clean typed field where the user manually enters the number.
3. Do not rely on browser default spinner UX.
4. Make this consistent across the CRM for similar numeric inputs.
5. Preserve validation and form submission behavior.
6. Prefer a shared safe solution if the project already has shared form/input components.
7. Do NOT break keyboard typing, tab navigation, arrow key handling, or mobile usability.

Target behavior:
- User can click into the field and type
- Mouse wheel over the focused field should not increment/decrement the value
- This should be true throughout the system for comparable number inputs

==================================================
TASK C — FLEET SEARCH MUST SEARCH MORE THAN PLATE
==================================================

Problem:
Current Fleet search appears limited to plate number only.

Required result:
Expand Fleet search so that the search input matches all relevant vehicle identity fields, including at minimum:
1. Plate number
2. Vehicle model name
3. Brand name

If data structure allows safely, also include:
4. Combined brand + model text
5. Techpassport number or internal vehicle identifier if already present in the UI/data source

Search requirements:
- Case-insensitive
- Partial match
- Trim surrounding spaces
- Works together with existing filters (region/status/category/etc.)
- Does not break current Fleet page behavior
- Keeps current UI look and placeholder style unless a minimal wording update is needed

If the current backend/API already supports only plate search:
- extend it carefully to include brand/model search
If the current Fleet filtering is frontend-side:
- update the filtering logic there

Before changing code, determine where the search is actually executed:
- frontend filtering
- backend query
- or both

Then implement the minimal correct fix.

==================================================
TASK D — SEARCH PLACEHOLDER / UX
==================================================

If the current Fleet search placeholder says only “PLATE NUMBER...” or similar, update it to reflect broader search behavior.
Use a short premium-looking placeholder such as:
- Search plate, brand, model

Do not over-explain in the UI.

==================================================
TASK E — MODEL IMAGE UPLOAD (CREATE / EDIT MODEL)
==================================================

Problem:
When creating or editing a vehicle model, there is currently no proper field for uploading a model image that can be reused consistently in the CRM and on the public website.

Required result:
Add support in both Add Model and Edit Model for uploading ONE image for the model.

Scope:
1. In the Add Model modal, add an image upload area/field.
2. In the Edit Model modal, add the same image upload area/field.
3. The uploaded image belongs to the MODEL (not the brand logo field unless the current architecture forces reuse safely).
4. This should represent the model’s main display image.

Behavior requirements:
1. User can upload one image for the model.
2. If an image already exists, Edit Model should show the current image preview.
3. User should be able to replace the current image with a new one.
4. If the project already has existing upload/storage logic, reuse it.
5. Do not invent a second competing upload system if one already exists.
6. Preserve validation and save/update flows.

Data/storage requirements:
1. First inspect the current schema/API/types to determine whether model image support already partially exists.
2. If model image field already exists somewhere, wire it correctly end-to-end.
3. If it does NOT exist, add the minimal correct implementation needed across:
   - schema
   - backend API
   - validation/types
   - CRM form state
   - save/update handlers
4. Use a clear field name consistent with the current codebase, such as modelImageUrl or imageUrl only if naming matches existing patterns.
5. Do not break existing brand logo behavior. Brand logo and model image are separate concepts unless the existing codebase explicitly combines them.

UI requirements in CRM:
1. Add a clean dark-theme upload block/field inside Add Model and Edit Model.
2. Show uploaded image preview in the form.
3. In the Models list/table/cards inside Fleet Management, show a small thumbnail for each model where image exists.
4. Thumbnail should be visually neat and aligned with the current CRM style.
5. If no model image exists, use current fallback behavior or a safe placeholder consistent with current design.

==================================================
TASK F — PUBLIC WEBSITE MUST USE THE MODEL IMAGE
==================================================

Problem:
The public website should show the model image that was uploaded from the CRM model form.

Required result:
1. On the website, each vehicle/model card should display the uploaded model image as the primary visible image.
2. Use exactly ONE main image per model from the CRM-uploaded image field.
3. If the website currently uses a different image source, inspect carefully and update only the required path/data flow.
4. If model image is missing, keep a safe fallback and do not break existing cards/layouts.
5. Do not redesign the website card UI beyond what is required to display the correct uploaded model image.

Implementation requirements:
1. Inspect how website vehicle/model data is currently fetched.
2. Ensure the model image field is exposed through the necessary backend/public API layer.
3. Ensure the website frontend receives and renders that image.
4. Keep the existing booking/fleet/public page logic intact.

==================================================
TASK G — IMPLEMENTATION DISCIPLINE
==================================================

Follow this order:

Step 1:
Inspect the current implementation and identify exact files involved for:
- model create/edit UI
- fleet list rendering
- fleet search logic
- numeric shared input logic
- model/vehicle public website card rendering
- upload/storage flow
- schema/API/type definitions

Step 2:
Implement only the requested changes.

Step 3:
Run factual validation:
- typecheck/build if available
- identify errors caused by your edits
- fix only those errors

Do NOT do unrelated cleanup/refactors.

==================================================
IMPORTANT CONSTRAINTS
==================================================

1. Reuse existing shared components if present.
2. Keep styling consistent with current dark CRM theme.
3. Avoid touching unrelated booking, dashboard, accounting, payments, parking, or admin AI code unless required by the data path.
4. Do not make speculative changes.
5. Do not silently change business logic.
6. If schema changes are necessary, make the minimal safe change only.
7. Keep the implementation production-safe and coherent with the existing project.

==================================================
OUTPUT FORMAT
==================================================

When done, return a PRECISE factual report with:

1. Files changed
2. What was changed in each file
3. Whether Fleet search is frontend-side or backend-side
4. Exact fields now included in Fleet search
5. Whether number wheel-change protection was applied globally/shared or per field
6. Confirmation that category dropdown is now scrollable
7. How model image upload was implemented
8. Exact model image field name used in schema/API/frontend
9. Where model image now appears in CRM
10. Where model image now appears on the website
11. Any schema/API changes made
12. Any build/type issues found and how they were resolved

Do not give a vague summary.
Be precise and factual.
Do not omit technical details.