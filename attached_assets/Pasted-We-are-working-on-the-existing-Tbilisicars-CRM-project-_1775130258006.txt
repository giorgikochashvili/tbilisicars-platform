We are working on the existing Tbilisicars CRM project.

Context:
- Operations Center page (Dashboard) has Pickups and Dropoffs tables
- Currently columns overflow horizontally
- Important fields like TIME are not visible without horizontal scroll
- Mobile layout is also broken and hard to read

Goal:
Fix Operations (Pickups & Dropoffs) layout so that:
1. ALL important data is visible WITHOUT horizontal scrolling
2. Layout is compact, clean, and readable
3. Works perfectly on both desktop and mobile
4. No data loss, only layout/UI restructuring

STRICT RULES:
- Do NOT change backend logic or API
- Do NOT remove any data fields
- Only adjust UI + layout
- Do NOT break existing dashboard structure
- Reuse existing components where possible

----------------------------------

# PLAN

1. Replace table-based layout with responsive grid/card layout
2. Prioritize fields visibility
3. Make desktop fit all columns in container width
4. Convert mobile rows into stacked cards
5. Ensure TIME is always visible
6. Remove horizontal scroll completely

----------------------------------

# FILES TO TOUCH

- artifacts/crm/src/pages/Dashboard.tsx
OR
- artifacts/crm/src/components/dashboard/Operations.tsx
(depending on current structure)

- Any related table component used for Pickups/Dropoffs

----------------------------------

# IMPLEMENTATION

## 1. REMOVE HORIZONTAL TABLE OVERFLOW

- Find container with:
  overflow-x: auto OR scroll
- REMOVE horizontal scrolling behavior

----------------------------------

## 2. DESKTOP LAYOUT (IMPORTANT)

Replace table layout with CSS grid:

Each row should use:

grid-template-columns:
- Ref (small)
- Client (flex)
- Phone (fixed small)
- Vehicle (flex)
- Amount (small)
- Route (small)
- Time (small)

Example:
grid grid-cols-[60px_1.5fr_1fr_1.5fr_0.8fr_0.8fr_0.8fr]

Ensure:
- text is truncated with ellipsis where needed
- no column pushes layout outside container

----------------------------------

## 3. PRIORITY FIX

Columns MUST be visible without scroll:

ORDER:
- REF
- CLIENT
- PHONE
- VEHICLE
- AMOUNT
- ROUTE
- TIME (CRITICAL)

If needed:
- reduce font-size slightly
- reduce padding
- truncate long text (vehicle names)

----------------------------------

## 4. MOBILE LAYOUT (VERY IMPORTANT)

On mobile (sm and below):

Replace table rows with CARD layout:

Each booking becomes a card:

Structure:

Top row:
- Client Name (bold)
- Ref (#25)

Second row:
- Vehicle + plate

Third row:
- Phone

Fourth row:
- Route (KUT AIR → TBS DT)

Bottom row:
- Amount (left)
- Time (right, highlighted)

Use:
flex flex-col gap-1

Make it VERY readable without zoom/scroll

----------------------------------

## 5. VISUAL IMPROVEMENTS

- Align everything vertically center
- Reduce row height slightly
- Use subtle separators instead of heavy borders
- Keep dark theme intact
- Ensure consistent spacing

----------------------------------

## 6. EMPTY STATE

Dropoffs empty state:
- Center message properly
- Keep spacing consistent with Pickups

----------------------------------

# DONE

After implementation:

1. No horizontal scroll exists
2. Time is always visible
3. Desktop looks compact and aligned
4. Mobile shows card-based layout (clean & readable)
5. No backend changes
6. Only CRM frontend affected