Approved for Build — Phase 2A only.

Proceed exactly with the approved Phase 2A plan.

Strict scope:
- Implement new booking payment types:
  - ADDITIONAL_PAYMENT
  - EXTRA_DAYS_PAYMENT
- Keep all old DB enum values for historical records:
  - DEPOSIT_RECEIVED
  - DEPOSIT_RETURNED
  - REFUND
  - ADJUSTMENT
- Do NOT delete, rename, or migrate old historical payment values.
- Do NOT implement Phase 2B extra charge / extension UI.
- Do NOT implement Advance Payment / Pending Receivables.
- Do NOT implement Deposit field.
- Do NOT implement Fixed Monthly Expenses.
- Do NOT change permissions or role guards.
- Do NOT touch public website.
- Do NOT touch pricing engine or public booking flow.
- Do NOT deploy.
- Do NOT run production DB migrations.
- Do NOT perform Git Sync or Droplet build.

Files allowed:
1. lib/db/migrations/0008_booking_payment_types.sql
2. lib/db/src/schema/accounting.ts
3. artifacts/api-server/src/services/admin-booking-payments.service.ts
4. artifacts/api-server/src/routes/admin-booking-payments.ts
5. artifacts/api-server/src/services/admin-accounting.service.ts
6. artifacts/crm/src/pages/BookingDetail.tsx
7. artifacts/crm/src/pages/Accounting.tsx

Implementation details:

1. DB migration file:
Create:
lib/db/migrations/0008_booking_payment_types.sql

Content should only add enum values idempotently:
ALTER TYPE booking_payment_type_enum ADD VALUE IF NOT EXISTS 'ADDITIONAL_PAYMENT';
ALTER TYPE booking_payment_type_enum ADD VALUE IF NOT EXISTS 'EXTRA_DAYS_PAYMENT';

2. Schema:
Update bookingPaymentTypeEnum in lib/db/src/schema/accounting.ts to include:
- ADDITIONAL_PAYMENT
- EXTRA_DAYS_PAYMENT

3. Backend payment service:
Update PaymentType union.
Update PAYMENT_TYPE_ACCOUNTING:
- ADDITIONAL_PAYMENT → INCOME / "Extra Payment"
- EXTRA_DAYS_PAYMENT → INCOME / "Extra Days Payment"

Update getBookingPaymentSummary() so totalPaid and totalPaidOriginal include:
- BOOKING_PAYMENT
- ADJUSTMENT historical compatibility
- ADDITIONAL_PAYMENT
- EXTRA_DAYS_PAYMENT

Do not change updateBookingPaymentStatus() unless TypeScript requires it, because it should continue using summary.totalPaid.

4. Backend payment route:
Update POST/create validation so new payments allow only:
- BOOKING_PAYMENT
- ADDITIONAL_PAYMENT
- EXTRA_DAYS_PAYMENT

Important:
This whitelist must affect only creation of new payments.
GET payment history and DELETE existing payments must continue working for old historical payment rows with:
- DEPOSIT_RECEIVED
- DEPOSIT_RETURNED
- REFUND
- ADJUSTMENT

Update audit action/label maps for:
- ADDITIONAL_PAYMENT
- EXTRA_DAYS_PAYMENT

5. Accounting categories:
Add "Extra Days Payment" to INCOME_CATEGORIES in:
- artifacts/api-server/src/services/admin-accounting.service.ts
- artifacts/crm/src/pages/Accounting.tsx

Keep backend and CRM arrays in sync.

6. CRM BookingDetail:
Payment Type dropdown should show exactly:
- Booking Payment
- Additional Payment
- Extra Days Payment

Keep old labels in PAYMENT_TYPE_LABELS for historical records:
- Deposit Received
- Deposit Returned
- Refund
- Adjustment

Add labels and type colors for:
- Additional Payment
- Extra Days Payment

No redesign.

After implementation:
1. Show exact changed files.
2. Show migration file content.
3. Run relevant typechecks/build checks for API and CRM if available.
4. Confirm no public website files changed.
5. Confirm no permissions changed.
6. Confirm no pricing/public booking flow changed.
7. Confirm no Phase 2B extension/extra-charge UI was implemented.
8. Confirm historical old payment labels remain.
9. Confirm old payment types are not selectable for new payment creation.
10. Confirm ADDITIONAL_PAYMENT and EXTRA_DAYS_PAYMENT count toward Total Paid and paymentStatus via getBookingPaymentSummary().
11. Commit with message:
Add additional and extra days payment types