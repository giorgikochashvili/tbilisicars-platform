Revise the plan before build. The current proposal is close, but it does NOT yet fully match the real production situation we diagnosed.

Critical corrections required:

1) Do NOT assume all production files are only in artifacts/api-server/local-uploads.
Real production has used more than one local uploads directory over time. The fix must support explicit known directories, not just cwd-derived guesses.

2) Replace the legacy-directory plan with an explicit ordered directory strategy:
- Primary directory:
  - process.env.UPLOADS_DIR if set
  - otherwise a deterministic, explicit default directory suitable for production and stable across deploys
- Explicit legacy fallback directories must include known historical locations, not only cwd-based relative guesses.
Include support for these known paths in the fallback list when they are different from primary:
- /root/local-uploads
- /var/www/tbilisicars-platform/artifacts/api-server/local-uploads

You may also include a narrow cwd-based fallback only if useful, but explicit known production paths are mandatory. Do NOT rely mainly on process.cwd() or parent-relative guesses.

3) Keep write path single-source-of-truth:
- PUT /storage/local-uploads/:filename must write only to the resolved primary uploads dir
- GET may read from primary first, then explicit legacy dirs in order
- Do not introduce ambiguity on write path

4) Keep public URL format unchanged:
- /api/storage/local-uploads/<filename>

5) Audit utility must use the correct production table name:
- vehicle_model
not vehicle_models
Check the actual schema/types before implementation. Do not guess table names.

6) Be careful not to break other local-upload consumers such as pickup/dropoff or similar uploaded photos.
This fix must stabilize shared local upload path resolution without changing upload request payloads, API contracts, or unrelated business logic.

7) Startup logging should clearly show:
- primary uploads dir
- ordered legacy dirs
Example:
[UPLOADS] primary=/... legacy=[/root/local-uploads, /var/www/tbilisicars-platform/artifacts/api-server/local-uploads, ...]

8) Frontend fallback image plan is good; keep it minimal and non-destructive.

Please return a revised concise plan that explicitly reflects the real production directories above and confirms the correct DB table/schema usage before build.