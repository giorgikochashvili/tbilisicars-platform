Approved for Build — Phase 5B only.

Implement Fixed Monthly Expenses CRM UI only, based on the approved Phase 5B plan.

Strict scope:
- CRM UI only.
- Use the existing Phase 5A backend routes.
- Do NOT change backend routes/services unless a tiny import/type fix is absolutely required.
- Do NOT create DB migrations.
- Do NOT touch booking/payment logic.
- Do NOT touch public website.
- Do NOT touch pricing/public booking flow.
- Do NOT touch Georgia Bank / online payment.
- Do NOT change permissions/role guards.
- Do NOT touch SEO/noindex/sitemap tasks.
- Do NOT deploy.
- Do NOT perform Git Sync or Droplet build.
- Do NOT run production migrations.

Allowed files:
1. artifacts/crm/src/pages/FixedExpenses.tsx — new component
2. artifacts/crm/src/pages/Accounting.tsx — import + render only

Implementation details:

1. Create:
artifacts/crm/src/pages/FixedExpenses.tsx

This component should be self-contained and use the existing CRM UI patterns.

It should use the existing Phase 5A backend routes:
- GET    /api/admin/accounting/fixed-expenses
- POST   /api/admin/accounting/fixed-expenses
- PATCH  /api/admin/accounting/fixed-expenses/:id
- DELETE /api/admin/accounting/fixed-expenses/:id
- POST   /api/admin/accounting/fixed-expenses/:id/post
- GET    /api/admin/accounting/fixed-expenses/:id/posted-months

2. Accounting.tsx integration:
- Add one import for FixedExpenses.
- Render <FixedExpenses /> as a new card/section below the existing ledger/table section.
- Do not wrap the existing Accounting page in tabs.
- Do not move existing JSX.
- Do not modify existing Accounting add/edit/delete/rates logic.

3. UI features:
FixedExpenses should allow staff to:
- list templates
- create template
- edit template
- deactivate/reactivate template
- post a template for selected month
- see active/inactive status
- see clear duplicate-post handling

Template fields:
- name
- category
- amount
- currency
- dueDay 1–28
- notes
- isActive

Use EXPENSE_CATEGORIES matching Accounting.tsx:
- Service / Maintenance
- Fuel
- Refund
- Office Expense
- Salary
- Marketing
- Airport Office Fee
- Parking Fee
- Other Expense

Currencies:
- GEL
- USD
- EUR

4. UX copy:
Section helper text:
“Define recurring expenses once. Post them manually each month to record them in the accounting ledger.”

Create/Edit modal helper:
“This is a template only — no expense is recorded until you post it for a specific month.”

Post confirmation:
“Post this fixed expense for YYYY-MM? This will create one accounting expense entry.”

Post modal warning:
“Posting creates one accounting expense entry in the ledger. This cannot be undone from this screen.”

Duplicate message:
“Already posted for YYYY-MM. Choose a different month or check the accounting ledger.”

Deactivate confirmation:
“Deactivate this template? Existing posted accounting entries will not be affected.”

5. Posted months:
- Do not fetch posted months for all templates on page load.
- Fetch posted months only when opening the Post modal for one template:
  GET /api/admin/accounting/fixed-expenses/:id/posted-months
- Cache results locally per template id.
- Use cached posted months only as a UX hint.
- Backend 422 remains authoritative if cache is stale.

6. Query invalidation:
After successful create/update/deactivate/reactivate/delete/post:
- Refresh fixed expense templates list.
After successful post:
- Also refresh existing Accounting ledger and summary.

Important:
Read Accounting.tsx and use the actual existing query keys used by the current ledger and summary queries.
Do not invent wrong query keys if Accounting.tsx uses different ones.

7. Error handling:
- Duplicate post 422 should show a clear “Already posted…” message.
- Inactive template 422 should show a clear inactive message.
- Delete blocked 422 should show:
  “This template has existing posted entries and cannot be deleted. Deactivate it instead.”
- Generic errors can use the existing destructive toast style.

8. Safety:
- Template create/edit/delete must not create accounting_entries.
- Only Post for month creates accounting entry through backend.
- Deactivate/reactivate must not delete posted accounting entries.
- Existing Accounting manual entry CRUD must remain unchanged.
- Existing rates editor must remain unchanged.

After implementation:
1. Show exact changed files.
2. Confirm only FixedExpenses.tsx and Accounting.tsx changed.
3. Confirm no backend files changed.
4. Confirm no DB migration created.
5. Confirm no website files changed.
6. Confirm no booking/payment files changed.
7. Confirm existing Accounting manual entry/rates code was not modified.
8. Run CRM typecheck/build check if available.
9. Confirm CRM remains at existing baseline with no new errors in changed files.
10. Commit with message:
Add fixed expenses UI