STOP. Do NOT build yet.

This is a critical time-handling task. The current plan mixes investigation and fixes. We must separate them.

Proceed with INVESTIGATION ONLY and return a VERIFIED diagnosis + final safe fix plan.

DO NOT implement any fixes yet.

IMPORTANT BUSINESS RULE:
When a customer creates a reservation from ANY country / timezone in the world, the booking must enter the system with EXACTLY the same time the customer selected.
No hidden shift, no +4/-4 change, no browser/server timezone corruption.
The CRM must also preserve and display that same intended booking time consistently in every relevant view.

---

REQUIRED INVESTIGATION (must be completed first):

1) Server timezone
Run on API server:
node -e "console.log(process.env.TZ, new Date('2024-01-01T09:00').toISOString())"

Report:
- Is server running in UTC or UTC+4 (Asia/Tbilisi)?

---

2) Database actual stored values
Run read-only query:
SELECT id, pickup_datetime, dropoff_datetime FROM booking
WHERE deleted_at IS NULL
ORDER BY id DESC
LIMIT 10;

Compare for the same bookings:
- DB stored value
- Website/customer-selected intended time (if traceable)
- Operations display
- Booking Detail display
- Edit form prefill value

Confirm:
- Are values stored correctly?
- Are they stored as UTC-normalized values?
- Or are they already shifted incorrectly before display?

---

3) Formatter / parsing / serialization usage mapping (CRITICAL)

Explicitly list:

A) Website booking input → how selected datetime is captured
B) Website submit payload → exact serialization format
C) API route → how request datetime is parsed
D) DB insert/storage → what exact value is persisted
E) CRM Operations view → which formatter function?
F) CRM Booking Detail static view → which formatter function?
G) Edit form prefill → how time is extracted
H) Save/update path → how datetime string is reconstructed and sent

---

4) Determine scope of the bug

Answer clearly:
- Is this ONLY a display inconsistency?
- Or does it also affect edit/save?
- Are NEW bookings currently stored correctly?
- Are old bookings still correct in DB?
- Is website booking submission itself safe for users from any timezone worldwide?
- Or is the current implementation only accidentally correct in some environments?

---

5) Global timezone correctness requirement

You must explicitly verify this requirement:

If a customer in any country selects, for example, 22:00 during booking,
the system must preserve 22:00 as the intended reservation time across:
- Website summary
- API
- DB
- CRM Operations
- CRM Booking Detail
- Edit form
- Email / PDF

If any layer breaks this rule, identify it precisely.

---

6) Risk assessment

Explain:
- What happens if we apply only display fix?
- What happens if we change save path?
- What happens if we change website submission?
- Could any proposed fix silently corrupt existing bookings?

---

FINAL OUTPUT REQUIRED:

Return:

1. CONFIRMED root cause(s) — not assumptions
2. Verified behavior of:
   - website input
   - API payload
   - DB storage
   - Operations display
   - Booking Detail display
   - Edit/save roundtrip
   - new bookings from arbitrary timezones
3. SAFE fix plan split into:

A) Guaranteed-safe CRM display fix (no data risk)
B) Edit/save fix (only if proven necessary)
C) Website submission/storage fix (ONLY if proven necessary)

DO NOT include code changes yet.
DO NOT apply fixes yet.

We will approve the final fix plan before build.