We are working inside an EXISTING Tbilisicars CRM&WEB monorepo built in Replit. Do NOT redesign anything. Do NOT touch unrelated modules. Work ONLY on the voucher AI extraction failure in the CRM Bookings → Import Voucher flow.

STRICT CONTEXT
- Existing flow already works structurally:
  Upload → Review/Edit → Confirm
- Manual fallback works
- Upload works
- Admin auth works
- Route exists and is reachable
- Endpoint: POST /api/admin/voucher-import/extract
- Route uses upload.single("file")
- Current UI symptom after screenshot upload:
  "Failed to extract data from file. Please fill in details manually."
  unresolved fields include:
  contactFullName, pickupLocation, dropoffLocation, pickupDatetime, dropoffDatetime
- Backend reaches fallback with extracted: {} and extractionFailed: true
- This means the flow is wired, but AI extraction is failing somewhere inside the extraction service path

FILES TO FOCUS ON FIRST
- artifacts/api-server/src/routes/admin-voucher-import.ts
- artifacts/api-server/src/services/admin-voucher-import.service.ts
- the OpenAI integration wrapper used by this service:
  @workspace/integrations-openai-ai-server
- any env/config path used by that wrapper

ABSOLUTE RULES
- Do NOT assume the cause
- Do NOT make speculative fixes first
- Do NOT touch UI unless absolutely required for compatibility
- Do NOT modify booking creation logic
- Do NOT modify database schema
- Do NOT modify location normalization logic unless directly required
- Do NOT refactor architecture
- Keep the endpoint contract and fallback behavior intact
- Make the smallest safe fix only after root cause is proven

PHASE 1 — READ-ONLY ROOT CAUSE AUDIT FIRST
Before changing code, inspect the full execution path for image extraction and determine the exact failure point:
1. confirm file upload reaches the route
2. confirm base64 conversion is correct
3. confirm the OpenAI wrapper is being called correctly for image input
4. confirm model name compatibility in this exact Replit integration
5. confirm whether any unsupported options are being passed
6. confirm whether the request throws before response
7. confirm whether response returns but parseExtractionJson fails
8. confirm whether the model is returning non-JSON / empty JSON / refusal / malformed content

PHASE 2 — ADD TEMPORARY PRECISE DEBUGGING
Add temporary targeted logs ONLY in the voucher extraction path to reveal the exact failure:
- log before OpenAI call:
  - mimeType
  - base64 length
  - model
- log after OpenAI call if success:
  - whether choices[0].message.content exists
  - raw content length
  - first 300 chars of raw response
- log in catch:
  - err?.status
  - err?.message
  - err?.name
  - err?.response?.data if available
- log inside parseExtractionJson:
  - whether regex matched
  - whether JSON.parse failed
  - whether parsed object was empty

IMPORTANT:
- Keep logs narrow and only around voucher extraction
- Do NOT add noisy global logging
- After finding the root cause, keep only minimal useful logs or remove temporary noisy logs

PHASE 3 — IMPLEMENT ONLY THE CONFIRMED MINIMAL FIX
Possible categories include, but are not limited to:
- unsupported response_format option in this wrapper/path
- incorrect image content structure for this SDK/proxy
- model incompatibility for vision requests in this environment
- wrapper incompatibility with chat.completions + image_url
- parser mismatch with actual returned content
- missing safe fallback for non-string content blocks

But do NOT choose one until confirmed by logs/code.

PHASE 4 — SELF-VERIFY IN CODE
After fixing, verify all of these:
- screenshot upload no longer always returns extracted: {}
- extractionFailed becomes false when AI reads visible voucher data
- partially unreadable fields may remain null, which is acceptable
- manual fallback still works if extraction genuinely fails
- no unrelated regressions in auth, upload, route, or booking flow

DELIVERABLE FORMAT
At the end, return a short, exact report with:
1. confirmed root cause
2. exact file(s) changed
3. exact minimal fix applied
4. why this fix is safe
5. what temporary logs were added/removed
6. exact Replit Preview test steps
7. expected response shape when working

CRITICAL INSTRUCTION
Do not stop at “likely cause”.
Do not say “probably”.
Prove the actual failure point from code + runtime logging, then fix only that.