This is a LIVE production-connected Tbilisicars CRM & WEB project.

A new Discount system was just added and deployed, but on production the booking flow no longer loads.
The public website shows:
"Unable to load booking form. Please try again later."

IMPORTANT CONTEXT:
This likely happened after the discount feature deployment because the production database may not have received the new schema changes yet.

Production DB connection:
DATABASE_URL=postgres://tbilisicars:tbilisicars123@localhost:5432/tbilisicars

Your task:
Audit the current repository and determine the EXACT production-safe fix for this issue.

GOALS:
1. Verify whether the new Discount feature introduced:
   - new tables
   - new join tables
   - new booking columns
2. Verify how this project is supposed to run DB migrations on production:
   - existing migration system
   - exact command(s)
   - whether raw SQL migration files were added
3. Verify which public endpoint likely fails when booking form loads:
   - /api/public/booking-config
   - /api/public/quote
   - related booking bootstrap endpoints
4. Check whether the code now assumes discount tables/columns exist and fails if migration was not run.
5. Provide the EXACT droplet commands required to bring production live safely.

IMPORTANT RULES:
- Do NOT change architecture
- Do NOT refactor unrelated code
- Do NOT redesign the discount feature
- Focus only on restoring production safely
- If the issue is missing migration, say so clearly
- If the issue is a wrong migration command, provide the exact correct command
- If the issue is a code bug, identify the exact file and exact fix needed

OUTPUT FORMAT:
Return:
1. Root cause
2. Exact file(s) involved
3. Exact production migration/deploy commands to run on the droplet
4. Whether code changes are needed or not
5. If code changes are needed, give the minimal surgical fix only

Be very precise and assume this is a live system. Do not guess. Inspect the repo and derive the exact answer from the current project files.