Tbilisicars Website — Hero Card Label + Stats Text + Red Numbers + REAL Subtle Input Transparency Fix

Continue from the current working state.

IMPORTANT:
- UI/text styling changes only
- Do NOT change booking logic
- Do NOT change layout structure
- Do NOT touch backend
- Do NOT move sections
- Do NOT modify form behavior
- Only apply the exact content and styling changes below

==================================================
TASK 1 — CHANGE HERO SEARCH CARD TITLE
==================================================

Replace the current card title:

"Find Your Car"

With exactly:

"Choose Your Trip Details"

Do not change its placement or icon alignment.
Only update the visible title text.

==================================================
TASK 2 — UPDATE FIRST STATS CARD TEXT
==================================================

In the stats section below the hero card, replace the first card text.

Current meaning:
- "10,000+"
- "Served Customers"

Change it so the first stats card reads exactly:

Top line:
"12K+"

Bottom line:
"Trusted By Customers"

If a more natural balanced version fits the existing card layout better, use:
Top line:
"12K+"
Bottom line:
"Trusted Customers"

But prefer this if it fits cleanly:
Top line:
"12K+"
Bottom line:
"Trusted By Customers"

Do NOT touch the other card labels unless needed by styling consistency.

==================================================
TASK 3 — MAKE ALL STATS NUMBERS RED
==================================================

In the stats cards section:
- Make all numeric values / main highlighted figures red
- Keep all descriptive labels/subtitles in their current lighter muted color

Examples:
- "12K+" → red
- "250+" → red
- "4.6" → red

But:
- "Trusted By Customers" stays in the existing muted/light text color
- "Vehicles" stays in the existing muted/light text color
- "Overall Rating" stays in the existing muted/light text color

Use the same brand red family already used elsewhere in the homepage.

==================================================
TASK 4 — REAL SUBTLE TRANSPARENCY FOR DATE/TIME INPUTS
==================================================

Current issue:
The date/time fields still look too solid.
The previous transparency work is not visually convincing enough.

Apply a REAL but SUBTLE glass/translucent style to these fields:
- Pickup Date & Time
- Return Date & Time

Also apply the same treatment to the location field if needed for consistency.

IMPORTANT VISUAL GOAL:
These fields must look slightly transparent, not fully opaque.
The background behind them should faintly show through.
But they must remain highly readable and usable.

IMPLEMENTATION DIRECTION:
- Use actual alpha transparency in the field background (not just a darker solid color)
- Keep border visible and premium
- Keep text/icons clear
- Optional very subtle backdrop blur is allowed
- Do NOT overdo transparency
- Do NOT make fields washed out

This is meant to be:
- subtle
- premium
- visible
- usable

==================================================
TASK 5 — KEEP EVERYTHING ELSE UNCHANGED
==================================================

Do NOT change:
- Hero heading text
- Hero paragraph text
- Button text
- Form layout
- Navbar
- Booking logic
- Page structure

==================================================
DELIVERABLE
==================================================

Return:
1. Exact file(s) changed
2. Confirmation that "Find Your Car" was changed to "Choose Your Trip Details"
3. Confirmation that the first stats card now uses "12K+" and the updated customer label
4. Confirmation that all stats numbers are now red
5. Confirmation that the stats labels remained in the lighter muted text color
6. Confirmation that the date/time fields now have visible subtle transparency
7. Confirmation that logic was unchanged

==================================================
MANUAL TEST
==================================================

Homepage:
- Hero card title reads: "Choose Your Trip Details"
- First stats card shows "12K+"
- Stats numbers are red
- Stats labels remain muted/light
- Pickup Date & Time field is subtly transparent
- Return Date & Time field is subtly transparent
- Fields remain readable and usable
- No layout or logic regressions