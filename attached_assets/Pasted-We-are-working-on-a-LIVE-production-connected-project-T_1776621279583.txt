We are working on a LIVE production-connected project: TBILISICARS CRM&WEB.

IMPORTANT RULES:
- This task is Plan A only
- Do NOT touch website submission logic
- Do NOT touch edit/save path yet
- Do NOT touch DB schema
- Do NOT mutate any existing booking data
- Do NOT refactor unrelated code
- Make only the guaranteed-safe CRM display fix
- At the end, tell me exactly which files you changed and what you verified

TASK: PLAN A — SAFE CRM BOOKING DETAIL DISPLAY FIX ONLY

CONFIRMED INVESTIGATION RESULT:
- CRM Operations / Bookings list / Dashboard already display booking times using the Asia/Tbilisi formatter
- Booking Detail static display uses UTC formatter
- This causes the same booking to appear with a 4-hour difference between Operations and Booking Detail
- We must fix ONLY this display inconsistency first

GOAL:
For the same booking, CRM Operations and CRM Booking Detail must show the same Tbilisi time.

REQUIRED CHANGE:
In BookingDetail.tsx, replace the Booking Detail static display usage of the UTC-only booking formatter functions with the existing Tbilisi-aware formatter functions already used elsewhere in CRM.

Specifically:
- Replace usages of:
  - formatBookingDate
  - formatBookingDateTime
- With:
  - formatDate
  - formatDateTime

Only where this affects the static display of booking times in Booking Detail.

IMPORTANT:
- Do NOT change splitDT yet
- Do NOT change saveOverview/save path yet
- Do NOT change DateTimePicker yet
- Do NOT change API/backend/DB
- Do NOT change any other CRM pages unless strictly required by imports

EXPECTED RESULT:
- The same booking shows the same time in:
  - CRM Operations
  - CRM Bookings list
  - CRM Booking Detail static display
- No stored data changes
- No behavior changes in edit mode
- No save logic changes

LIKELY FILES:
- artifacts/crm/src/pages/BookingDetail.tsx
- artifacts/crm/src/lib/utils.ts (read only unless import/use adjustment is needed)

DELIVERABLE FORMAT:
1. Brief summary of the exact change
2. Exact file(s) changed
3. Exact formatter replacements made
4. What you verified
5. Explicit confirmation that no data mutation or save logic was changed