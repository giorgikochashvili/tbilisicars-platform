We need to review and plan three production-stability concerns identified during our internal source-code review.

Mode: PLAN / INSPECT ONLY.
Do not modify files.
Do not build.
Do not run migrations.
Do not refactor.
Do not touch unrelated website/CRM/API logic.

Project context:
This is a live production car rental platform for Tbilisicars.
Website customer bookings are model-level bookings, not specific vehicle assignments.
Admin/CRM bookings may assign a specific physical vehicle.
Online card payments are not integrated yet, but Georgia Bank online payment integration is planned soon, so payment transaction consistency is important.
Parking management should remain accessible to all intended staff/admin users, but route-level permission consistency should be correct.

Scope: only inspect and plan C1, C2, C3.

C1 — Admin booking specific-vehicle conflict check transaction boundary

Internal review concern:
In artifacts/api-server/src/services/admin-bookings.service.ts, checkVehicleConflict appears to run outside the db.transaction in createAdminBooking, and possibly related update/assignment flows.

Goal:
Prevent admin/CRM double-booking risk for a specific physical vehicle by ensuring the vehicle conflict check and booking insert/update happen inside the same transaction boundary where practical.

Please inspect:
- Every place checkVehicleConflict is called.
- createAdminBooking flow.
- updateAdminBooking flow if relevant.
- Any vehicle assignment/change flow if relevant.
- Whether checkVehicleConflict currently uses db/pool directly.
- Whether a tx-aware version is needed.
- Whether the safest fix is moving the existing check inside db.transaction or creating a small tx-aware helper.
- Whether SELECT ... FOR UPDATE on the vehicle row is advisable now or should be deferred.

Important:
Do not change public website model-level booking availability logic in this task.
Do not change pricing, statuses, emails, PDFs, discounts, extras, one-way fee logic, or customer-facing website behavior.

C2 — Payment status update should be inside the payment transaction

Internal review concern:
In artifacts/api-server/src/services/admin-booking-payments.service.ts, payment insert and accounting entry insert are inside a transaction, but getBookingPaymentSummary and updateBookingPaymentStatus appear to run after the transaction commits.

Goal:
Make payment record/accounting entry/payment_status update atomic, so a payment cannot be recorded while booking.payment_status remains stale because of a crash/restart between operations.

Please inspect:
- addBookingPayment flow.
- deleteBookingPayment flow.
- Any update/edit payment flow if it exists.
- getBookingPaymentSummary implementation.
- updateBookingPaymentStatus implementation.
- Which parts use pool.query and which use Drizzle db/tx.
- Whether these functions need optional tx/client parameters.
- The safest minimal way to keep existing payment business rules unchanged.

Important:
Do not implement Georgia Bank payments now.
Do not change payment status rules except transaction boundary consistency.
Do not change CRM UI in this task.
Do not change customer emails, PDFs, booking creation, or pricing.

C3 — Parking write route permission consistency

Internal review concern:
In artifacts/api-server/src/routes/admin-parking.ts, parking write routes POST/PATCH/DELETE use requireAdmin but may not use requirePermission("canManageParking").

Goal:
Make parking write routes permission-consistent while preserving intended access for staff. Business preference: all intended active admins/staff should have parking management permission.

Please inspect:
- All admin parking routes.
- Which routes are read-only vs write.
- Current requireAdmin / requirePermission usage.
- Where requirePermission is imported and used elsewhere.
- admins schema permission fields, especially canManageParking.
- Any seed/default admin creation or role setup logic.
- Whether existing production admins would need canManageParking=true before deploy.

Important:
Do not remove parking access from intended staff.
If adding requirePermission could lock out users, explain the exact pre-deploy DB verification/update needed.
Do not change parking business logic, parking zones, capacity rules, dashboard widgets, or CRM parking UI in this task.

Output required:
1. Confirmed findings with exact file paths and function/route names.
2. Minimal implementation plan for C1 only.
3. Minimal implementation plan for C2 only.
4. Minimal implementation plan for C3 only.
5. Risks and edge cases.
6. Whether any DB migration is needed.
7. Exact files that would change if we approve Build mode.
8. Exact verification steps after implementation.
9. Explicit list of what will NOT be touched.

Do not modify files yet.
Return only the plan/inspection result.