Proceed with Build for #3 - Fix Add Vehicle HTTP 500.

Important constraints
- This Tbilisicars CRM&WEB monorepo is already working in many areas.
- Do NOT refactor broadly.
- Do NOT redesign fleet architecture.
- Do NOT touch unrelated working modules.
- Make the smallest safe production fix only for the Add Vehicle create flow.
- Use the investigation findings from Plan mode exactly as the base context.

Primary task
Diagnose the exact runtime exception and fix the Add Vehicle HTTP 500.

Required execution order

1. Reproduce the bug with runtime visibility
- Start the API server in a way that exposes the real server error/stack trace.
- Submit a minimal create vehicle request matching the CRM payload.
- Capture the exact thrown error and exact failing line.
- Do not guess. Fix must be based on the actual runtime error.

2. Apply the minimal targeted fix
- Fix only the failing code path in the create flow.
- Most likely areas already identified:
  - db.insert(vehicleTable).values(...).returning()
  - immediate post-insert getAdminVehicle(row.id)
  - response shaping after insert
  - field mapping mismatch at runtime
- Keep all other fleet logic untouched unless the exact same root cause affects shared behavior.

3. Improve error handling only as needed
- Keep existing architecture.
- If the current handler swallows the useful error, add minimal safe server-side logging for unexpected errors so future 500s are diagnosable.
- Do not expose sensitive stack traces to the frontend.
- If input is invalid, return a proper 4xx message instead of generic 500 where appropriate.

4. Resolve the vin / techpassportNumber contract mismatch carefully
- There is already a known mismatch:
  - generated api-client-react types still use vin
  - backend/api-zod contract uses techpassportNumber
- Update the generated client types or regenerate from the actual contract if safe.
- Do this only if it does not require unrelated backend/DB refactors.
- Final frontend/backend contract must consistently use techpassportNumber.

5. Verify end-to-end after the fix
Confirm all of these:
- Add Vehicle saves successfully from CRM modal
- success toast appears
- created vehicle appears in vehicle list
- edit vehicle still works
- vehicle list still renders
- search/filter still works
- no TypeScript errors
- no build errors

Files already identified
- artifacts/crm/src/pages/Fleet.tsx
- artifacts/api-server/src/routes/admin-fleet.ts
- artifacts/api-server/src/services/admin-fleet.service.ts
- artifacts/api-server/src/middlewares/errorHandler.ts
- lib/api-zod/src/generated/api.ts
- lib/api-client-react/src/generated/api.schemas.ts
- lib/db/src/schema/fleet.ts

Required output when done
Return:
1. exact runtime root cause
2. exact files changed
3. exact minimal fix implemented
4. confirmation of verification results