URGENT targeted fix: PDF voucher vehicle image still does not appear on live vouchers.

Context:
Task #282 was implemented and deployed, but generated PDF vouchers still do not show the booked vehicle image in the Trip Details right-side area. The PDF generates successfully, so the image block is silently falling back/skipping.

Goal:
Find and fix the exact reason why the vehicle image is not rendered in PDF voucher.

Important rules:
- Do not change booking logic.
- Do not change pricing.
- Do not change database schema.
- Do not change API contracts/routes unless absolutely required to pass existing vehicleImageUrl.
- Do not change email layout.
- Do not redesign PDF.
- Do not move existing PDF sections.
- Do not add packages.
- PDF generation must never fail if image is missing or invalid.
- Keep silent fallback for customer-facing behavior, but add safe server debug logs so we can see why image was skipped.

Files to inspect:
- artifacts/api-server/src/services/pdf-voucher.service.ts
- artifacts/api-server/src/services/email.service.ts
- artifacts/api-server/src/routes/public-bookings.ts
- vehicle model image field/query used for booking/email/PDF

Required investigation/fix:

1) Confirm data flow
Verify that vehicleImageUrl is actually present in:
- public booking model query result
- emailParams
- sendBookingConfirmationEmail params
- generateBookingVoucherPdf params

If any handoff is missing, fix only that handoff.

2) Add safe debug logs inside PDF image block
Add concise logs like:
[pdf-voucher] vehicle_image_start ref=TC-xxxxx raw=<value>
[pdf-voucher] vehicle_image_resolved ref=TC-xxxxx source=<url-or-file> mode=<fetch|file>
[pdf-voucher] vehicle_image_drawn ref=TC-xxxxx width=125 height=<h>
[pdf-voucher] vehicle_image_skipped ref=TC-xxxxx reason=<missing|unsupported_format|fetch_failed|not_found|embed_failed|timeout>

Do not log customer personal data.
Do not throw.

3) Fix image source loading robustly for production
Do not rely only on HTTP fetch.

Support these cases:
- absolute http/https URL → fetch
- /api/storage/local-uploads/... → fetch from http://127.0.0.1:${PORT}/api/storage/local-uploads/...
- /storage/local-uploads/... → fetch from http://127.0.0.1:${PORT}/api/storage/local-uploads/...
- /local-uploads/... → first try reading local file from known upload dirs, then fallback to http://127.0.0.1:${PORT}/api/storage/local-uploads/...
- local-uploads/... → same as above

Known production upload dirs to try:
- /root/local-uploads
- /var/www/tbilisicars-platform/artifacts/api-server/local-uploads

If vehicleImageUrl is /local-uploads/file.jpg, local file candidate should be:
- /root/local-uploads/file.jpg
- /var/www/tbilisicars-platform/artifacts/api-server/local-uploads/file.jpg

4) Image format support
pdf-lib supports JPEG and PNG.
Before embedding:
- Detect jpg/jpeg/png by URL extension or magic bytes.
- If unsupported format such as webp, log reason=unsupported_format and skip silently.
- Do not add conversion packages.

5) Placement
Keep the intended placement from #282:
- Trip Details right-side area
- IMG_W = 125
- IMG_H cap = 90
- no overlap
- single page

6) Build
Run API build.
Report:
- exact files changed
- whether vehicleImageUrl was missing or image loading failed
- final supported URL/path cases
- exact debug log messages added