Project: TBILISICARS CRM&WEB
Context:
This is a live monorepo already built in Replit. Do NOT refactor architecture. Do NOT rewrite unrelated dashboard code. Do NOT change API contracts unless absolutely required. Make only targeted, production-safe changes for the CRM Dashboard Operations section.

Target:
CRM Dashboard → Operations section (Pickups / Dropoffs)

Goals:
1. Make Pickups and Dropoffs more informative
2. Remove already completed operations from the dashboard lists
3. Give Pickups and Dropoffs separate independent date controls
4. Preserve existing dashboard design language and dark theme
5. Do not break current filters, counts, or row click behavior

Required changes:

A) Add payment status to Operations rows
- In both Pickups and Dropoffs tables/cards, show payment status next to the amount
- Reuse existing booking payment status semantics/colors if already available in the project
- Expected examples: Paid, Unpaid, Partially Paid, Prepaid, etc.
- Keep it visually compact and aligned with the current table layout

B) Only show actionable same-day operations
- Pickups list:
  - show bookings scheduled for the selected pickup date
  - exclude bookings whose pickup has already been completed / booking already delivered / vehicle already handed over
- Dropoffs list:
  - show bookings scheduled for the selected dropoff date
  - exclude bookings whose dropoff has already been completed / booking already returned
- Canceled and No Show bookings must not appear in active operations
- This section should represent operational workload still needing action for that date, not historical items

C) Separate Pickup and Dropoff date controls
- Replace the single shared Operations date control with two independent date controls:
  - one for Pickups
  - one for Dropoffs
- Each section must navigate dates independently without affecting the other
- Preserve existing styling and keep controls compact
- Prefer inline section-level controls near each section title/header

D) Keep counts accurate
- Pickup count must reflect only rows actually shown in Pickups for its selected date
- Dropoff count must reflect only rows actually shown in Dropoffs for its selected date

Implementation rules:
- First inspect existing dashboard Operations data flow, selectors, and components
- Reuse existing booking status enums/types/constants
- Reuse existing payment status badge component if available
- Avoid duplicate business logic if helper functions already exist
- Do not change unrelated dashboard widgets
- Do not change parking logic in this task
- Do not add drag-and-drop customize logic in this task
- Do not redesign the whole dashboard

Edge cases:
- A booking scheduled today but already delivered must not remain in Pickups
- A booking scheduled today but already returned must not remain in Dropoffs
- Canceled / No Show must be excluded
- If a section has no rows for its selected date, show the existing empty state cleanly

Done looks like:
- Pickups and Dropoffs each have their own date selector
- Payment status is visible next to amount in both sections
- Only pending/actionable operations are shown for the selected date
- Counts match visible rows
- No other dashboard sections are affected

At the end:
1. List exactly which files were changed
2. Briefly explain the filtering logic now used for Pickups and Dropoffs
3. Mention any assumptions made about status mapping