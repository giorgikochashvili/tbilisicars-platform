We are continuing the live production monorepo project: Tbilisicars CRM&WEB.

Important context:
- The project is fully working in production
- Do NOT break existing functionality
- Do NOT refactor large parts of the system
- This is a controlled standardization fix
- We want consistent, timezone-correct date display across CRM and website

Goal:
Introduce a single, unified date formatting utility and use it in key visible places.

Target format:
"10 Apr 2026, 14:00" (24-hour format, English month short name)

Timezone:
All displayed dates must use "Asia/Tbilisi"

---

Tasks:

1. Create a shared date formatting utility

Location (choose appropriate existing shared place, e.g.):
- lib/utils/date.ts OR similar shared utils folder

Function example:
formatDateTime(date: Date | string): string

Behavior:
- Parse input safely
- Convert to Asia/Tbilisi timezone
- Format as: "DD Mon YYYY, HH:mm"
  Example: "10 Apr 2026, 14:00"

Use a safe and minimal approach (e.g. date-fns + timezone OR existing project tools if already present)

---

2. Replace date display in key UI locations (no overreach)

ONLY update clearly visible user-facing places:

CRM:
- Booking list (pickup/dropoff columns)
- Booking detail (pickup/dropoff section)
- Dashboard (Today's pickups / dropoffs)

Website:
- Booking confirmation page (after booking)
- Any visible booking summary section

DO NOT:
- Touch backend date storage
- Change API responses
- Change business logic
- Change date calculations
- Change filters or comparisons

---

3. Email & PDF

ONLY if formatting is currently inconsistent:
- Apply the same format function logic (or equivalent) in:
  - email.service.ts
  - pdf-voucher.service.ts

DO NOT rewrite structure — only unify displayed format

---

Rules:
- Do not use raw toLocaleString defaults
- Do not depend on user device locale
- Keep implementation minimal
- Do not refactor unrelated code

---

Validation:

1. All visible dates show in format:
   "10 Apr 2026, 14:00"

2. Dates match Asia/Tbilisi timezone

3. No regression in booking flow

4. No API or DB changes

---

Output format:

1. Where the utility was created
2. Where it was applied
3. Confirmation that timezone is fixed to Asia/Tbilisi
4. Anything intentionally NOT changed