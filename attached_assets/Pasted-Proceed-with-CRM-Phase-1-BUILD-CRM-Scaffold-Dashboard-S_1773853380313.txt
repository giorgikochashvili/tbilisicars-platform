Proceed with CRM Phase 1 BUILD: CRM Scaffold + Dashboard Screen.

Critical constraints:
- Do NOT modify the public website
- Do NOT modify the database schema
- Do NOT change existing backend behavior unless absolutely required for frontend compatibility
- Focus only on the new crm artifact and strictly necessary frontend wiring
- No write/mutation UI in this step

Primary goal:
Create the initial CRM application shell and wire the admin login flow and Dashboard screen using the already-live backend endpoints.

Required scope:

1. Create CRM artifact
- Scaffold a new React + Vite crm artifact in the monorepo
- Configure it to use @workspace/api-client-react and @workspace/api-zod
- Set up TailwindCSS and shadcn/ui consistent with existing project patterns
- Ensure API calls work correctly with relative base path /crm and shared /api backend

2. Admin auth flow
- On app load, check active admin session via GET /api/auth/admin/me with credentials included
- If unauthenticated, redirect to /crm/login
- Build a Login page that accepts email + password
- Login should call POST /api/auth/admin/login with credentials: include
- On success, redirect to /crm
- On failure, show a clear but generic error message

3. CRM shell
- Build a persistent left sidebar
- Include navigation links for:
  - Dashboard
  - Bookings
  - Fleet
  - Customers
  - Locations
  - Extras
  - Rates
  - Promotions
- All non-dashboard routes should render placeholder pages for now
- In the sidebar footer, show admin name/email and a separate Logout button
- Logout must call POST /api/auth/admin/logout and redirect to /crm/login

4. Dashboard screen
- Use the existing generated React Query hooks for:
  - /api/admin/dashboard/summary
  - /api/admin/dashboard/today
  - /api/admin/dashboard/fleet-snapshot
- Show:
  - summary stat cards
  - today pickups table
  - today dropoffs table
  - fleet status tiles
- Use loading skeletons while fetching
- Use color-coded booking status badges
- Keep money formatting simple and centralized in a helper; do not hardcode broad currency assumptions deep across components

5. Routing
- CRM root should redirect based on auth state
- Dashboard should be the default authenticated route
- Placeholder routes should be created for all future modules

6. Validation
- Run typecheck
- Confirm login → dashboard render → logout flow works
- Confirm unauthenticated access redirects to login

Output:
- files created/updated
- crm route structure
- auth flow summary
- dashboard components created
- any TODOs left for later modules

End with:
Ready for CRM Phase 2 — Fleet and Locations Screens