Build / Implement the Delivery Service phase using the two created plans:

#291 - Delivery Badge + Email Address Display
#292 - CRM Manual Booking Delivery Service Option

Implement both exactly as planned.

Strict scope:
1. CRM Bookings list:
- Add Delivery badge when pickupType === "hotel" OR dropoffType === "hotel".
- Add pickupType/dropoffType to the list response only if needed for the badge.

2. Customer confirmation email:
- Show pickup delivery address under Pickup when pickupAddress exists.
- Show return collection address under Return when dropoffAddress exists.
- Add the same conditionally to plain-text email if it mirrors trip details.
- Do not redesign the email.

3. CRM manual New Booking form:
- For non-airport/Downtown pickup location, show Delivery Service checkbox.
- When checked, show required "Pickup delivery address / hotel" field.
- For non-airport/Downtown return location, show Delivery Service checkbox.
- When checked, show required "Return collection address / hotel" field.
- If location changes to airport, hide/clear delivery option and address.
- Save using existing fields: pickupType, pickupAddress, dropoffType, dropoffAddress.

Hard rules:
- Do not change database schema.
- Do not change pricing logic.
- Do not change availability logic.
- Do not change payment logic.
- Do not change booking status logic.
- Do not change public website booking flow.
- Do not change PDF voucher.
- Do not send confirmation emails automatically.
- Do not touch Customer Cabinet.
- Do not touch vehicle image logic.
- No new packages.
- Keep changes surgical.

Expected files:
- artifacts/crm/src/pages/Bookings.tsx
- artifacts/api-server/src/services/admin-bookings.service.ts only if list response needs pickupType/dropoffType
- artifacts/api-server/src/services/email.service.ts
- artifacts/api-server/src/routes/public-bookings.ts if pickup/dropoff address must be passed to email params

Validation:
- Run relevant CRM/API builds or typechecks.
- Report exact files changed.
- Confirm no automatic confirmation email was added.
- Confirm normal non-delivery manual bookings still work unchanged.