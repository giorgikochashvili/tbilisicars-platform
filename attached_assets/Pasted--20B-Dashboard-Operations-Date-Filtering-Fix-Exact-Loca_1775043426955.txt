#20B - Dashboard Operations Date Filtering Fix (Exact Local-Date Matching)
Project: Tbilisicars CRM&WEB

Important:
This project is already fully built and interconnected inside Replit. Make this as a minimal, precise fix only. Do not redesign the dashboard. Do not refactor unrelated modules. Do not make broad assumptions. Fix only the Dashboard Operations date logic so pickups and dropoffs show the exact correct bookings for the selected date.

==================================================
PROBLEM
==================================================

Dashboard > Operations is still wrong.

Observed issues:
1. Not all real bookings for the selected date are shown
2. Some bookings from the next day appear in the wrong day
3. The widget must show all operationally relevant bookings for that exact selected date
4. There are no canceled / no-show bookings involved in this case, so do not treat this as a canceled/no-show issue

Business rule:
If a booking’s pickup date is Apr 1, it must appear in Pickups for Apr 1.
If a booking’s dropoff date is Apr 1, it must appear in Dropoffs for Apr 1.
If a booking is on Apr 2, it must NOT appear in Apr 1 operations.

==================================================
REQUIRED BEHAVIOR
==================================================

For the selected Dashboard Operations date:

Pickups must include all bookings whose pickup local date is exactly that selected date.

Dropoffs must include all bookings whose dropoff local date is exactly that selected date.

Required statuses to include for operations:
- PENDING
- CONFIRMED
- DELIVERED / rented-out state if that is how current rentals are represented
- RETURNED if relevant to same-day operational visibility

Only exclude clearly non-operational records such as:
- CANCELED
- NO_SHOW

Most important:
No booking from Apr 2 should appear in Apr 1.
No booking from Apr 1 should be missed from Apr 1.

==================================================
ROOT CAUSE DIRECTION
==================================================

This is most likely a timezone/local-date filtering bug.

The current Dashboard Operations logic is likely using an incorrect UTC midnight window or incorrect date conversion.
That causes:
- some valid same-day bookings to be excluded
- some next-day bookings to appear on the wrong selected day

Fix the filtering so it matches the CRM/business local date used by staff in Georgia.

Georgia operational timezone:
- UTC+4
- no DST complication needed here

==================================================
IMPLEMENTATION REQUIREMENTS
==================================================

1. Inspect the backend service powering Dashboard Operations
2. Inspect how selected date is converted into pickup/dropoff filtering
3. Replace incorrect UTC-day filtering with correct local operational date filtering
4. Ensure both Pickups and Dropoffs use the same exact local-date logic
5. Keep date navigation working
6. Ensure badge count equals visible rows
7. Ensure no frontend truncation or extra filtering remains

Preferred rule:
For selected date D in Georgia local time:
- Pickups = bookings whose pickup_datetime belongs to local date D
- Dropoffs = bookings whose dropoff_datetime belongs to local date D

Do not use loose or approximate date windows.
Do not let UTC conversion shift bookings into the wrong day.

==================================================
SAFETY RULES
==================================================

- Do not redesign the widget
- Do not touch unrelated dashboard cards
- Do not change unrelated booking logic
- Do not broad-refactor services
- Keep changes minimal and precise
- Preserve existing response shape unless a tiny additive change is absolutely necessary
- Do not add hacks in the frontend to compensate for wrong backend filtering; fix the root cause properly

==================================================
DELIVERABLE FORMAT
==================================================

At the end provide:
1. Changed files
2. Exact root cause found
3. Exact local-date filtering fix implemented
4. Confirmation that Apr 1 only shows Apr 1 bookings, not Apr 2
5. Confirmation that all real bookings for the selected date now appear

==================================================
MANUAL TEST CHECKLIST
==================================================

- Selected date Apr 1: all Apr 1 pickups appear
- Selected date Apr 1: no Apr 2 pickups appear
- Selected date Apr 1: all Apr 1 dropoffs appear
- Selected date Apr 1: no Apr 2 dropoffs appear
- Count badge equals visible rows
- Previous/next date navigation still works
- Widget layout/styling still works correctly

Implement carefully and do not disturb working areas.