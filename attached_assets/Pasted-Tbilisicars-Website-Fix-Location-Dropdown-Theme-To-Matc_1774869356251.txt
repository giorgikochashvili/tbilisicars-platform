Tbilisicars Website — Fix Location Dropdown Theme To Match Calendar Dark Style (UI ONLY)

Continue from the current working state.

IMPORTANT:
- Do NOT change booking logic
- Do NOT change form structure
- Do NOT change field behavior
- Do NOT touch transparency anymore
- Ignore previous transparency attempts if they caused styling problems
- This task is ONLY to fix the location select/dropdown visual theme

==================================================
PROBLEM
==================================================

The location dropdown/select is visually broken.

When opened, it shows a light/default browser-style dropdown (white/light background),
which does NOT match the premium dark theme of the website.

This is wrong.

The dropdown should visually match the dark datepicker/calendar style.

==================================================
GOAL
==================================================

Restyle the location select dropdown so it matches the same dark visual family as the calendar/date picker.

Use the calendar/date picker as the visual reference:
- dark navy background
- light text
- subtle border
- premium dark theme
- no white dropdown panel

==================================================
TASK
==================================================

Fix the location dropdown/select in the hero booking card.

Apply dark theme styling to:
- closed select field
- opened dropdown menu/panel
- option rows/items
- hover state
- selected/highlighted state if supported

==================================================
VISUAL REQUIREMENTS
==================================================

Match the calendar style as closely as possible:

1. Dropdown panel background:
- dark navy / dark blue
- NOT white
- NOT default browser look

2. Text:
- light grey / white readable text
- category/group labels also must remain readable if present

3. Borders:
- subtle dark/light border matching current premium UI

4. Hover / active state:
- use the same red accent family already present in the site
OR a subtle darker highlight if that looks cleaner

5. General look:
- premium
- dark
- clean
- consistent with datepicker

==================================================
IMPORTANT IMPLEMENTATION NOTE
==================================================

If the current native HTML select cannot be styled well enough across the opened dropdown panel,
then use the smallest safe improvement possible with the existing component approach.

But:
- do NOT rewrite the booking flow
- do NOT introduce a large new architecture
- do NOT replace unrelated inputs
- do NOT touch calendar styling
- only fix the location dropdown appearance

If the select component already supports custom menu styling, use that.
If a shared select wrapper exists, style only the relevant booking-card usage safely.

==================================================
DO NOT CHANGE
==================================================

- Do NOT continue transparency experiments
- Do NOT change layout
- Do NOT change button
- Do NOT change field order
- Do NOT touch datepicker behavior
- Do NOT modify backend logic
- Do NOT touch other sections

==================================================
DELIVERABLE
==================================================

Return:
1. Exact file(s) changed
2. How the dropdown menu styling was fixed
3. Confirmation that the opened location dropdown is no longer white/default
4. Confirmation that the dropdown now matches the dark calendar theme
5. Confirmation that booking logic was unchanged

==================================================
MANUAL TEST
==================================================

Homepage:
- Open the location dropdown
- Confirm the dropdown panel is dark, not white
- Confirm option text is readable
- Confirm the styling feels close to the calendar/date picker
- Confirm selecting a location still works normally