Approved for Build.

Implement the revised C1 / C2 / C3 plan only.

This is a live production system. Make surgical code changes only. Do not touch unrelated logic.

Strict scope:
1. C1 — Admin booking specific-vehicle conflict protection
2. C2 — Payment status transaction consistency
3. C3 — Parking write route permission consistency

Do not modify:
- Website frontend
- CRM frontend
- Public website booking availability logic
- Pricing
- Booking statuses business rules
- Emails
- PDFs
- Discounts
- Extras
- One-way fee logic
- Georgia Bank / online payment integration
- SEO
- sitemap / robots
- DB schema
- migrations
- seedSystemRoles
- unrelated route files

C1 implementation requirements:
- In artifacts/api-server/src/services/admin-bookings.service.ts
- Add optional tx/client support to checkVehicleConflict.
- Add optional tx/client support to validateVehicleBelongsToModel if needed.
- In createAdminBooking:
  - move specific-vehicle validation/conflict logic inside the db.transaction.
  - if data.vehicleId exists, lock the vehicle row inside the transaction using the existing Drizzle .for("update") pattern or equivalent tx-bound raw SQL.
  - lock must happen before conflict check and before booking insert.
  - then run validateVehicleBelongsToModel if needed.
  - then run checkVehicleConflict using the same tx.
  - then insert booking/extras as before.
- In updateAdminBooking:
  - wrap the relevant validation + update in a transaction.
  - conflict check must not only depend on data.vehicleId.
  - if vehicleId OR pickupDatetime OR dropoffDatetime changes, load the current booking and compute effective vehicleId/pickup/dropoff values.
  - if final/effective booking has a specific vehicleId, lock that vehicle row with FOR UPDATE using the same tx.
  - then check conflict excluding the current booking id.
  - then update booking using the same tx.
- Preserve existing ConflictError / NotFoundError behavior as much as possible.
- Do not change public model-level website booking logic.

C2 implementation requirements:
- In artifacts/api-server/src/services/admin-booking-payments.service.ts
- getBookingPaymentSummary must accept an optional tx/client and use the same transaction connection when provided.
- updateBookingPaymentStatus must accept an optional tx/client and use the same transaction connection when provided.
- Do not call pool.query from inside a db.transaction.
- Convert the needed summary/status queries to Drizzle using (tx ?? db), preserving existing calculation logic.
- addBookingPayment must include payment insert/accounting entry insert/summary calculation/payment_status update inside the same db.transaction.
- deleteBookingPayment must include payment/accounting delete/summary calculation/payment_status update inside the same db.transaction.
- listBookingPayments can still call getBookingPaymentSummary without tx in read-only mode.
- Preserve all current payment status business rules:
  - UNPAID / HALF / PAID logic unchanged
  - rounding tolerance unchanged
  - exchange-rate guard unchanged
  - PREPAID / REFUNDED handling unchanged
- Do not implement Georgia Bank payments now.

C3 implementation requirements:
- In artifacts/api-server/src/routes/admin-parking.ts
- Import requirePermission if not already imported.
- Add requirePermission("canManageParking") only to parking write routes:
  - POST /admin/parking
  - PATCH /admin/parking/:id/zone
  - DELETE /admin/parking/:id
- Keep GET /admin/parking as requireAdmin only.
- Do not change parking zones, parking capacity logic, dashboard widgets, or CRM parking UI.

Important for C3:
- Do not create a migration.
- Do not run production SQL.
- Do not add data backfill code.
- Production can_manage_parking data verification/update will be handled separately before deploy.

After implementation:
1. Report exact files changed.
2. Report exact functions/routes changed.
3. Run TypeScript check for the API package.
4. If there are errors, fix only errors caused by this change.
5. Confirm no DB migration was created.
6. Confirm no website/CRM frontend files changed.
7. Confirm no public booking, pricing, emails, PDFs, SEO, Georgia Bank, or schema files changed.
8. Provide a concise verification summary.