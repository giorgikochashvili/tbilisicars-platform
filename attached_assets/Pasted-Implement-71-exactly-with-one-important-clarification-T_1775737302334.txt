Implement #71 exactly, with one important clarification:
- The required booking metadata fields for this task are:
  1. source
  2. externalReservationCode
- Do NOT introduce or wire any separate reservationCode field unless it already exists and is already used by the current booking detail contract for another established purpose.
- This task is specifically about Source + External Reservation Code.

PROJECT RULES
- Live production CRM project
- Do NOT refactor unrelated code
- Do NOT change pricing, payment, handover, or website logic
- Do NOT break existing booking flows
- Keep changes surgical and consistent with existing dark UI

IMPLEMENTATION

1) BOOKING LIST (desktop + mobile)
In Bookings.tsx:
- Show contactPhone under customer name in desktop table rows and mobile cards
- Add a WhatsApp quick action using:
  https://wa.me/{cleanPhone}
- cleanPhone must strip spaces and leading +
- WhatsApp click must not trigger row click; use stopPropagation()

2) BOOKING DETAIL
In BookingDetail.tsx:
- In the overview/customer area, show:
  - customer phone number
  - WhatsApp button
- Also show:
  - Source
  - External Reservation Code
- Empty values should render as em dash / placeholder consistent with existing UI

3) NEW / EDIT BOOKING FORM
In Bookings.tsx:
- Extend EMPTY_BOOKING with:
  - source: "Walkin"
  - externalCode: ""
- In openEditBooking, prefill from saved booking values
- In the Reservation section add:
  - Source Select with fixed options:
    Walkin, Street, Web, Discovercars, Vipcars, Carflexi
  - External Reservation Code input
- Keep design consistent and mobile safe

4) CREATE / UPDATE PAYLOAD
In buildPayload:
- use booking.source instead of hardcoded "admin"
- include:
  externalReservationCode: booking.externalCode || null

5) API / SERVICE WIRING
In admin-bookings.service.ts and api-zod:
- Ensure detail/list/update paths expose and accept:
  - contactPhone
  - source
  - externalReservationCode
- Only add fields actually required for this task
- Rebuild any generated/api-zod output only as needed by current project conventions

6) ACCEPTANCE
Done only when:
- phone + WhatsApp visible in booking list desktop/mobile
- phone + WhatsApp visible in booking detail
- Source + External Reservation Code visible in booking detail
- Source + External Reservation Code editable in new/edit booking
- existing bookings preload those fields correctly
- create/update persist those fields correctly
- mobile layout remains clean