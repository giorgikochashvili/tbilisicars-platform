TBILISICARS CRM&WEB — HOMEPAGE BATCH 2.1 (STATS ANIMATION)

IMPORTANT:
This is a LIVE production-connected project.
Do NOT change architecture.
Do NOT add any external libraries.
Do NOT touch backend, CRM, API, routing, or other sections.
Add only a lightweight animation to existing trust stats.

TARGET FILE:
- artifacts/website/src/pages/Home.tsx

GOAL:
Add subtle count-up animation to numeric stats in the trust strip.

==================================================
A) ANIMATION TARGETS
==================================================

Animate ONLY these:

1. 10+ → animate from 0 → 10
2. 15,000+ → animate from 0 → 15000
3. 4.7+ → animate from 0 → 4.7

DO NOT animate:
- "<15 min Pickup & Drop-off"
- "24/7 Customer Support"

These must stay static.

==================================================
B) IMPLEMENTATION RULES
==================================================

1. Use React useEffect + useState
2. No external libraries (no react-countup, no framer-motion)
3. Animation duration ~800ms–1200ms
4. Use requestAnimationFrame or simple interval

5. Animation runs ONCE when component mounts

==================================================
C) DISPLAY FORMAT
==================================================

After animation:

- 10 → "10+"
- 15000 → "15,000+"
- 4.7 → "4.7+"

So:
- format numbers properly (thousands separator for 15000)
- keep + suffix

==================================================
D) CODE STRUCTURE
==================================================

Option 1 (preferred simple):
- create small inline helper function inside component:
  animateValue(start, end, duration)

OR

Option 2:
- use separate state per stat

Keep code clean and minimal.

==================================================
E) PERFORMANCE
==================================================

- must not re-run on every render
- no unnecessary re-renders
- no memory leaks

==================================================
DONE WHEN
==================================================

- 3 stats animate smoothly on page load
- animation completes in ~1 second
- "<15 min" and "24/7" remain static
- no layout shift
- no performance lag
- no TypeScript errors