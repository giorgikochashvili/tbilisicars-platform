Revise and consolidate these split plans into ONE final implementation, then proceed directly to Build.

IMPORTANT
- This is a live, already-built production project.
- Do NOT rewrite architecture.
- Do NOT refactor unrelated code.
- Do NOT split work into separate independent implementations.
- Merge all 3 plans into one coherent change set and then build it in the safest order.
- Preserve all existing working behavior outside this task.
- Make only surgical fixes required by the approved business logic.

CONSOLIDATION RULES
1. Deduplicate overlapping recommendations across the 3 plans.
2. Resolve any contradictions before coding.
3. Use the real existing codebase as source of truth.
4. Do not leave partial implementation.
5. After consolidation, execute the final implementation in one build.

APPROVED BUSINESS LOGIC TO IMPLEMENT

A) VEHICLE CITY INTEGRITY
- Vehicle current city is determined by vehicle.location_id -> location.city.
- A vehicle must only appear in the city where it currently belongs.
- This must be enforced in booking assign flows.
- Calendar / timeline / dashboard city behavior that is already correct must not be broken.

B) CURRENT LOCATION MUTATION RULE
- Keep the existing operationally-correct cross-city behavior if current code already moves the vehicle to booking.dropoffLocationId on pickup and keeps it there on return.
- Do NOT change that lifecycle if it is already correctly implemented.
- The goal is not to redesign that behavior, only to preserve and rely on it correctly.

C) ASSIGN DROPDOWN CITY FILTER
- Fix the booking assign vehicle dropdown so it no longer shows vehicles from all cities.
- The dropdown must derive city from booking.pickupLocationId and pass that city to the backend vehicle list query.
- Keep this surgical. Backend city filtering already exists according to the plan findings.

D) HARD PICKUP LOCK
- Once a vehicle has an active DELIVERED booking, pickup on another booking using the same vehicle must be blocked until the prior rental is operationally closed by dropoff completion.
- This must be enforced in backend pickup validation, not only UI.
- Reuse current error handling patterns already present in the codebase. Do not invent a new architecture.

E) SOFT “RETURNING SOON” RULE
- Add the 2-hour warning window for assign dropdown:
  - if scheduled new pickup is more than 2 hours before the active rental’s scheduled dropoff -> vehicle must NOT appear
  - if scheduled new pickup is within 2 hours before that scheduled dropoff -> vehicle MAY appear in assign dropdown
  - such vehicles must be visually marked with a clear light-blue / cyan warning state
- Assignment may be allowed in this warning state
- BUT pickup must still be blocked until prior dropoff is actually completed
- Implement this with minimal API change and no unrelated refactors

F) VEHICLE CREATION DEFAULT REGION LOCATION
- Make the city -> default airport location mapping explicit, not dependent on DB row ordering.
- Tbilisi -> TBS Airport
- Kutaisi -> Kutaisi Airport
- Batumi -> Batumi airport/main airport-equivalent location already used in current DB
- Use the actual DB/location truth already found in code and preserve existing UI behavior.

IMPORTANT CONTRADICTION CLEANUP BEFORE BUILD
- Remove any typo/inconsistency in the plan text before coding.
- Example: do NOT keep the incorrect acceptance example where a Tbilisi dropoff results in locationId=4 if that ID belongs to Batumi.
- Normalize the “returning soon” rule to exactly this:
  - vehicle return at 18:00
  - new pickup at 15:30 => hidden
  - new pickup at 16:00 => visible as warning
  - new pickup at 17:00 => visible as warning

IMPLEMENTATION EXPECTATIONS
- Make backend validation first
- Then API/query/schema changes if needed
- Then frontend dropdown filtering and warning styling
- Then explicit default location mapping
- Then run typecheck/build validation
- Then summarize exactly what changed

DO NOT STOP AT PLAN
Proceed directly to Build after revising/merging the plan.

OUTPUT EXPECTATION
At the end, provide:
1. exact files changed
2. what logic was implemented in each
3. whether any existing code behavior was intentionally preserved
4. any follow-up manual tests that should be run