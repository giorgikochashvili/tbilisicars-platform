# SEO Phase Task Candidate — Audit Safe Redirects for Old Public Routes Only

We are continuing a controlled SEO/Google visibility cleanup for the live TBILISICARS production project.

Important:
This is PLAN ONLY. Do not implement yet.
We need an exact, code-evidence-based plan for safe redirects from old public website routes found in Google Search Console.

Context:
Search Console shows old 404/Soft 404 URLs from previous site structures:
- /tbilisi
- /batumi
- /kopitnari
- /terms-and-conditions
- /contact
- /ru/home
- /search
- /cancel-booking
- /payment-success
- /index.php/attributes/fuel-consumption-6-8-l-100km/
- /__Routes.infoAdicional
- /assets/fonts/__Routes.infoAdicional

We do NOT want to blindly redirect everything.

Please inspect the current website routing and nginx/API assumptions from the repository and propose the safest minimal redirect strategy.

Likely safe redirects to evaluate:
- /tbilisi → /car-rental-tbilisi
- /batumi → /car-rental-batumi
- /kopitnari → /car-rental-kutaisi
- /terms-and-conditions → /terms
- /contact → /about or homepage, only if justified
- /ru/home → homepage, only if justified

Routes that should likely NOT be redirected unless there is strong evidence:
- /search
- /cancel-booking
- /payment-success
- /index.php/...
- /__Routes.infoAdicional
- /assets/...

Plan requirements:
1. Identify the exact file(s) where these redirects should best be handled.
   - Prefer website client-side routing only if appropriate.
   - If server/Nginx redirect is more correct, say so clearly, but do not implement.
2. For each old URL, classify:
   - Redirect now
   - Leave 404/410
   - Needs more business decision
3. For each recommended redirect, explain why it is safe and what target URL should be.
4. Explicitly state out of scope:
   - no booking logic changes
   - no API/backend changes unless absolutely necessary for routing
   - no CRM changes
   - no DB/schema/migrations
   - no broad SEO rewrite
5. Do not implement. Return only the plan, affected files, and risk assessment.