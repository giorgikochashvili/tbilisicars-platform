URGENT PRODUCTION FIX.

After production deploy, the API/CRM crashed on the DigitalOcean droplet.

PM2 evidence:
- tbilisicars-api restart count increased repeatedly
- uptime reset to seconds
- error log repeated:

TypeError [ERR_INVALID_ARG_TYPE]: The "path" argument must be of type string or an instance of URL. Received undefined
at fileURLToPath(...)
at artifacts/api-server/dist/index.cjs

Root cause is very likely Phase 3 PDF voucher logo loading in pdf-voucher.service.ts using fileURLToPath(import.meta.url). This fails after the API is bundled to CommonJS dist/index.cjs in production.

Fix only this production startup/runtime bug.

Rules:
- Do not change database schema.
- Do not change booking creation logic.
- Do not change pricing, availability, payment, CRM workflow, website flow, Delivery Service, manual booking, or vehicle assignment.
- Do not change emails except the PDF voucher logo loading fallback.
- Do not add packages.
- Do not remove PDF generation.
- Do not remove fallback behavior.

Required fix:
- Remove/avoid fileURLToPath(import.meta.url) from pdf-voucher.service.ts and any code bundled into CommonJS.
- Logo loading must NEVER throw during module load or API startup.
- If logo file cannot be found or embedded, PDF generation must fall back to drawing text "Tbilisicars".
- API server must start successfully after production build even if the logo file is missing.

Important production detail:
The PM2 process has exec cwd as /root on the droplet, so do not rely only on process.cwd() pointing to the project root.
Use safe candidate paths with try/catch, including absolute/known deployment paths, for example:
- /var/www/tbilisicars-platform/artifacts/api-server/src/assets/logo.png
- /var/www/tbilisicars-platform/artifacts/api-server/dist/assets/logo.png
- project-relative fallbacks if safely available

But all path attempts must be wrapped so failure never crashes startup.

Keep vehicle image silent fallback unchanged.

After fix:
- Run API build.
- Confirm changed files.
- Confirm no TypeScript errors from changed files.