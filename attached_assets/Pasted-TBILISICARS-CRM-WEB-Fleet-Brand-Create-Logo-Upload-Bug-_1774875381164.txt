TBILISICARS CRM&WEB — Fleet Brand Create / Logo Upload Bug Fix

You are working inside an existing production-style monorepo for Tbilisicars CRM&WEB.

This is a targeted bug-fix task. Do NOT redesign Fleet Management. Do NOT touch unrelated modules. Do NOT rewrite working code outside the narrow area needed to fix this.

==================================================
BUGS OBSERVED IN PREVIEW
==================================================

In CRM → Fleet Management → Brands:

1. Brand logo upload fails with:
- "Upload failed"
- "Failed to get upload URL"

2. Brand save fails with:
- HTTP 500 Internal Server Error

This happens in the Add Brand flow.

The Fleet refactor UI appears to be present, but the create flow is broken. We need a safe, minimal, production-style fix.

==================================================
GOAL
==================================================

Fix the Add Brand and Edit Brand flow so that:

1. Brand can be created successfully with only:
- brand name
- optional logo

2. Brand can also be created successfully WITHOUT a logo

3. Logo upload actually works end-to-end OR, if this repo does not yet have a working upload backend for this flow, implement the safest minimal working solution consistent with the project

4. Brand creation must never throw a generic HTTP 500 for expected validation or duplicate cases
- expected validation errors must return clean 4xx responses with readable messages

==================================================
STRICT INSTRUCTIONS
==================================================

First inspect the actual current code and trace the real failing path before changing anything.

You must inspect:
- the CRM Add Brand modal submit flow
- the logo upload helper/component used by the brand form
- the API endpoint used to request upload URL or upload files
- the admin fleet brand create route/service
- the request validation schema for create/update brand
- the backend service logic for normalized duplicate brand checking
- any recently changed brand DTO / Zod / api-zod types

Do NOT guess.
Do NOT add parallel architecture.
Do NOT break existing Fleet UI that is already working.
Do NOT touch Bookings, Auth, Website booking flow, or unrelated CRM pages.

==================================================
WHAT TO DEBUG
==================================================

You must identify exactly why the preview currently shows:

A. "Failed to get upload URL"
Potential causes may include:
- missing upload endpoint
- wrong route path
- wrong HTTP method
- wrong response shape expected by frontend
- auth/session issue on upload URL request
- environment/config issue for upload service
- frontend calling a removed or nonexistent helper
- upload component expecting generic file service that is not wired in this app

B. "HTTP 500 Internal Server Error" on Save Brand
Potential causes may include:
- create brand payload still includes removed fields or wrong field names
- backend create schema mismatched with frontend payload
- logoUrl/logoKey/logoPath handling is broken
- duplicate-check logic throws instead of returning a safe validation error
- DB insert references removed columns
- response parser mismatches actual payload
- service still partially depends on old field shape

==================================================
REQUIRED FIXES
==================================================

1. Brand create must work with NO logo
- If user enters only brand name and clicks Save Brand, the brand should be created successfully
- No upload step should be required for plain brand creation

2. Brand create must work WITH logo
- If the user uploads a logo and saves, it should store correctly and the brand should be created successfully
- Keep the implementation consistent with the project’s existing upload pattern if one exists

3. If this codebase does NOT have a fully working reusable upload infrastructure for this flow:
- implement the smallest safe working solution for brand logo upload only
- keep it localized and clean
- do not overengineer
- do not invent a giant media system

4. Safe validation/error handling
- Duplicate brand name must return a clean user-facing validation error, not HTTP 500
- Missing/invalid payload must return a clean 4xx validation error
- Upload failures must return clear messages
- Frontend toast/error UI must display the backend message when available

5. Edit Brand must continue to work
- Existing logo preview must still load
- Removing logo must work
- Replacing logo must work
- Editing name only must work even without reuploading

==================================================
DEBUG/IMPLEMENTATION EXPECTATIONS
==================================================

Inspect and fix the actual files involved, likely including:
- artifacts/crm/src/pages/Fleet.tsx
- any Fleet brand modal/form component used there
- any file upload utility/helper used by CRM
- artifacts/api-server/src/routes/admin-fleet.ts
- artifacts/api-server/src/services/admin-fleet.service.ts
- lib/api-zod/src/generated/api.ts
- relevant shared schemas/DTOs if needed

Also inspect whether the frontend is doing one of these bad patterns:
- always attempting upload URL generation even when no file is selected
- blocking create on upload service even when logo is optional
- sending wrong property name for logo
- assuming upload success shape incorrectly

==================================================
EXPECTED FINAL BEHAVIOR
==================================================

After the fix:

1. Add Brand:
- typing only "Toyota" and pressing Save Brand must create the brand successfully
- no 500 error
- no upload error if no file selected

2. Add Brand with logo:
- selecting a PNG logo and saving must succeed
- uploaded logo must later appear in Brands / Models / Vehicles UI where applicable

3. Duplicate brand:
- entering an existing normalized brand name must show a clean validation message
- not a server crash

4. Edit Brand:
- prefilled modal still works
- logo preview still works
- remove/replace logo still works

==================================================
REGRESSION SAFETY
==================================================

Do NOT break:
- Fleet models/vehicles UI already implemented
- techpassportNumber changes
- public On Request logic
- CRM auth/session
- bookings
- any unrelated upload flow if one already exists

==================================================
DELIVERABLE FORMAT
==================================================

After fixing, provide:

1. Root cause summary
- what caused the upload failure
- what caused the 500 on brand save

2. Exact files changed

3. What was fixed in frontend

4. What was fixed in backend

5. Confirmation that all these were manually verified:
- create brand without logo
- create brand with logo
- duplicate brand validation
- edit brand with existing logo
- remove/replace logo