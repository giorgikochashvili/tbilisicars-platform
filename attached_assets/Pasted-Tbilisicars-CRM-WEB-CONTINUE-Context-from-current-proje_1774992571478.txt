Tbilisicars CRM&WEB CONTINUE

Context from current project state:
- upload/storage fix is already completed and working
- brand logo upload works
- model image upload works
- live droplet deploy already succeeded
- CRM + website image path flow is fixed
- do NOT touch CRM upload logic
- do NOT touch API/storage logic
- this task is website rendering/layout only

Current issue:
On the public website, uploaded transparent PNG vehicle images look bad inside vehicle cards on desktop / larger breakpoints.
Mobile already looks good and must be preserved.

Observed desktop problem:
- car PNGs are too zoomed / cropped
- important car parts are cut off
- image area looks unbalanced and not premium
- rendering is inconsistent across different uploaded PNG sizes

Goal:
Fix car card image presentation structurally on website cards, especially desktop/tablet larger layouts, without harming current mobile behavior.

Your job:
1. Audit the exact website components responsible for rendering vehicle/model images in:
   - booking flow vehicle selection cards
   - fleet listing cards
2. Identify whether they use:
   - a shared card/image component
   - or separate duplicated image wrapper logic
3. Inspect current image wrapper and image classes:
   - height
   - aspect ratio
   - object-fit
   - overflow
   - alignment
   - breakpoint-specific md/lg/xl behavior
4. Compare why mobile currently looks good but desktop looks cropped
5. Implement the cleanest premium fix

Important implementation rules:
- preserve current good mobile presentation
- target desktop/tablet larger breakpoints structurally
- do NOT use random CSS hacks or per-image offsets
- do NOT touch upload/storage/backend/CRM
- prefer a reusable/shared image rendering solution if possible
- transparent PNG vehicle images should be displayed with a fit strategy appropriate for isolated car cutouts
- final result must produce:
  - consistent image area across all car cards
  - centered premium presentation
  - no important cropping of the car
  - stable rendering for different PNG dimensions

Preferred UI direction:
- use a dedicated consistent image stage/wrapper
- use object-contain for vehicle PNGs instead of aggressive crop behavior
- keep a premium card look with subtle background/stage styling if needed
- use responsive breakpoint logic so mobile stays effectively as-is while desktop improves

Deliverables:
1. First, print a short audit summary:
   - exact files/components found
   - current problematic classes/logic
   - why desktop breaks but mobile looks okay
2. Then implement the fix
3. Keep edits minimal and localized
4. At the end, print:
   - files changed
   - exact behavior changed
   - confirmation that CRM/upload/storage logic was untouched