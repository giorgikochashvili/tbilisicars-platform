Task: Fix 2 live production bugs — Booking page vehicle list not loading + BookingDetail document buttons still opening broken URLs

Context:
This is a live production-connected monorepo (Tbilisicars CRM & WEB), currently operating on Batumicars.com as the temporary domain.
We are in operational stabilization phase.
Do NOT refactor architecture.
Do NOT redesign pages/modules.
Do NOT change unrelated flows.
Fix only the two confirmed bugs below with minimal, surgical changes.

Confirmed Bug 1 — Booking page fails to load vehicles
Current live behavior:
- Opening the booking page with valid query params now shows:
  "Unable to load booking form. Please try again later."
- Vehicle cards do not appear at all
- This started after the recent duration-aware price change using the optional `days` param in `/api/public/booking-config`

Strong suspicion:
- The recent `days` support in booking-config introduced a backend or request-handling regression
- Most likely in `/api/public/booking-config` route logic and/or the frontend request params

Required goal:
- Booking page must load normally again
- Vehicle list/cards must appear again
- Duration-aware displayed pricing must still work if possible
- If the exact-duration price logic is causing the crash, keep the fix minimal and restore a safe working flow first
- Do NOT break existing quote calculation logic

Instructions for Bug 1:
1. Inspect exact current root cause
   - frontend request params/query key in `artifacts/website/src/pages/Booking.tsx`
   - backend parsing/validation/query logic in `artifacts/api-server/src/routes/public-bookings.ts`
2. Identify why the page is failing completely
3. Fix it with the smallest safe change
4. Preserve backward compatibility
5. If needed, simplify the days-related logic so the endpoint becomes stable again, but do not redesign the pricing engine
6. Final result must be:
   - booking page loads
   - vehicle list appears
   - no runtime crash
   - displayed price logic is either correct and working, or safely reverted to previous stable behavior with a clearly stated note

Confirmed Bug 2 — BookingDetail document buttons still generate broken URLs
Current live behavior:
- In BookingDetail, these top buttons still open broken paths:
  - Rental Agreement
  - Booking Voucher
  - Handover Sheet
  - Return Sheet
- The previous BASE_URL normalization fix was not sufficient
- The resulting links are still sometimes relative/broken instead of opening correctly from domain root

Required goal:
- Make these buttons always open correctly
- Do NOT rely on `import.meta.env.BASE_URL` for these document links anymore
- Use explicit absolute root paths starting with `/crm/...`

Required implementation for Bug 2:
Replace URL construction for:
- Rental Agreement
- Booking Voucher
- Handover Sheet
- Return Sheet

Also apply the same fix to:
- Payment Receipt
- Deposit Receipt
- Deposit Return

Correct pattern example:
- `/crm/document/${id}/agreement`
- `/crm/document/${id}/voucher`
- `/crm/handover/${id}`
- `/crm/return/${id}`

Rules:
- Do NOT change button UI
- Do NOT change routes themselves
- Only fix URL construction
- No BASE_URL usage for these document links

Files to inspect first:
- `artifacts/website/src/pages/Booking.tsx`
- `artifacts/api-server/src/routes/public-bookings.ts`
- `artifacts/crm/src/pages/BookingDetail.tsx`

Output format:
A. Exact root cause for Bug 1
B. Exact root cause for Bug 2
C. Minimal implementation plan
D. Exact files changed
E. What was changed in each file
F. What was intentionally NOT changed
G. Very short manual test checklist