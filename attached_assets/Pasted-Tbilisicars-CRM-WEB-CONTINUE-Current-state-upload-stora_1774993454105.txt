Tbilisicars CRM&WEB CONTINUE

Current state:
- upload/storage flow is already fixed
- brand logo upload works
- model image upload works
- website image path flow works
- recent fix changed vehicle PNG rendering from crop-heavy behavior to cleaner contain behavior
- current result is improved, but desktop presentation still feels too much like wide horizontal listing rows
- mobile currently looks acceptable and should not be broken

New goal:
Improve the WEBSITE vehicle card presentation on desktop/tablet by redesigning the vehicle listing layout into a cleaner premium grid-card system, while preserving mobile behavior and keeping all business logic untouched.

Important:
This is WEBSITE ONLY.
Do NOT touch:
- CRM
- backend/API
- storage/upload
- auth
- booking logic
- pricing logic
- filtering logic
- database/schema
- deploy/server config

Problem with current desktop layout:
- vehicle cards are still too horizontally stretched
- transparent PNG vehicle images look cleaner now, but visually they sit inside oversized row/listings
- image and info areas feel too separated
- too few vehicles are visible at once on desktop
- the experience does not yet feel premium enough for the website

Desired desktop/tablet direction:
Replace the current wide listing-style vehicle presentation with clean rectangular premium cards.

Target behavior:
- desktop: show 3 cards per row by default
- very large screens: optionally 4 cards per row if layout supports it cleanly
- tablet: 2 cards per row
- mobile: preserve current mobile-friendly behavior as much as possible (1 column is fine)
- cards must remain responsive and visually consistent

Vehicle card structure should become:
1. top area:
   - category badge / status badge / on-request badge if already present
2. image stage:
   - dedicated consistent image area
   - transparent PNG car image centered
   - object-contain
   - premium dark/gradient background can remain or be slightly refined
   - no car cropping
3. info area:
   - vehicle/model name
   - key specs chips (seats / transmission / fuel / etc.)
4. action/price area:
   - price or contact/on-request state
   - keep existing interactions/selection behavior intact

Scope:
Apply this redesign consistently to:
- Fleet page vehicle cards
- Booking page vehicle selection cards

If there is also a booking confirmation preview image using the same visual system, only refine it if needed — do not over-expand scope.

Implementation rules:
- first audit the exact components/files currently used
- identify whether Fleet and Booking cards share reusable structure or have duplicated markup
- prefer a reusable/shared presentation pattern only if it can be done safely with minimal risk
- otherwise update the two page implementations carefully and locally
- keep edits minimal outside the vehicle card UI
- do NOT rewrite unrelated page structure
- do NOT change data fetching or selection logic
- do NOT break mobile
- do NOT introduce random CSS hacks or per-model exceptions

Design intent:
The final result should feel like a premium car catalog/showroom on desktop:
- clean rectangular cards
- better visual balance
- more vehicles visible per screen
- car images displayed beautifully and consistently
- easier browsing and comparison
- still aligned with the current dark luxury website style

Deliverables:
1. First print a short audit summary:
   - exact files/components involved
   - whether current markup is shared or duplicated
   - what layout structure currently causes the “wide listing” feeling
   - what responsive grid/card approach you will implement
2. Then implement the redesign
3. Keep the work localized to website vehicle card presentation
4. At the end print:
   - files changed
   - exact layout behavior changed across breakpoints
   - confirmation that CRM/backend/upload logic was untouched