# Fix #8 — Build the promo maxUses enforcement correctly and transaction-safely

Project context:
This is a live production monorepo project (Tbilisicars CRM&WEB).
We are following a strict hardening/fix sequence.
Do NOT rewrite architecture.
Do NOT refactor unrelated code.
Do NOT change any other file.
Make only the required safe fix in the target file.

Target file:
- artifacts/api-server/src/routes/public-bookings.ts

Goal:
Correctly enforce promo maxUses so that a discounted booking can NEVER succeed without consuming a promo usage slot.

Critical requirement:
Do NOT use the rejected approach:
- no “pre-check then post-booking transaction re-check”
- no path where booking succeeds with promo discount but promo.timesUsed is not incremented

Required implementation:
1. Keep the existing promo code lookup logic only as a lightweight early read if needed for discount preparation, but the AUTHORITATIVE promo availability check must happen inside the SAME db.transaction() as booking creation.
2. If promo code is applied:
   - inside the transaction, re-read the promo row from DB
   - verify promo exists and is active
   - if maxUses is not null, require timesUsed < maxUses
   - if promo is at limit, abort the transaction and return the same style validation error response
3. If promo is valid:
   - create the booking inside the SAME transaction
   - insert extras inside the SAME transaction if applicable
   - increment promo.timesUsed by +1 inside the SAME transaction
4. There must be NO path where:
   - booking is created with promo discount
   - but promo.timesUsed is not incremented
5. maxUses === null must still behave as unlimited usage

Important implementation guidance:
- You already inspected that the website-booking path can inline the booking insert instead of relying on a service call that writes outside the transaction.
- Use the existing project transaction style with db.transaction(...)
- Keep response shapes aligned with current route behavior
- Keep all changes inside:
  - artifacts/api-server/src/routes/public-bookings.ts

Do NOT change:
- any other file
- schema
- response contracts except using existing validation error style
- frontend code
- package dependencies

Verification required:
1. Confirm the authoritative promo check now happens inside the same transaction as booking creation
2. Confirm booking create + extras insert + promo increment are in the same transaction
3. Confirm over-limit promo aborts the booking instead of silently skipping increment
4. Confirm maxUses null still works as unlimited
5. Confirm no unrelated files changed

Return:
- exact diff summary
- key transaction block
- exact error behavior for over-limit promo
- confirmation that discounted booking cannot succeed without consuming a promo usage slot