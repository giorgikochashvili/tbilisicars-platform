#301 — Fix Booking noindex Helmet on Step 1

We need a surgical correction to the SEO #298 implementation.

Problem:
The /booking page now has <meta name="robots" content="noindex,follow" /> in Booking.tsx, but it is only rendered in the Steps 2–6 return block. The Booking component has an early return for step === 1, and that Step 1 return does not include the Booking <Helmet>. As a result, the live rendered DOM on https://tbilisicars.com/booking Step 1 still shows the homepage fallback canonical and does not show noindex.

Evidence:
- Source Booking.tsx contains noindex in the later return Helmet.
- Built JS contains noindex.
- Browser console on live /booking Step 1 shows URL = /booking and H1 = Choose Your Perfect Car, but document.head only has homepage canonical and no noindex.
- Therefore Booking Step 1 is missing its Helmet block.

Task:
Fix Booking.tsx so the Booking page Helmet metadata is rendered for ALL booking states/steps, including Step 1.

Preferred implementation:
- Create a small shared JSX constant or component inside Booking.tsx, e.g. bookingHelmet or BookingHelmet, containing the existing Booking Helmet metadata:
  - title: Book a Car in Georgia | Tbilisicars
  - description
  - canonical: https://tbilisicars.com/booking
  - meta robots: noindex,follow
  - existing OG/Twitter tags
- Render this same Booking Helmet in both:
  1. the step === 1 return block
  2. the existing steps 2–6 return block
- Avoid duplicating metadata manually if possible.

Strict scope:
- Only artifacts/website/src/pages/Booking.tsx should change.
- Do not change booking logic, form state, API calls, pricing, availability, payment, customer cabinet, emails, PDF vouchers, sitemap, robots.txt, App.tsx, CRM, API, backend, DB, or dependencies.
- Do not redesign UI.
- Do not change the current metadata content except making it render on Step 1 too.

Done looks like:
- On /booking Step 1, rendered DOM contains:
  <meta name="robots" content="noindex,follow">
  <link rel="canonical" href="https://tbilisicars.com/booking">
- Steps 2–6 still contain the same Booking Helmet metadata.
- Website typecheck/build passes.
- Report exact changed files and explain how Step 1 now receives Helmet.