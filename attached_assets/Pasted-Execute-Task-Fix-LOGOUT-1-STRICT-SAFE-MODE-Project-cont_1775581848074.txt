Execute Task — Fix #LOGOUT-1 (STRICT SAFE MODE)

Project context:
This is a LIVE production monorepo (Tbilisicars CRM & WEB).
The system is already working and must NOT be broken.

CRITICAL RULE:
Do NOT refactor.
Do NOT improve.
Do NOT optimize.
Do ONLY the exact fixes below.

----------------------------------------
FILES YOU ARE ALLOWED TO MODIFY (ONLY THESE 3)
----------------------------------------

1. artifacts/crm/src/hooks/use-auth.tsx
2. artifacts/api-server/src/routes/admin-auth.ts
3. artifacts/api-server/src/app.ts

NO OTHER FILES ARE ALLOWED TO BE CHANGED.

----------------------------------------
TASK 1 — FRONTEND LOGOUT (FIX REDIRECT BUG)
----------------------------------------

In:
artifacts/crm/src/hooks/use-auth.tsx

Find logout function.

REPLACE it with:

const logout = async () => {
  try {
    const res = await fetch("/api/auth/admin/logout", {
      method: "POST",
      credentials: "include",
    });

    if (res.ok) {
      window.location.href = "/crm/login";
    } else {
      console.error("[logout] server error:", res.status, res.statusText);
    }
  } catch (err) {
    console.error("[logout] request failed:", err);
  }
};

IMPORTANT:
- REMOVE any .finally() redirect logic
- DO NOT add React Query
- DO NOT change anything else in this file

----------------------------------------
TASK 2 — BACKEND LOGOUT (DESTROY SESSION + CLEAR COOKIE)
----------------------------------------

In:
artifacts/api-server/src/routes/admin-auth.ts

FIND logout route and REPLACE ENTIRE HANDLER with:

router.post("/auth/admin/logout", (req, res) => {
  req.session.destroy((err) => {
    if (err) {
      console.error("[logout] session destroy error:", err);
      return res.status(500).json({ message: "Logout failed" });
    }

    res.clearCookie("connect.sid", {
      path: "/",
      httpOnly: true,
      sameSite: "lax",
      secure: process.env.NODE_ENV === "production",
    });

    return res.json({ message: "Logged out" });
  });
});

IMPORTANT:
- DO NOT modify session middleware
- DO NOT change cookie name unless it is already different
- DO NOT touch any other route

----------------------------------------
TASK 3 — CORS SAFE FALLBACK (DO NOT BREAK PROD)
----------------------------------------

In:
artifacts/api-server/src/app.ts

REPLACE ONLY the CORS CONFIG BLOCK with:

const allowedOrigins = process.env.CORS_ORIGINS
  ? process.env.CORS_ORIGINS.split(",").map((o) => o.trim()).filter(Boolean)
  : null;

app.use(cors({
  origin: (origin, callback) => {
    if (!origin) return callback(null, true);

    if (!allowedOrigins || allowedOrigins.length === 0) {
      return callback(null, true);
    }

    if (allowedOrigins.includes(origin)) {
      return callback(null, true);
    }

    return callback(new Error("Not allowed by CORS"));
  },
  credentials: true,
}));

IMPORTANT:
- DO NOT change any other part of app.ts
- DO NOT modify other middleware
- DO NOT remove existing imports

----------------------------------------
FINAL VALIDATION (MANDATORY)
----------------------------------------

After applying changes, show:

1. FULL updated code of:
   - use-auth.tsx (logout function only)
   - admin-auth.ts (logout route only)
   - app.ts (CORS block only)

2. Confirm:
   - Only these 3 files were changed
   - No other files touched
   - No TypeScript errors

----------------------------------------
EXPECTED RESULT
----------------------------------------

- Logout request succeeds
- Session is destroyed
- Cookie is cleared
- Redirect happens ONLY on success
- /api/auth/admin/me returns 401 after logout
- No regressions anywhere else

----------------------------------------
ABSOLUTE RULE
----------------------------------------

If anything is unclear — DO NOTHING outside these instructions.