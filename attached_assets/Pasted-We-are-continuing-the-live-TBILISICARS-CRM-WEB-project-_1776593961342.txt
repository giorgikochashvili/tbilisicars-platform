We are continuing the live TBILISICARS CRM&WEB project already fully built in Replit.

Important:
- This task is ONLY for the Step 4 checkbox / Terms / Privacy interaction behavior
- Do NOT touch booking status logic again
- Do NOT touch booking time logic again
- Do NOT touch scroll behavior again
- Do NOT touch success-page account block yet
- Do NOT touch email redesign/layout yet
- Do NOT touch PDF voucher layout/design yet
- Do NOT touch footer or Step 1 cleanup yet
- Do NOT refactor unrelated code
- Do NOT change architecture
- Keep this surgical and production-safe

Goal:
Fix the interaction behavior on the booking form step where the user accepts Terms & Conditions and Privacy Policy.

Confirmed current UX problem:
Clicking the checkbox area currently triggers the modal/open behavior, which makes it harder to simply check/uncheck the acceptance box.
This is incorrect.

Required final behavior:
1. Clicking the checkbox box itself must only toggle the checked/unchecked state.
2. Clicking the non-link surrounding label text should also only toggle the checkbox if that is how the current label is structured.
3. Clicking the red "Terms & Conditions" text must open only the Terms modal.
4. Clicking the red "Privacy Policy" text must open only the Privacy modal.
5. The modal/open behavior must NOT be attached to the whole container anymore.
6. Validation/required acceptance logic must remain unchanged.

Constraints:
- First inspect the exact current JSX structure and event handling
- Apply the smallest safe fix
- Keep the current form fields and validation logic intact
- Do NOT redesign the whole step
- Do NOT change unrelated Step 4 layout/content
- Do NOT touch booking logic, pricing, API, CRM, DB, or schema

What I need from PLAN:
1. Exact file(s) that must change
2. The exact current event structure causing the problem
3. The smallest safe JSX/event fix
4. Whether the checkbox and label should share one toggle target or be split
5. Any risks or ambiguity found
6. Wait for approval before BUILD

This must be implemented as a minimal, precise fix. Precision matters more than speed.