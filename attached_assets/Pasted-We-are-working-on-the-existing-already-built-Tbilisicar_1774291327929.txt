We are working on the existing, already-built Tbilisicars CRM project. Do NOT restructure the project, do NOT rewrite modules, and do NOT change business logic outside the exact issues below. Apply minimal, production-safe fixes only.

IMPORTANT CONTEXT:
- This project is already live and functioning on a server.
- We only want targeted UX/UI fixes and bug fixes.
- Keep the existing architecture, routes, APIs, and components.
- Do not rename files/folders unless absolutely necessary.
- After fixes, the project must still build with `pnpm build`.

GOALS:
1. Improve booking date/time input UX in CRM
2. Fix currency mismatch in booking modal/details
3. Fix payment status remaining "UNPAID" after payment registration

==================================================
TASK 1 — IMPROVE BOOKING DATE/TIME PICKERS
==================================================

Problem:
The current booking form date/time inputs are clunky and confusing:
- date picker UX is poor
- time picker is too granular and heavy
- users should be able to click the field itself to open the calendar
- time selection should be simpler
- 15-minute intervals are enough
- date and time should feel clean, fast, modern, and easy to use

Required behavior:
- In booking create/edit flow, pickup and dropoff date fields must open the calendar when the input field is clicked
- Time should be selected from a cleaner dropdown or time picker with 15-minute intervals only
- The UX should clearly pair date + time side by side
- Keep dark theme styling consistent with the CRM
- Make it easy for an operator to enter bookings quickly

Implementation guidance:
- Find the booking form component(s) used for:
  - pickup date/time
  - dropoff/return date/time
- Replace the current awkward input/picker behavior with a better UX using the existing component stack if possible
- Prefer a clean date picker + compact time select
- Time options should be:
  - 00, 15, 30, 45 minute intervals
- Input click should open the picker
- Do not over-engineer this
- Do not introduce a completely different booking flow
- Preserve existing form state and API payload structure

Acceptance criteria:
- Clicking the date input opens the calendar
- User can easily pick date and time
- Time list only uses 15-minute intervals
- Works for both pickup and dropoff
- Styling matches CRM and feels polished

==================================================
TASK 2 — FIX BOOKING CURRENCY DISPLAY CONSISTENCY
==================================================

Problem:
A booking amount may be, for example, 189 EUR in the list/detail, but when opening the booking/payment-related modal or form, the currency shows GEL (₾) instead of the actual booking currency.

Required fix:
- Wherever booking amount/currency is shown in booking detail/modals/forms, it must use the real booking currency
- Do not default visually to GEL if the booking currency is EUR or USD
- Ensure the currency select/value is initialized from the booking record correctly
- Ensure displayed symbol and stored currency code are consistent everywhere

Check all relevant places:
- booking row/list
- booking details drawer/modal/page
- pricing/payment section
- payment registration modal/form
- any edit booking modal where amount/currency appears

Acceptance criteria:
- If booking is EUR, the modal/form/details also show EUR
- If booking is GEL, it shows GEL
- If booking is USD, it shows USD
- No mismatch between displayed amount currency and saved booking currency

==================================================
TASK 3 — FIX PAYMENT STATUS NOT UPDATING AFTER PAYMENT
==================================================

Problem:
After registering a payment, the booking/payment badge still remains "UNPAID" even though payment exists.

Required fix:
- When a payment is added, booking payment status/summary must refresh and display correctly
- If total paid > 0 but less than total amount, show the correct partial state if the system supports it
- If fully paid, show PAID
- If unpaid, only then show UNPAID
- Use the existing business rules already in the project where possible
- Do not invent a new payment model unless absolutely necessary

Investigate and fix both layers if needed:
1. Backend:
   - booking payment summary calculation
   - payment status derivation logic
   - API response after add payment
2. Frontend:
   - stale state after successful payment submission
   - missing refetch / invalidation / optimistic update
   - badge reading from wrong field

What to verify:
- Add payment to an unpaid booking
- UI updates immediately or after refetch
- payment summary cards update
- badge/status updates correctly
- booking details and list stay consistent

Acceptance criteria:
- After payment registration, UNPAID must not remain incorrectly
- Payment summary and payment badge must reflect actual state
- Booking list and booking detail must stay consistent

==================================================
CONSTRAINTS
==================================================

- Do NOT refactor unrelated modules
- Do NOT change database schema unless absolutely required
- Do NOT break existing booking creation flow
- Keep changes minimal and production-safe
- Preserve API compatibility
- Preserve current dark CRM design language, but improve the UX polish for date/time controls

==================================================
DELIVERABLES
==================================================

After implementing:
1. List exactly which files were changed
2. Explain what was fixed in each file
3. Confirm that:
   - `pnpm build` passes
   - booking date/time UX is improved
   - currency mismatch is fixed
   - payment status refresh/logic is fixed

Before coding:
- First inspect the existing booking form, payment components, and related backend payment summary/status logic
- Then implement only the minimal fixes required for the above tasks