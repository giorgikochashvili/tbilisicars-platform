You are working on the LIVE production Tbilisicars CRM&WEB monorepo.

IMPORTANT:
This is PLAN MODE ONLY.
Do NOT edit files.
Do NOT implement anything yet.

Context:
Phase 1 is already complete.
A new isolated component already exists:

artifacts/website/src/components/SearchButton.tsx

The component:
- visually matches the current Search Vehicles button
- accepts an onSearch prop
- contains a lightweight Framer Motion arrow animation
- supports reduced-motion users
- has a hard-timeout fallback
- does NOT currently affect production behavior because it is not mounted anywhere yet

Goal of this planning step:
Analyze the safest way to integrate this component into the homepage booking flow with minimal risk.

Critical restrictions:
- Do NOT touch CRM.
- Do NOT touch backend/API.
- Do NOT touch booking logic.
- Do NOT touch pricing logic.
- Do NOT touch payment logic.
- Do NOT touch email/PDF logic.
- Do NOT change routes.
- Do NOT change SEO metadata.
- Do NOT modify validation behavior.
- Do NOT introduce any additional libraries.
- Do NOT redesign the homepage.
- Do NOT add heavy animations or assets.
- Keep the existing UX identical except for the arrow transition effect.

Tasks:

1. Read the existing Home.tsx implementation again.
Identify:
- exact location of the current button
- exact handleSearch flow
- validation sequence
- where navigate() happens
- whether SearchButton can safely call the existing handleSearch unchanged

2. Verify the current SearchButton prototype
Inspect:
- prop contract
- animation lifecycle
- timeout fallback
- reduced-motion handling
- TypeScript safety

3. Determine the safest integration approach
Explain:
- whether Home.tsx should remain responsible for validation and navigation
- whether SearchButton should remain purely presentational
- whether the current prototype introduces any timing or race-condition risks

4. Analyze UX timing
Evaluate:
- is 700ms ideal?
- should animation be shorter?
- should button text animate or remain static?
- should the button lock during animation?
- should navigation occur exactly on animation completion or slightly before?

5. Mobile/performance review
Assess:
- GPU compositing
- layout shift risk
- potential jank on low-end mobile devices
- whether Framer Motion usage remains lightweight

6. Accessibility review
Verify:
- reduced-motion behavior
- keyboard accessibility
- focus behavior
- button disabled state
- screen-reader implications

7. Exact file changes needed
List ONLY:
- which lines/components in Home.tsx would change
- what import(s) would be added
- what button block would be replaced

Do NOT edit yet.

8. Risk analysis
Assess:
- booking flow regression risk
- validation interruption risk
- animation getting stuck
- double navigation risk
- mobile UX risk

9. Done looks like
Describe:
- exact visual behavior
- exact click-to-navigation flow
- expected user perception

10. Final recommendation
At the end:
State clearly whether this Phase 2 integration is:
- SAFE TO BUILD
or
- NEEDS REVISION

Again:
PLAN ONLY.
Do NOT implement.
Do NOT edit files.
Return only analysis and integration recommendation.