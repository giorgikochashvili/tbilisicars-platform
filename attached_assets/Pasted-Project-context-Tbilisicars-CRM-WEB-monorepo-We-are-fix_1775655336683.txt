Project context:
Tbilisicars CRM&WEB monorepo.
We are fixing a public website pricing/display issue before launch.
This is a controlled stabilization task. Do not refactor architecture.

Business rule to enforce:
- "On Request" does NOT mean unavailable
- "On Request" means the car/model is offerable but not instantly confirmed
- Therefore, price must still be visible if a WEB rate exists in CRM
- Availability state and price visibility must be separated
- Public website must show prices from CRM Rates (WEB), not hide them based on availability/on-request state
- The word "Unavailable" should not appear in this public website flow for these offerable models

Current problem:
- Some website cards show "Contact for pricing" or hide the price
- On Request models currently hide price
- This is incorrect
- We need to make all models show their CRM WEB price when a WEB rate exists
- On Request badge/message should remain, but price must also remain visible

CRITICAL RULES:
- Do NOT change architecture
- Do NOT change accounting source-of-truth
- Do NOT redesign the UI
- Do NOT remove the On Request concept
- Do NOT change CRM Rates structure
- Do NOT change booking flow structure
- Do NOT hardcode prices
- Do NOT use guesses or fallback hacks
- Do NOT touch unrelated CRM or API logic
- Make only the minimum safe changes needed

Required outcome:
1. Public website Fleet cards:
   - if a model has a CRM WEB price, show the price
   - if the model is On Request, still show the price AND keep the On Request status
   - do not hide the price because of on-request/unavailable-like logic

2. Public website Booking page model cards:
   - same exact rule as Fleet
   - if CRM WEB price exists, show it
   - if On Request, keep the On Request messaging but do not hide the price

3. Wording:
   - do not show "Unavailable" for these offerable On Request models
   - "Contact for pricing" should only remain as a true fallback when no CRM WEB price exists for that model

4. Source of truth:
   - prices must come from CRM WEB Rates
   - inspect the current public pricing source/query and verify why some models are getting null price
   - fix only the minimal logic required so website-visible models correctly receive their WEB rate

Investigation requirements:
- Inspect the public website pricing source for Fleet and Booking pages
- Inspect how min_price_per_day / display price is derived
- Inspect why some models with CRM WEB rates still return null price
- Confirm whether the issue is:
  a) frontend display logic only
  b) backend/public pricing query mismatch
  c) both
- Then implement the smallest correct fix

Expected deliverables after implementation:
1. Brief root-cause summary
2. Exact files changed
3. Confirmation that:
   - On Request price now remains visible
   - no unavailable wording is shown for these offerable models
   - prices come from CRM WEB rates
   - Contact for pricing only appears when no WEB rate exists
4. Validation/build result
5. Show the key updated pricing render blocks and the relevant public pricing source logic