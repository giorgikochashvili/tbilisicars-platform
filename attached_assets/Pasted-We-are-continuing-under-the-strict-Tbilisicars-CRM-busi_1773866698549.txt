We are continuing under the strict Tbilisicars CRM business specification.

Current confirmed state:
- Booking creation works
- Fleet foundation works
- Locations foundation works
- Booking lifecycle works
- Conflict logic works
- Vehicle status/location updates work
- Dashboard operational board works

Do NOT redesign working modules.
Do NOT touch dashboard logic unless strictly necessary.
Do NOT add speculative features.

Your task now is to align ONLY these modules with real Tbilisicars rental business logic:
1. Customers
2. Extras
3. Rates
4. Promotions

==================================================
BLOCK 5 GOAL
==================================================

Make the commercial/admin support modules fully usable and aligned with Tbilisicars rental operations.

==================================================
A. CUSTOMERS MODULE
==================================================

Customers must be a real reusable customer database.

Required fields:
- full name
- phone
- email
- country
- passport / ID
- driving license
- notes

Requirements:
- Add Customer must work
- Edit Customer must work
- Saved customer must appear in the list
- Existing customers must be reusable in bookings
- Customer detail/list view should clearly show useful identity data
- Avoid generic empty CRM behavior

If customer-booking history view already exists, verify it.
If not, add only the minimum useful linkage display if simple.

==================================================
B. EXTRAS MODULE
==================================================

Extras are rental add-ons attached to bookings.

Examples:
- Child seat
- GPS
- WiFi
- Additional driver
- Snow chains

Each extra should support:
- name
- pricing type if supported
- price
- max charge days if supported
- active/inactive

Requirements:
- Add Extra must work
- Edit Extra must work
- Saved extra must appear in the list
- Extras must remain booking-oriented, not vehicle-owned

If booking-extra linking already exists, verify it.
If not, do not overbuild; just ensure the Extras module itself is correct and usable.

==================================================
C. RATES MODULE
==================================================

Rates must reflect rental pricing logic, not abstract/generic pricing.

A rate must clearly answer:
- which model
- which date range
- which location if applicable
- what price

Requirements:
- Add Rate must work
- Edit Rate must work
- Saved rate must appear in the list
- The structure must stay understandable for rental operations
- Avoid overly abstract “rate plan” complexity if it hides the actual meaning

At minimum, the module must clearly support:
- model-based pricing
- date validity
- optional location specificity
- amount/value

If the current schema is too generic, simplify the UI presentation toward rental clarity without breaking data.

==================================================
D. PROMOTIONS MODULE
==================================================

Promotions must remain simple and useful.

Each promotion should support:
- code or campaign name
- discount type
- discount value
- validity dates
- active/inactive
- optional model targeting
- optional location targeting

Requirements:
- Add Promotion must work
- Edit Promotion must work
- Saved promotion must appear in the list
- Avoid unnecessary complexity

==================================================
E. REQUIRED VERIFICATION
==================================================

You must actually verify in the running UI:

1. Add Customer works
2. Edit Customer works
3. Add Extra works
4. Edit Extra works
5. Add Rate works
6. Edit Rate works
7. Add Promotion works
8. Edit Promotion works

Also verify:
- customers remain reusable in booking flow
- rates UI is understandable in rental terms
- promotions UI is understandable in rental terms

==================================================
F. FINAL REPORT FORMAT
==================================================

At the end, report only in this structure:

1. Customer module result
2. Extras module result
3. Rates module result
4. Promotions module result
5. Verified UI flows
   - Add Customer: works / not
   - Edit Customer: works / not
   - Add Extra: works / not
   - Edit Extra: works / not
   - Add Rate: works / not
   - Edit Rate: works / not
   - Add Promotion: works / not
   - Edit Promotion: works / not
6. Remaining gaps, if any

Do not claim completion unless you actually tested the UI flows.