Project: TBILISICARS CRM&WEB

Context:
This is a live monorepo already built in Replit. Several booking-related improvements are already implemented (default status = Confirmed, removed Delivery Type dropdown, etc). Do NOT revert existing working behavior. Do NOT break API contracts. Apply only targeted improvements to booking form logic.

Target:
CRM → New Booking form
Pickup & Dropoff sections

Goal:
Make pickup and dropoff behavior fully location-driven, consistent, and clean — no redundant fields, no confusion.

---

REQUIRED FIXES

1) REMOVE any remaining Delivery Type / Return Type logic completely
- Ensure there is NO logic anywhere depending on:
  - deliveryType
  - returnType
- These must not appear in:
  - UI
  - validation
  - submission payload logic
- Location selection must be the ONLY source of truth

---

2) LOCATION TYPE DETECTION (centralize logic)

Create a single helper function used in booking form:

getLocationType(locationName: string)

Rules:
- if name contains "airport" → return "AIRPORT"
- if name contains "hotel" → return "HOTEL"
- else → return "OFFICE"

Make it:
- case-insensitive
- safe (null/undefined protected)

This helper MUST be reused for both:
- pickup
- dropoff

---

3) PICKUP BEHAVIOR

Based on selected pickup location:

A) AIRPORT
- Show only:
  - date/time
- Hide:
  - address fields
  - hotel fields

---

B) HOTEL
- Show:
  - hotel name / hotel number input
  - address input (optional or required depending existing logic)
- These fields should:
  - appear ONLY for hotel
  - be clean and minimal (no duplication)

---

C) OFFICE (Downtown / fixed location)
- Do NOT show editable address input
- Instead:
  - show fixed address (read-only or label)
- This address should come from location data if available

---

4) DROPOFF BEHAVIOR (mirror pickup)

Apply EXACT SAME logic for dropoff:

- AIRPORT → simple
- HOTEL → show hotel fields
- OFFICE → show fixed address only

NO asymmetry allowed between pickup and dropoff

---

5) FORM STATE CLEANUP (IMPORTANT)

When user changes location:

- Clear irrelevant fields automatically:
  Example:
  - switching from HOTEL → AIRPORT:
    - clear hotel name
    - clear address
  - switching from OFFICE → HOTEL:
    - allow new inputs cleanly

No stale data must remain hidden in state

---

6) VALIDATION RULES

- HOTEL:
  - require at least hotel name OR address (based on existing validation style)
- AIRPORT:
  - no extra required fields
- OFFICE:
  - no manual input required

Do NOT break existing Zod schemas — extend safely if needed

---

7) UI / UX REQUIREMENTS

- Keep form clean and compact
- No duplicated labels
- No confusing fields appearing/disappearing incorrectly
- Mobile-friendly layout must remain intact
- Do NOT redesign the whole form — only fix behavior

---

8) BACKEND SAFETY

- Do NOT change API structure unless absolutely necessary
- If backend already supports optional address/hotel fields → reuse
- Ensure submission payload stays compatible

---

EDGE CASES

- Editing existing booking:
  - form must correctly reflect saved data
- Switching locations multiple times:
  - no broken state
- Unknown location name:
  - fallback to OFFICE

---

DONE LOOKS LIKE

- No Delivery Type anywhere
- Pickup & Dropoff behavior fully controlled by location
- Hotel fields appear ONLY when needed
- Office shows fixed address only
- Switching locations cleans up form state correctly
- No broken validation
- Existing booking edit works correctly

---

At the end:

1. List files changed
2. Show helper function implementation
3. Confirm no DeliveryType logic remains
4. Confirm payload compatibility unchanged