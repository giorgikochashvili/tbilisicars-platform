We are continuing an EXISTING Tbilisicars CRM&WEB monorepo project. Do NOT redesign anything. Do NOT touch unrelated modules. Work ONLY inside the AI Booking Import / voucher extraction flow.

CURRENT CONFIRMED STATE
- CRM → Bookings → Import Voucher UI flow already exists and works:
  Upload → Review/Edit → Confirm
- Manual fallback works
- Upload works
- Admin auth works
- Route exists and is reachable
- Endpoint is: POST /api/admin/voucher-import/extract
- Route uses upload.single("file")
- The issue is specifically this:
  when a voucher screenshot is uploaded, the UI goes to Review step but shows:
  “Failed to extract data from file. Please fill in details manually.”
  and unresolved fields like:
  contactFullName, pickupLocation, dropoffLocation, pickupDatetime, dropoffDatetime
- The backend response shape currently reaches fallback with extracted: {} and extractionFailed: true
- This means the overall flow is wired, but AI extraction is failing somewhere inside the extraction service path

IMPORTANT RULES
- Do NOT assume the cause
- Do NOT blindly refactor the architecture
- Do NOT change unrelated booking logic
- Do NOT change other AI modules
- Do NOT modify CRM UI except if a tiny compatibility adjustment is absolutely required
- Preserve current manual fallback behavior
- Preserve current endpoint contract and response shape
- Keep changes minimal and surgical

FILES TO INSPECT FIRST
- artifacts/api-server/src/routes/admin-voucher-import.ts
- artifacts/api-server/src/services/admin-voucher-import.service.ts
- any OpenAI integration wrapper used by this flow, especially:
  @workspace/integrations-openai-ai-server
- any config/env access relevant to OpenAI requests in this project

YOUR TASK
1) First do a READ-ONLY root-cause audit of the voucher extraction path.
2) Determine the exact runtime failure point for image extraction:
   - whether OpenAI request throws
   - whether the model call succeeds but parsing fails
   - whether response format is incompatible with the current SDK/wrapper
   - whether image content is sent incorrectly
   - whether the integration wrapper is using an unsupported API option
3) Only after confirming the exact cause, implement the MINIMAL SAFE FIX.
4) Keep the current flow:
   upload → AI attempt → review/edit → manual completion if still unresolved
5) Do not remove fallback warnings. Only make extraction actually work if possible.

DEBUGGING REQUIREMENTS
- Inspect existing try/catch in admin-voucher-import.service.ts
- Inspect how openai.chat.completions.create(...) is called for image extraction and PDF/text extraction
- Inspect how raw model output is parsed by parseExtractionJson(...)
- Inspect the exact runtime compatibility of the current SDK wrapper with the options being passed
- If needed, add TEMPORARY precise server logs around the extraction call and parsing step, use them to confirm the issue, then keep only useful minimal logs or remove noisy ones before finalizing
- If a previous change introduced response_format: { type: "json_object" }, verify whether that is actually supported in this exact integration path before keeping it. Do not assume. Confirm.

SUCCESS CRITERIA
- Uploading a voucher screenshot no longer immediately falls back to extracted: {}
- The endpoint returns extracted fields when visible on the screenshot
- If some fields are still not readable from the image, only those remain unresolved
- No unrelated regressions
- No route/path/auth breakage
- Manual fallback still works for genuinely unreadable files

IMPLEMENTATION CONSTRAINTS
- Prefer the smallest possible code diff
- Reuse existing helper functions where possible
- Do not introduce a new architecture or new dependency unless absolutely necessary
- Do not modify database schema
- Do not change existing booking creation logic
- Do not change location normalization logic unless directly required by extraction correctness

AFTER CHANGES
Return a short build summary with exactly:
- root cause found
- files changed
- minimal fix applied
- why this fix is safe
- how to test in Replit Preview