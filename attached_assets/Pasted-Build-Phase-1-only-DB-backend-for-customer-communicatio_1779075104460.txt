Build Phase 1 only: DB + backend for customer communication/contact marker.

Goal:
Add persistent backend support for marking a booking/customer as contacted.

Do NOT build Dashboard UI marker yet.
Do NOT build BookingDetail UI marker yet.

Allowed files:
- lib/db/src/schema/bookings.ts
- new migration file under lib/db/src/migrations/
- artifacts/api-server/src/services/admin-dashboard.service.ts
- artifacts/api-server/src/services/admin-bookings.service.ts
- artifacts/api-server/src/routes/admin-bookings.ts

Do NOT change:
- artifacts/crm/src/pages/Dashboard.tsx
- artifacts/crm/src/pages/BookingDetail.tsx
- lib/api-spec/openapi.yaml
- lib/api-zod/**
- lib/api-client-react/**
- generated files
- website/SEO/email/PDF
- pricing logic
- payment logic
- booking status logic
- fleet availability logic
- pickup/dropoff filtering logic

Required backend implementation:
1. Add one minimal persistent field to booking table:
   customer_contacted boolean NOT NULL DEFAULT false

2. Update Drizzle schema:
   customerContacted: boolean("customer_contacted").notNull().default(false)

3. Add a migration SQL file:
   ALTER TABLE booking ADD COLUMN customer_contacted boolean NOT NULL DEFAULT false;

4. Add customerContacted to Dashboard booking row select:
   In admin-dashboard.service.ts, include:
   customerContacted: bookingTable.customerContacted

5. Add customerContacted to Booking Detail select:
   In admin-bookings.service.ts, include customerContacted in bookingDetailSelect.

6. Add a small service function:
   toggleCustomerContacted(id: number, contacted: boolean)
   It should update only customerContacted and return:
   { customerContacted: boolean }

7. Add a dedicated micro endpoint:
   PATCH /admin/bookings/:id/contacted
   Body:
   { contacted: boolean }
   Response:
   { customerContacted: boolean }

8. Do not route this through the full booking update endpoint.
   Do not touch pricing/status/payment logic.

9. If safe and already available in this file, record an audit log event for the toggle:
   entityType: booking
   entityId: booking id
   action/summary showing customerContacted changed
   actorId from current admin session if available
   Do not create a new audit system.

10. For GET /admin/bookings/:id:
   If customerContacted would be stripped by GetAdminBookingResponse.parse, apply the planned route-local bypass only for this one route:
   res.json(booking)
   instead of:
   res.json(GetAdminBookingResponse.parse(booking))
   Do not change other routes.

Verification:
1. Confirm migration file created.
2. Confirm no OpenAPI/codegen/generated files were touched.
3. Confirm API builds/starts.
4. Confirm PATCH /admin/bookings/:id/contacted works with a test booking.
5. Confirm GET /admin/bookings/:id includes customerContacted.
6. Confirm GET /admin/dashboard/today rows include customerContacted.
7. Run API checks/typecheck if available.
8. Separate pre-existing errors from new errors.
9. Goal: 0 new errors.

Report:
- exact files changed
- exact migration file name
- whether migration was applied in preview/dev DB
- whether endpoint was tested
- whether no frontend files were touched