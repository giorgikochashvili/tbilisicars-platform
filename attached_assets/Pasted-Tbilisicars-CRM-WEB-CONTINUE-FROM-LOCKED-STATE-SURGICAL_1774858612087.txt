Tbilisicars CRM&WEB — CONTINUE FROM LOCKED STATE (SURGICAL CHANGES ONLY)

You are working on an already built and partially deployed production-grade monorepo.

CRITICAL:
- Do NOT re-architect anything
- Do NOT rewrite the booking flow
- Do NOT touch unrelated modules
- Do NOT break existing region filtering
- Do NOT change booking status logic
- Do NOT introduce a new design system
- Reuse existing components, API routes, DTOs, services, and styling patterns
- Minimal, surgical changes only
- Preserve current dark UI and current booking step flow

STACK / CONTEXT:
- Monorepo with pnpm
- Frontend: React + Vite (website + CRM)
- Backend: Express + PostgreSQL + Drizzle
- Staging is already working end-to-end
- Booking flow already exists and works
- DateTimePicker was already reworked recently
- Inventory logic and datepicker clipping were already improved
- Dashboard / Fleet / Bookings / Website already exist

CURRENT TARGET:
Implement the following website booking-flow improvements safely on top of the existing system.

==================================================
TASK GROUP A — HOMEPAGE SEARCH UX
==================================================

A1) Auto-open Drop-off picker after Pick-up is completed
Requirement:
- On the homepage booking/search card, when the user selects Pick-up date/time and clicks “Done”, automatically move focus/open the Drop-off date/time picker immediately.
- User must not need to manually click the Drop-off field.
- This should work only after valid Pick-up date/time is selected.
- Keep existing validation logic.
- Do not break current date/time component behavior.

Implementation notes:
- Reuse existing DateTimePicker component behavior if possible.
- Add minimal state/control wiring only.
- If the component supports controlled open state, use it.
- If not, add the smallest possible enhancement to support “open next field after confirm”.
- Do not rewrite the picker component from scratch.

Acceptance:
- Select pickup date/time → click Done → dropoff picker opens automatically.
- No duplicate popups.
- No scroll jump.
- No console errors.

==================================================
TASK GROUP B — STEP 1 (BOOK YOUR CAR) LAYOUT + FILTERS
==================================================

B1) Move Booking Summary to the far left side on Step 1
Requirement:
- On booking Step 1 (“Choose Your Vehicle”), move the Booking Summary sidebar to the LEFT side.
- It should sit flush to the left side of the booking content area.
- Vehicle cards/list should appear to the right of it.
- Keep desktop layout premium and balanced.
- On smaller screens, preserve responsive behavior and stack cleanly.

B2) Add filters into that left sidebar area
Inside the left sidebar, together with Booking Summary, add usable Step 1 filters:

Required filters:
- Category
- Drive type
- Transmission
- Seats
- Date/time editing directly from sidebar

Behavior:
- Filters must update the visible vehicles immediately and cleanly.
- Date changes from the sidebar must re-run availability correctly using existing booking availability logic.
- No page reset, no broken state, no loss of selected search context.
- Edit Search should remain functional.
- Sidebar date/time editing must feel like inline refinement, not a separate restart.

Important:
- Reuse existing availability logic from the current codebase.
- Reuse existing fleet metadata fields already used by the website / CRM.
- Do not invent new vehicle logic.
- Do not break “On Request” behavior if already implemented/in-progress.

Filtering logic:
- Category filter uses existing category/group/class info already shown on cards.
- Drive type filter should use existing vehicle/spec field if present in the codebase/data model.
  - If the field is not currently available in the website payload but exists in current backend/domain, expose it minimally through the existing public booking config/vehicle response.
  - Do not create a new architecture.
- Transmission filter uses existing transmission value.
- Seats filter uses existing seats count, with sensible matching (e.g. exact count or “X+” if existing UI pattern supports it).
- Date/time editing in sidebar must update trip duration and totals immediately.

B3) Step 1 header spacing
Requirement:
- Move “Book Your Car” block slightly upward.
- Reduce the large empty vertical space above the booking step content.
- Keep it clean and premium, but more compact.
- Do not change text content yet, only positioning/spacing.

Acceptance:
- Sidebar is on the left on desktop.
- Summary + filters feel like one coherent left panel.
- Vehicle results remain on the right.
- Filters and date changes work without broken transitions.
- Totals and trip info update correctly.
- Mobile/tablet stays usable.

==================================================
TASK GROUP C — VEHICLE CARD PRICE DISPLAY
==================================================

C1) Show prices in EUR on Step 1 vehicle cards
Requirement:
- On vehicle cards in Step 1, display:
  - main bold price = FULL TOTAL in EUR for the selected trip
  - smaller secondary price = DAILY PRICE in EUR
- The current display should be reversed: do NOT make daily price the main number anymore.
- Example desired visual hierarchy:
  - Main: €500 total
  - Small: €100/day

Rules:
- Reuse existing pricing calculation logic.
- Reuse existing currency conversion / booking currency logic already present in the system.
- Do not hardcode random conversions in the component if backend already provides normalized values.
- Prefer backend/source-of-truth values if available.
- If current public booking vehicle result only returns one price form, minimally extend it so the frontend can render:
  - total price in EUR
  - daily price in EUR
- Keep summary totals consistent with selected currency display.
- Do not break existing booking math.

Acceptance:
- Vehicle card main price = total EUR
- Secondary small text = per-day EUR
- Booking summary remains internally consistent
- No mismatch between selected vehicle and sidebar summary

==================================================
TASK GROUP D — STEP 2 LABEL / CONTENT
==================================================

D1) Rename Step 2 title
Change:
- “Extras” → “Additional Extras & Services”

This should update:
- Step label in progress indicator
- Main section heading
- Any related subtitle text where appropriate

D2) Pull extras from CRM-managed data
Requirement:
- The Step 2 page must render extras/services coming from the CRM-managed extras data already stored in the system.
- Reuse the current extras source/API already used by the booking flow or public booking config.
- Show proper title, pricing, and description/info if such fields already exist in the current data model/API.
- Do not create fake/static extras.
- If extras currently exist in CRM but Step 2 does not show enough metadata, minimally expose only what is needed.

Acceptance:
- Step 2 title is updated
- CRM-added extras appear in the website flow
- If no extras exist, empty state remains clean and intentional

==================================================
TASK GROUP E — PROMO CODES FROM CRM
==================================================

E1) Promo codes must be manageable from CRM
Requirement:
- Promo codes should be created and managed from the CRM so they can later be sent to customers.
- Reuse existing promo domain, validation endpoint, tables, services, DTOs, or admin modules if they already exist.
- Do NOT invent a new promo engine if promo logic already exists in the project.
- Wire the website promo input to CRM-managed promo codes.

Expected CRM capabilities (minimal admin scope):
- Create promo code
- Set discount type/value according to existing promo logic in the codebase
- Set active/inactive
- Optional validity dates if current promo logic already supports them
- Website booking flow should validate and apply them using existing promo validation flow

Important:
- Do not redesign the CRM
- Add only the minimal admin UI/pages/forms needed, ideally inside the existing promos/admin module if present
- Do not break current booking pricing logic

Acceptance:
- Admin can create a promo in CRM
- Promo code can be entered on website booking flow
- Validation works
- Discount is reflected correctly in booking totals
- Invalid/inactive promo behaves correctly

==================================================
IMPLEMENTATION RULES
==================================================

1. First inspect the existing code and REUSE what already exists.
2. Do not create duplicate components if current ones can be extended.
3. Keep API changes minimal and only where strictly required.
4. Do not change database schema unless absolutely required by missing promo support.
5. If promo schema/domain already exists, use it instead of creating new tables.
6. Do not touch CRM dashboard/fleet/bookings unless needed for promo management or shared reusable components.
7. Do not break current Step 1 → Step 2 → Step 3 flow.
8. Keep current dark design language and button styles.

==================================================
DELIVERABLE FORMAT
==================================================

After implementation, return:

1. EXACT files changed
2. Short summary per file
3. Any new route/service added
4. Any API response fields added/extended
5. Clear manual test checklist for:
   - homepage pickup → auto-open dropoff
   - Step 1 sidebar left layout
   - filters
   - sidebar date editing
   - EUR total + daily display
   - Step 2 extras rendering
   - CRM promo creation + website promo usage

==================================================
MANUAL TEST CASES TO PASS
==================================================

- Homepage:
  - Select pickup date/time and press Done
  - Dropoff opens automatically
  - Select dropoff normally
  - Search continues as before

- Step 1:
  - Sidebar is on the left
  - Booking summary visible
  - Category filter works
  - Drive type filter works
  - Transmission filter works
  - Seats filter works
  - Changing dates in sidebar updates results
  - Edit Search still works
  - No broken navigation or blank state bug

- Pricing:
  - Card shows total EUR prominently
  - Smaller daily EUR also visible
  - Summary remains correct

- Step 2:
  - Label is “Additional Extras & Services”
  - CRM extras appear correctly

- Promo:
  - Promo created in CRM
  - Promo validates on website
  - Discount applies correctly
  - Invalid code shows clean error

Now inspect the existing codebase and implement these changes surgically.