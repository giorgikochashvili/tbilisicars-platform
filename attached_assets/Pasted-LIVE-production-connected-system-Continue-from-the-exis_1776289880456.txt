LIVE production-connected system. Continue from the existing Tbilisicars CRM & WEB project built in Replit. Do NOT treat this as a new project. Do NOT redesign architecture. Do NOT refactor unrelated code.

TASK: Implement urgent public booking flow UI/UX + flow-stability fixes before live customer testing.

IMPORTANT:

* This is a LIVE production-connected system
* Make only surgical changes
* Preserve existing booking logic unless explicitly required below
* Do NOT rewrite the booking architecture
* Do NOT change DB schema
* Do NOT change API contracts unless absolutely required
* Focus on customer-facing booking usability, ordering logic, and flow stability
* Be extremely careful not to introduce step-state loss or booking flow instability

---

GOAL

Polish and correct the website booking journey so real customers can complete reservations comfortably, logically, and without losing progress.

---

BATCH SCOPE

1. HOME PAGE — TEMPORARILY HIDE STATS

Temporarily hide the homepage stats section/cards such as:

* 12K+
* 250+ Vehicles
* similar trust/stat counters

Requirements:

* hide cleanly
* do not delete permanently
* keep implementation easy to restore later

---

2. BOOKING STEP 1 — VEHICLE LIST ORDER / PRIORITY LOGIC

Problem:
Vehicle cards are appearing with unclear ordering.
“On Request” vehicles currently appear too prominently and can appear before more suitable options.

Important clarification:
“On Request” vehicles are still real fleet models. They should NOT be removed entirely.
They represent models that exist in the fleet, but may not be instantly available in that exact moment/context.
They may still be shown to customers so they are not hidden from the catalog.

Goal:
The vehicle list should follow a sensible customer-facing order.

Requirements:

* inspect the current vehicle/model query and sorting logic
* prioritize the most relevant and properly available options first
* “On Request” vehicles may remain visible, but they must NOT appear before actually available / more suitable options
* available or better-matching options must be surfaced ahead of on-request ones
* keep the ordering logical, clean, and trustworthy
* preserve existing booking functionality
* no fake availability logic
* no architecture rewrite

IMPORTANT:
Do not remove On Request support.
Do not let On Request cards dominate the top ahead of stronger available choices.

---

3. BOOKING STEP 1 — STICKY SIDEBAR PANELS

On the vehicle selection page, make these left-side panels follow during scroll on desktop:

* Locations
* Booking Summary
* Filters

Requirements:

* desktop: comfortable sticky behavior
* mobile: do NOT create an annoying or heavy sticky UI
* mobile layout must remain clean and unobtrusive
* use responsive behavior only where appropriate

---

4. INSURANCE STEP — SIMPLIFY TO ONE OPTION

Replace the current multi-option insurance presentation for now with a simpler initial version.

New requirement:
Show only one insurance option:

* Full Insurance

Behavior:

* one visible selection control/card/button
* when selected or expanded, show detailed insurance information below

Details section should include:

* what is covered
* what is not covered
* deposit amount
* excess amount

Requirements:

* simple, clean, trustworthy design
* preserve booking step flow
* keep implementation easy to expand later
* do not overengineer

IMPORTANT:
This is a temporary simplification for launch readiness.

---

5. STEP 4 — NATIONALITY DROPDOWN DESIGN FIX

Problem:
The nationality dropdown and scrollbar do not fit the dark site design.

Requirements:

* restyle the dropdown panel, search input, list, hover/selected states, and scrollbar so it visually fits the existing dark theme
* remove the visually jarring white scrollbar/input mismatch
* keep functionality the same

---

6. STEP 4 — AGE INPUT CLEANUP

Problem:
Age input styling/behavior looks off and feels unnecessary in the current format.

Requirements:

* keep age as a simple numeric input
* clean styling to match the design
* helper text below should clearly indicate:
  Minimum age: 21
  Maximum age: 70
* if spinner controls are causing visual issues, simplify them appropriately without breaking numeric input behavior

---

7. TERMS & PRIVACY — OPEN IN MODAL, NOT PAGE NAVIGATION

Problem:
On the booking form, clicking Terms & Conditions or Privacy Policy navigates away to full pages and breaks the reservation flow/state.

Requirements:

* clicking Terms & Conditions opens its content in a modal/dialog/overlay
* clicking Privacy Policy opens its content in a modal/dialog/overlay
* user must remain inside the booking flow
* no booking step state loss
* modal must be readable on both desktop and mobile
* use current existing website content/pages as source if possible
* do not duplicate large hardcoded content if the project already has a reusable source/component

IMPORTANT:
This is a high-priority UX fix.

---

8. BOOKING FLOW STABILITY / STATE PRESERVATION

High priority requirement:
Normal user actions must not break or corrupt the reservation flow.

Inspect and harden the booking step state behavior for:

* browser back
* browser forward
* page refresh
* internal navigation between steps/pages
* returning to the booking flow after opening auxiliary content

Goal:
These actions must NOT cause:

* step-state loss
* broken step progression
* mixed or corrupted booking data
* confusing resets
* stuck/half-broken booking UI

Requirements:

* inspect current booking state persistence/resume logic
* make only minimal safe fixes if needed
* preserve the existing booking flow structure
* do NOT introduce a major state-management rewrite
* prioritize stability and consistency

IMPORTANT:
This must be handled carefully and surgically.
No overengineering.

---

SAFETY RULES

* No unrelated cleanup
* No refactor of the whole booking module
* No new architecture
* No DB changes
* No breaking existing API/backend booking submission
* Keep all changes surgical and production-safe

---

OUTPUT FORMAT

1. Implementation plan grouped by item 1–8
2. Exact files to change
3. Any risk notes
4. Build directly OR needs revise
5. If building, keep the implementation minimal and production-safe
