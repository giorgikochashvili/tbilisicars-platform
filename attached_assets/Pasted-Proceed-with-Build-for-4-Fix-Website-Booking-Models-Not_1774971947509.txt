Proceed with Build for #4 - Fix Website Booking — Models Not Visible.

Use the confirmed Plan findings exactly. Do NOT re-investigate unrelated areas. Do NOT refactor broadly. Do NOT touch backend or website booking logic unless absolutely required by compilation. Make the smallest safe CRM-side fix only.

Confirmed root cause
The CRM Model form is missing availableForExternalSystems wiring.
Because of that, when a model is created or edited in CRM, availableForExternalSystems is omitted from the API payload, PostgreSQL uses the column default false, and /api/public/booking-config excludes the model because it filters by available_for_external_systems = true.

Important constraints
- Do NOT change backend logic unless required for type/build consistency
- Do NOT change public website Booking.tsx
- Do NOT redesign the CRM form
- Do NOT touch unrelated fleet flows
- Keep all existing working logic intact
- Only implement the missing CRM form wiring for availableForExternalSystems

Required changes
In artifacts/crm/src/pages/Fleet.tsx inside ModelsTab:
1. Add availableForExternalSystems: boolean to the model form state
   - default true for new models
2. In edit/open modal flow, read the current value from item.availableForExternalSystems
3. In save/create/update payload, include availableForExternalSystems
4. Add a labeled Switch toggle in the model form UI
   - place it below the existing Active toggle
   - keep styling consistent with the current form
   - label should clearly indicate website visibility, for example:
     - "Show on website"
     - optional helper text if the form already uses helper text patterns
5. Preserve existing Active toggle behavior exactly as-is

Verification required
After the fix, verify:
- creating a new model with "Show on website" ON persists availableForExternalSystems=true
- editing an existing model reads the current value correctly
- toggling it OFF hides the model from public booking results
- toggling it ON shows the model in /api/public/booking-config
- models with zero available vehicles still appear as On Request (existing behavior)
- no TypeScript errors
- no build errors

Files
Primary:
- artifacts/crm/src/pages/Fleet.tsx

Only touch other files if strictly required for typing/build consistency.

Required output when done
Return:
1. exact files changed
2. exact minimal changes made
3. confirmation of verification results