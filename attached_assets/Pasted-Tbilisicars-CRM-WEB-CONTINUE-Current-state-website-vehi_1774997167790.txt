Tbilisicars CRM&WEB CONTINUE

Current state:
- website vehicle image work is in good shape
- now focus on CRM Fleet UI polish only
- do NOT touch website
- do NOT touch backend/API/database/upload/storage/auth

New task:
Clean up the CRM Fleet "Add Vehicle" modal and related model presentation so the UI fits the design better and feels less visually noisy.

Observed issues:
1. In Fleet -> Vehicles -> Add Vehicle modal, the "Vehicle Model" dropdown panel is too tall and does not fit nicely inside the modal/layout.
2. The small emblem/model images shown in the Vehicle Model dropdown feel unnecessary and visually cluttered.
3. The same emblem/model thumbnail treatment also feels too busy in Fleet model-related UI.

Goal:
Make the Vehicle Model selection clean, scrollable, and text-focused.

Required changes:
1. In the Add Vehicle modal:
   - make the Vehicle Model dropdown panel properly scrollable
   - add a max-height so the dropdown stays inside the modal comfortably
   - keep keyboard/mouse selection behavior intact
   - preserve the current dark CRM style

2. Remove the small emblem/model image thumbnails from:
   - the Add Vehicle -> Vehicle Model dropdown options
   - the Fleet Models tab list/table rows wherever that small emblem thumbnail is shown
   - any closely related Fleet model selection UI where the same thumbnail is being rendered unnecessarily

3. Keep only clean text presentation for model names in those places.
4. Do not change CRUD logic, save behavior, IDs, API calls, or form submission behavior.

Implementation rules:
- audit the exact CRM files/components involved first
- keep edits minimal and localized
- do NOT affect website cards or public pages
- do NOT change upload/storage support; just stop rendering those thumbnails in these CRM UI locations
- keep the modal layout visually balanced
- preserve all existing functionality

Deliverables:
1. First print a short audit summary:
   - exact files/components involved
   - where the dropdown panel styling is defined
   - where the emblem/model thumbnails are rendered
2. Then implement the UI cleanup
3. At the end print:
   - files changed
   - exact UI behavior changed
   - confirmation that backend/API/upload/storage logic was untouched