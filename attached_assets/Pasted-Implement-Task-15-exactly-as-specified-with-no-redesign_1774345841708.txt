Implement Task #15 exactly as specified, with no redesigns and no unrelated refactors.

GOAL
Fix the remaining booking creation bugs without breaking Task #14 fixes.

ROOT CAUSES TO FIX

1) Request-body datetime validation bug
CreateAdminBookingBody currently uses zod.date() for pickupDatetime and dropoffDatetime. JSON requests send strings, so these always fail validation and return HTTP 400.
Fix:
- In lib/api-zod/src/generated/api.ts, replace request-body zod.date() usages for booking datetime fields with zod.coerce.date().
- Also check equivalent admin booking update/request schemas and make them consistent.
- IMPORTANT: if this file is generated from another source (OpenAPI/codegen source), update the true source as well or clearly preserve the fix so it does not get overwritten on rebuild.
- Rebuild lib/api-zod after the change.

2) “Any model” invalid payload bug
The New Booking dialog allows SelectItem value="any". On submit this becomes parseInt("any") => NaN => JSON null => backend rejects with 400.
Fix in artifacts/crm/src/pages/Bookings.tsx:
- Validation must treat "any" exactly like no model selected.
- Show clear frontend validation error: "Please select a vehicle model".
- In the payload builder, omit vehicleModelId entirely if the value is "any", empty, non-numeric, or NaN.
- Keep vehicleQueryParams behaviour consistent with this logic.
- “Any model” may remain visible in UI as a no-filter state, but it must never produce a silent backend 400.

3) New customer phone/email mapping bug
For new customers, the frontend currently sends:
- customerFullName
- customerPhone
- customerEmail
as top-level fields.
The backend Zod schema expects:
customerData: { fullName, phone, email }
Fix:
- Remap new customer submission payload to:
  customerData: {
    fullName: ...,
    phone: ...,
    email: ...
  }
- Only send customerData when creating/selecting a new customer.
- If an existing customer is selected, send customerId and do not send customerData.

DO NOT CHANGE
- Booking business logic
- Conflict logic
- Pricing logic
- Form layout/design
- Parking workflow
- HandoverModal typing fixes from Task #14

REGRESSION REQUIREMENTS
Do not break:
- TBS Air Parking “Enter Car in Parking” modal
- HandoverModal form-reset / typing stability
- existing vehicle query filtering logic already fixed in Task #14

FILES TO CHECK
- lib/api-zod/src/generated/api.ts
- artifacts/crm/src/pages/Bookings.tsx
- artifacts/api-server/src/routes/admin-bookings.ts
- artifacts/api-server/src/services/admin-bookings.service.ts

VERIFICATION REQUIRED
1. Run TypeScript verification:
- CRM: tsc --noEmit
- API server: tsc --noEmit
- lib/api-zod compile/build if required

2. Smoke test mentally and by code path:
- Existing customer + valid model => booking creates successfully, no HTTP 400
- New customer + valid model => booking creates successfully and customer is saved with full name, phone, email
- “Any model” selected => frontend blocks submit with “Please select a vehicle model”
- No regression in TBS Air Parking modal
- No regression in HandoverModal typing stability

DONE ONLY WHEN
- New Booking no longer returns HTTP 400 for valid submissions
- New customer contact fields persist correctly
- “Any model” no longer causes silent backend failure
- TypeScript passes cleanly
- Changes are minimal and targeted