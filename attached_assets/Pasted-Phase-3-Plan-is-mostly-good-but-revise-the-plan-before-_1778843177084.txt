Phase 3 Plan is mostly good, but revise the plan before implementation.

Do not implement yet.

Please revise the Phase 3 plan with these corrections:

1) PDF attachment filename
Do NOT use # in the filename.
Customer-facing display should be #754, but attachment filename should be safe:
booking-754.pdf or booking-voucher-754.pdf
Use a helper that converts TC-00754 to 754 for filenames.

2) Reference formatting
Keep raw database/API reference unchanged.
Use display-only formatting helpers.
Success page, customer email, internal email, PDF voucher should display #754.
Copy button should copy #754.
PDF/email filename should use numeric safe format without #.

3) Email logo
Remote logo image in customer email is acceptable only with safe fallback.
Keep alt text and/or visible text fallback so the email still looks acceptable if Gmail blocks images.
Do not use advanced email CSS.

4) PDF logo
Using a copied logo asset inside api-server is acceptable.
But logo loading/embed failure must never crash PDF generation.
If logo cannot be loaded or embedded, fall back to drawing text "Tbilisicars" as today.
No uncaught module-load errors.

5) PDF vehicle image
Vehicle image is desired, but must be implemented with strict silent fallback.
If image URL is missing, relative path cannot be resolved, fetch fails, image is unsupported, or timeout/error occurs, the PDF must still generate normally without the image.
Do not add packages.
Do not change storage logic.
Do not change booking creation/pricing logic.
Do not let vehicle image fetch block or break email sending.

6) PDF layout
Vehicle image should be visible but not too large, placed in the right-side free space near Trip Details / vehicle-location area.
It must not push existing text out of place and should keep the voucher one page if possible.

7) Customer email redesign
Keep it email-client-safe.
Improve spacing and hierarchy.
Change total amount color away from red/alarm style.
Discount line should remain green.
Do not change pricing calculations.

8) Internal email
Make it more scannable and use #754 display reference.
Do not over-design it.
No delivery behavior changes.

9) File scope
List final exact files to change.
Avoid new files except the copied api-server logo asset if truly needed.
No DB schema change.
No new package.
No pricing/payment/availability/booking creation changes.

Return the revised implementation plan only.