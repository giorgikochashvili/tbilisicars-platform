Revise only small Dashboard Operations extras chip UI.

Target:
CRM Dashboard → Operations → Pickups/Dropoffs → vehicle block extras chip.

Current state:
The extras chip now appears, for example “Seat”, but it is displayed under the CONFIRMED badge and makes the vehicle block too tall.

Required change:
1. Move the extras chip to the right side of the booking status badge.
   Example layout:
   [CONFIRMED] [Seat +1]

2. The extras chip should stay on the same row as the status badge when space allows.
   - Desktop: status badge and extras chip should be inline.
   - Mobile: also try inline; if very narrow, it may wrap naturally, but should not break layout.

3. Show quantity/count in the chip.
   - If quantity is 1, show: “Seat +1”
   - If quantity is 2, show: “Seat +2”
   - If multiple extras exist, show compactly:
     [Seat +1] [SIM +1]
   - If there are more than the display limit:
     desktop: up to 3 chips then [+N]
     mobile: up to 2 chips then [+N]

4. Keep short labels:
   - Child Seat → Seat
   - Baby Seat → Baby
   - SIM Card → SIM
   - Additional Driver → Driver
   - Booster Seat → Booster
   - WiFi Router / Wi-Fi → WiFi
   - GPS Navigation → GPS
   - Snow Chains → Chains
   - Cross Border Permit → Border

5. Keep extras chip green/success/informational, not red.
   - Must remain visually consistent with dark CRM theme.
   - Slightly readable, compact, clean.

6. Do not increase row height unnecessarily.
   - The main purpose is to avoid making the vehicle block taller.
   - Keep status + extras in a flex row with wrapping only if needed.

Strict guardrails:
- Do not touch backend.
- Do not touch API services.
- Do not touch OpenAPI.
- Do not run codegen.
- Do not touch generated files.
- Do not touch database.
- Do not change extras loading/mapping logic.
- Do not change booking/pricing/payment/status/fleet logic.
- Do not touch website/SEO/email/PDF.
- Only change Dashboard.tsx visual rendering/classes if possible.

After change:
- Report exact file changed.
- Confirm no backend/API/OpenAPI/codegen/generated files were touched.
- Confirm desktop and mobile layout behavior.
- Run CRM check/typecheck if available.
- Goal: 0 new errors.