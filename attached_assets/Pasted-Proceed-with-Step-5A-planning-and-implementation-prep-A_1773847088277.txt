Proceed with Step 5A planning and implementation prep: Admin Session Authentication.

Critical context:
- Database foundation is live
- Public API foundation is live
- Step 4B auth/session stubs need correction before protected endpoints
- Do NOT modify web or crm in this step
- Do NOT change database schema
- Focus only on api-server, api-spec, generated api-zod/client outputs, and strictly required package configuration

Primary goal:
Correct the session identity model and implement admin session authentication safely, without mixing customer and admin identity semantics.

Required scope:

1. Correct session model
- In auth/session typing, change customer session identity to:
  - userId?: number
- Add admin session identity:
  - adminId?: number
- Remove:
  - isAdmin?: boolean
- Ensure session typing is shared consistently across auth middleware and auth routes

2. Correct protected middleware semantics
- Update requireAuth to check req.session.userId as number
- Update requireAdmin to check req.session.adminId as number
- requireAdmin must query adminsTable by:
  - adminsTable.id = req.session.adminId
  - and isActive = true
- Return:
  - 401 if no adminId in session
  - 403 if admin row is not found or inactive

3. Implement admin auth endpoints
Add these endpoints to OpenAPI first:
- POST /api/auth/admin/login
- POST /api/auth/admin/logout
- GET /api/auth/admin/me

4. Admin login behavior
- Request body: { email: string, password: string }
- Look up adminsTable by email
- Verify hashedPassword using bcrypt
- On success:
  - set req.session.adminId = admin.id
  - respond with safe admin payload only:
    { id, email, fullName, adminRole, permission flags }
- On failure:
  - return 401 with a generic invalid-credentials message
  - do not reveal whether email or password was wrong

5. Admin logout behavior
- Destroy session safely
- Return 200 JSON response

6. Admin me behavior
- Protect with requireAdmin
- Return current admin profile:
  - id
  - email
  - fullName
  - adminRole
  - permission flags

7. OpenAPI-first workflow
- Update lib/api-spec/openapi.yaml first
- Run codegen
- Then implement routes/services/middleware updates
- Use generated Zod schemas where appropriate

8. Validation
- Run typecheck
- Smoke test full admin flow:
  - login
  - access /api/auth/admin/me
  - logout
  - confirm /api/auth/admin/me returns 401

Out of scope:
- Customer auth / Replit OIDC
- CRM UI
- Customer protected endpoints
- Bookings mutations
- CRM-only modules beyond admin auth foundation

Output:
- exact files to update/create
- any blockers before BUILD
- exact Step 5A BUILD prompt

End with:
Ready for Step 5A BUILD — Admin Session Authentication