You are working on an existing Replit-built monorepo project for Tbilisicars CRM & Website.

IMPORTANT:
Do NOT redesign the app.
Do NOT introduce new features.
Do NOT change business logic unless required to restore the working state.
Do NOT overwrite working integrations.
Your only goal is to restore the local Replit development environment so the existing website and CRM work correctly again.

CURRENT PROBLEMS TO FIX
1. Public website homepage loads, but booking location data is not loading from the API.
2. CRM login page loads, but the known admin account admin@tbilisicars.com / 123456 is not authenticating locally.
3. Local development servers/ports may be misaligned with the current .replit configuration.
4. The project must be returned to a stable local preview state before any Git commit or deployment.

KNOWN PROJECT PORTS
Based on the current Replit configuration:
- API server should run on port 8080
- Website should run on port 19161
- CRM should run on port 22444

YOUR TASK
Inspect the current project and restore the working local state carefully.

REQUIRED ACTIONS
1. Inspect the current .replit file, package scripts, and dev commands for:
   - artifacts/api-server
   - artifacts/website
   - artifacts/crm

2. Verify that:
   - API runs on 8080
   - Website runs on 19161
   - CRM runs on 22444

3. Inspect how the website and CRM determine the API base URL in local development.
   - Fix any broken proxy/base URL/env mismatch so website data loads correctly from the API.
   - Fix any broken CRM-to-API auth connectivity.

4. Inspect the booking-config flow end-to-end:
   - frontend call
   - request URL
   - API route
   - returned data
   - rendered location dropdown

5. Inspect CRM admin auth end-to-end:
   - login form submit
   - request URL
   - API route
   - database lookup
   - session behavior

6. Determine whether the local database currently contains the expected admin user.
   - If the local dev DB is missing the seeded admin user, restore the expected local seed state safely.
   - Do not destroy existing schema.
   - Only run the appropriate seed/init flow already used by this project if needed.

7. Fix only what is necessary to restore:
   - website booking-config loading
   - CRM login with admin@tbilisicars.com / 123456
   - correct local routing and preview behavior

8. Preserve all recent branding/content/page polish changes unless they directly caused the breakage.

9. At the end, provide:
   - exact root cause(s)
   - exact files changed
   - exact commands required to run API, Website, and CRM locally
   - whether seed/reset was needed
   - manual QA checklist for:
     - homepage locations loading
     - CRM login
     - booking-config API response
     - website ↔ API sync

SUCCESS CRITERIA
- Homepage pickup locations load again
- CRM login works again for admin@tbilisicars.com / 123456 in local Replit preview
- No unnecessary architectural rewrites
- No feature scope creep
- No deployment changes yet