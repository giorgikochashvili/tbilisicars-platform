You are working on the existing Tbilisicars CRM project.

Do NOT rebuild or change the existing TBS AIR PARKING module.
It is already implemented and working.

Goal:
Add a Parking Overview widget to the CRM Dashboard that displays live parking status for Tbilisi Airport and links to the TBS AIR PARKING page.

==================================================
FEATURE — DASHBOARD PARKING OVERVIEW
==================================================

Business logic:
Dashboard must show a quick overview of parking zones:

- TERMINAL (max 5)
- OUT (max 10)
- FREE (unlimited)

This is a monitoring widget only.
Full management remains in the TBS AIR PARKING page.

--------------------------------------------------
1. Dashboard widget UI
--------------------------------------------------

Add a new section/card to the Dashboard page.

Title:
TBS AIR PARKING

Inside it display 3 mini-cards:

TERMINAL
- show occupancy: 4 / 5
- show FULL badge when 5 reached

OUT
- show occupancy: 7 / 10
- show FULL badge when 10 reached

FREE
- show number of vehicles: 6 vehicles
- no FULL state

Design:
- match existing dashboard UI (dark theme)
- clean and compact
- same style as Fleet Live Status cards

--------------------------------------------------
2. Data source
--------------------------------------------------

Use existing API:
GET /api/admin/parking

Do NOT create new API if not needed.

Calculate occupancy per zone dynamically.

--------------------------------------------------
3. Click behavior
--------------------------------------------------

The entire widget must be clickable.

On click:
→ navigate to /tbs-parking

--------------------------------------------------
4. UX
--------------------------------------------------

- cursor pointer on hover
- subtle hover highlight
- consistent with other clickable dashboard elements

--------------------------------------------------
5. Performance
--------------------------------------------------

- keep it lightweight
- avoid unnecessary extra API calls

--------------------------------------------------
6. Out of scope
--------------------------------------------------

Do NOT:
- redesign dashboard
- move existing widgets
- add editing inside dashboard
- duplicate parking logic

--------------------------------------------------
7. Verification
--------------------------------------------------

- widget visible on dashboard
- correct data shown
- FULL works correctly
- click navigates to /tbs-parking
- build passes

--------------------------------------------------
OUTPUT
--------------------------------------------------

Summarize implementation and confirm build success.