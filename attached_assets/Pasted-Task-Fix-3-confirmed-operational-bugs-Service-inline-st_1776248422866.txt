Task: Fix 3 confirmed operational bugs (Service inline status, website displayed price mismatch, booking detail action links)

Context:
This is a live production-connected monorepo (Tbilisicars CRM & WEB), currently operating on Batumicars.com as the temporary domain.
We are in operational stabilization phase.
Do NOT refactor architecture.
Do NOT redesign pages/modules.
Do NOT change unrelated flows.
Only address the three confirmed issues below with minimal, surgical fixes.

Confirmed issues to audit and fix:

1) Service module — status change still has too much click friction
Current state:
- Quick status change was added inside the row action dropdown submenu
- It works
But operationally this is still too hidden / click-heavy.

Required goal:
- Allow service status to be changed directly from the list row, visibly and quickly, without going through the 3-dot menu submenu
- Keep this lightweight and consistent with the existing UI
- Do NOT redesign the whole table
- Do NOT remove the existing full edit path
- Prefer a simple inline status control in the row itself if feasible

2) Website vehicle list/search price display is using the wrong rate bucket
Confirmed behavior:
- Vehicle cards in the booking/search step show the last 31+ day rate price
- But once selected, the booking summary/quote correctly calculates using the actual rental duration bucket
Example:
- Card visually suggests 31+ price
- Actual quote for 7 days correctly calculates 33 x 7 = 231 EUR

Required goal:
- In the vehicle selection/search results, displayed price must match the actual duration bucket for the currently selected trip length
- The displayed card price and the real quote logic must be aligned
- Do NOT change the actual pricing engine if quote calculation is already correct
- Fix only the display/source-of-price logic

3) Booking detail top action buttons have broken paths
Confirmed behavior:
- In booking detail, Edit Booking works
- But the other top action buttons are missing a leading "/" in the path
Affected buttons:
- Rental Agreement
- Booking Voucher
- Handover Sheet
- Return Sheet

Required goal:
- Fix these navigation paths so they open correctly from booking detail
- Only correct the broken route construction
- Do NOT redesign these buttons/pages

Instructions:
First inspect exact current implementation and confirm root cause for each.
Then implement only minimal fixes.
Preserve all existing business logic and compatibility.

Output format:
A. Exact root cause for each of the 3 bugs
B. Minimal implementation plan
C. Exact files to change
D. What will NOT be changed