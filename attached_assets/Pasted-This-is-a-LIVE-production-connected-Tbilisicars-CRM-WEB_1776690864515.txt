This is a LIVE production-connected Tbilisicars CRM & WEB project.
The project is already fully built and working in Replit.
DO NOT change architecture.
DO NOT refactor unrelated code.
DO NOT break existing pricing, booking, promo, voucher, CRM, or website flows.
Make only the exact scoped improvement below.

TASK:
Add a new Discount system for WEBSITE pricing only.

BUSINESS GOAL:
We already have a working WEB pricing system with Parent/Child rates, day-range tiers, one-way fee logic, extras logic, and promo logic.
We now need a separate Discount layer that can temporarily reduce website prices for selected models, within selected dates, and for selected pickup locations.

CRITICAL RULE:
Discounts must NOT overwrite or mutate existing rate plans.
Discounts must be applied as a separate layer AFTER the normal WEB price is calculated.

FINAL PRICE ORDER:
1. Calculate normal WEBSITE rental price from existing WEB pricing logic
   - including Parent/Child rate logic
   - including correct day-range tier logic
2. Then check whether an active Discount applies
3. If yes, reduce the rental price
4. Then continue the rest of the flow normally

IMPORTANT:
- Discount affects rental price only
- Discount must NOT reduce one-way fee
- Discount must NOT reduce extras
- Discount must NOT break existing promo behavior unless current logic already combines on rental price only
- Preserve current existing behavior for all non-discounted bookings

SCOPE TO IMPLEMENT

==================================================
A) NEW DISCOUNT DATA MODEL / BACKEND SUPPORT
==================================================

Create a dedicated Discount entity/table/model/service for WEBSITE discounts.

Required fields:
- id
- name
- discountType: "PERCENT" or "FIXED"
- value
- startDate
- endDate
- pickupLocationId
- isActive
- createdAt
- updatedAt

Also support relation/association to selected vehicle models:
- one discount can target multiple vehicle models
- selected models must be stored properly (join table or current project-consistent approach)

VALIDATION RULES:
1. No overlap allowed for the same:
   - pickupLocationId
   - vehicleModelId
   - active date range overlap
2. If an overlapping discount is attempted for the same model + same pickup location + overlapping period:
   - reject creation/update with a clear validation error
3. discountType=PERCENT:
   - value must be > 0
   - value must be <= 100
4. discountType=FIXED:
   - value must be > 0
5. startDate <= endDate
6. Must require at least one selected model
7. Must require pickup location
8. Must support active/inactive toggle
9. Discount is WEBSITE pricing only. Do not connect this to BROKER rates.

==================================================
B) CRM ADMIN UI — NEW DISCOUNTS MANAGEMENT
==================================================

Add a new CRM page/module for Discounts.
Keep visual style consistent with the dark CRM design.

Required capabilities:
1. Sidebar/menu entry:
   - "Discounts"
2. Discounts list page:
   - table/cards with:
     - Name
     - Pickup Location
     - Models count or short model preview
     - Discount type/value
     - Date range
     - Status
     - Actions
3. Add Discount button
4. Edit Discount
5. Enable/Disable discount
6. Delete discount (only if project conventions already allow destructive delete safely; otherwise soft-disable is preferred)

Discount form must include:
- Name
- Pickup Location (single select)
- Vehicle Models (multi-select)
- Discount Type (% or fixed)
- Value
- Start Date
- End Date
- Active toggle

UI/UX REQUIREMENTS:
- Show a clear validation error if overlap exists
- Keep it simple and reliable
- Do not redesign unrelated Rates page
- This is a separate Discount module/page, not embedded into Parent/Child rate grid editing

==================================================
C) WEBSITE PRICE ENGINE INTEGRATION
==================================================

Integrate the Discount layer into the website/public booking pricing pipeline.

RULE:
For a website search/booking, discount applies only if ALL are true:
- pickup location matches discount.pickupLocationId
- selected vehicle model is in the discount’s assigned models
- booking pickup date is within discount date range
- discount is active

Important:
- Apply discount to rental/base price only
- Do not apply to one-way fee
- Do not apply to extras
- Do not silently alter taxes/fees logic unless existing engine already calculates total from rental price + fees
- Keep all current calculations stable

OUTPUT REQUIREMENTS:
Where pricing data is returned for website vehicle cards / booking flow, include enough structured fields to support UI display, such as:
- originalRentalPrice
- discountedRentalPrice
- appliedDiscount
- discountType
- discountValue
- hasDiscount
- discountAmount
Use current project naming conventions if slightly different, but preserve this meaning clearly.

==================================================
D) WEBSITE UI DISPLAY
==================================================

For vehicle cards on the website, when a discount applies to that model/search:
1. Show original price crossed out
2. Show discounted price prominently
3. Show a clean badge such as:
   - "-10%"
   - or "SALE"
4. Keep design elegant, modern, and consistent with the current website
5. Only show this when discount actually applies
6. Do not create layout breakage on desktop or mobile

Also ensure the discounted price is used consistently in:
- booking step pricing
- booking summary/sidebar
- final booking calculation

==================================================
E) VOUCHER / PRICE BREAKDOWN
==================================================

The booking voucher / booking confirmation pricing breakdown must reflect discount transparently.

If discount was applied, show a breakdown like:
- Rental price: X
- Discount: -Y
- Final rental price: Z

Then continue with:
- extras
- one-way fee
- totals
according to current project structure

Important:
- Do not hide the discount
- Do not make voucher ambiguous
- Keep layout consistent with existing PDF/email design

If internal/customer email price breakdown currently supports this same structure safely, include the same discount line there too.
If not already structured for that, at minimum the voucher/PDF must show it correctly.

==================================================
F) BOOKING PERSISTENCE / AUDITABILITY
==================================================

When a discount is applied to a booking, persist enough information so that later price breakdown remains historically correct even if the discount rule is edited or disabled later.

Store discount snapshot data on booking creation, such as:
- discountId
- discountName
- discountType
- discountValue
- discountAmount
- originalRentalPrice
- discountedRentalPrice

Use current project conventions and naming style, but preserve this exact meaning.

IMPORTANT:
Do NOT recalculate past bookings from live discount rules when rendering voucher/history.
Past booking documents must reflect the discount state at booking creation time.

==================================================
G) DO NOT BREAK EXISTING FEATURES
==================================================

Must preserve:
- Parent/Child WEB rates
- BROKER rates
- Promo logic
- One-way fee logic
- Extras pricing logic
- Existing website booking flow
- Existing CRM booking flows
- Existing voucher generation
- Existing email sending
- Existing booking total persistence

If a discount does not apply, everything must behave exactly as before.

==================================================
H) IMPLEMENTATION STYLE
==================================================

- Make surgical changes only
- Reuse existing project patterns, API style, and naming conventions
- Prefer extending existing pricing response objects carefully rather than reworking architecture
- Add only the minimum DB/API/UI changes needed
- Keep code understandable and future-safe
- No placeholder logic
- No mock data
- No TODO-only implementation

==================================================
I) VALIDATION / ACCEPTANCE CRITERIA
==================================================

Done looks like:

CRM:
- New Discounts page exists
- Admin can create a discount with:
  - pickup location
  - selected models
  - type
  - value
  - date range
  - active status
- Overlapping discount for same model + same pickup location + overlapping period is blocked

Website:
- Matching discounted models show:
  - crossed-out old price
  - new discounted price
  - discount badge
- Non-discounted models behave exactly as before
- Price used in booking summary is the discounted rental price when applicable

Pricing integrity:
- Discount changes rental price only
- One-way fee remains untouched
- Extras remain untouched

Voucher / booking records:
- Discount appears in price breakdown clearly
- Booking stores discount snapshot data so historical bookings remain correct

Please implement the feature completely and carefully, then summarize:
1. database changes
2. backend/API changes
3. CRM UI changes
4. website UI changes
5. voucher/document changes
6. exact validation rules added
7. exact files changed