# BUILD TASK — Tbilisicars CRM auth stabilization + team member login fix + remove all placeholders project-wide

Project context
This is the existing Tbilisicars CRM&WEB monorepo project.
Important: this project has been built iteratively inside Replit, and much of the current logic, structure, and flow is Replit-generated and adapted over time.
Because of that, you must first understand and preserve the existing project logic instead of replacing it with a brand-new architecture.

Critical instruction
Do NOT refactor unrelated modules.
Do NOT redesign the product.
Do NOT replace the auth system.
Do NOT introduce JWT or a new auth architecture.
Do NOT remove the recently added Team Roles & Permissions work.
Audit the existing Replit-built logic first, then fix only the broken/inconsistent parts in one careful pass.

Preserve:
- current CRM structure
- current Team module direction
- current roles/permissions work
- current dark UI styling
- current session-based admin auth architecture

==================================================
PRIMARY GOALS
==================================================

Fix all currently broken or inconsistent CRM auth/account flows:

1) Main admin login / logout / relogin flow
2) Post-login redirect path bug (/crmdashboard must never happen)
3) Session persistence after login and refresh
4) Newly created Team members must be able to log in with the exact password entered during creation
5) Team member password save/update flow must be correct and secure
6) Remove ALL placeholder text from input-like fields across the entire project

==================================================
KNOWN PROBLEMS TO FIX
==================================================

A. LOGIN REDIRECT BUG
A successful CRM login has incorrectly redirected to:
/crmdashboard

Correct path must always be:
/crm/dashboard

This must be fixed permanently and safely, using the actual current routing/base-path logic of the project.

B. SESSION / COOKIE INSTABILITY
The CRM is deployed behind Nginx with the CRM served at /crm.
Login may succeed at the backend level, but frontend auth/session behavior can be inconsistent if cookie/session handling is not correct for this proxy setup.

The final behavior must be stable:
- login works
- refresh keeps the session
- logout works
- login again works

C. NEW TEAM MEMBER LOGIN FAILURE
Team members can be created in the Team module, but newly created accounts are not able to authenticate using the password entered during creation.

Likely failure points to audit and fix:
- password field ignored in frontend payload
- backend create-member flow not receiving password correctly
- password not hashed before insert
- hashed_password not stored correctly
- update flow incorrectly overwriting or clearing password
- login verification path inconsistent between main admin and created team members

D. PROJECT-WIDE PLACEHOLDER REMOVAL
Across the whole CRM and website project, there should be no placeholder text visible inside any field.
That means:
- no placeholder text in inputs
- no placeholder text in textareas
- no placeholder text in search fields
- no sample text inside auth fields
- no faint example text like email examples, name examples, search examples, etc.

Remove placeholders project-wide while preserving labels, accessibility, and usability.

==================================================
DONE LOOKS LIKE
==================================================

A) Main Admin flow works fully
- log in successfully
- redirected correctly to /crm/dashboard
- refresh does not kick user out
- logout works
- login again works

B) New Team Member flow works fully
- create a new team member from Team page with password
- password is stored correctly as a secure hash
- new team member can log in with that exact password
- refresh works
- logout works
- log in again works

C) Edit Team Member password behavior is correct
- if edit form password is blank, existing password remains unchanged
- if a new password is entered, it replaces the old one after proper hashing
- old password stops working, new password works

D) Redirect correctness
- never /crmdashboard
- always /crm/dashboard after successful login

E) Placeholders are removed project-wide
- no input placeholders anywhere in CRM
- no input placeholders anywhere in website/public-facing forms
- labels and UI still remain clear

F) No regression
- existing roles/permissions work remains intact
- existing main admin still works
- Team module still works
- no unrelated modules are broken

==================================================
PHASE 1 — AUDIT EXISTING REPLIT-BUILT LOGIC
==================================================

Before changing anything, inspect and understand the current real project flow.

Audit these areas carefully:

1. Frontend CRM login flow
- artifacts/crm/src/pages/Login.tsx
- how submit is handled
- whether it uses setLocation, window.location.href, navigate, or a mix
- how BASE_URL / BASE_PATH is joined
- what happens after successful login
- whether current-user/auth state is refreshed after login

2. Frontend auth state
- artifacts/crm/src/hooks/use-auth.tsx
- any auth provider / query layer / react-query usage
- how current user is loaded
- whether credentials are included correctly
- how logout clears auth state
- whether login invalidates/refetches the current user query

3. Backend session/auth
- artifacts/api-server/src/app.ts
- express-session config
- cookie settings
- trust proxy behavior
- login/logout/me endpoints
- ensure final config actually matches the deployed reverse-proxy architecture

4. Team member create/update flow
- artifacts/api-server/src/services/admin-team.service.ts
- artifacts/api-server/src/routes/admin-team.ts
- any related validators/schemas
- verify password handling on create
- verify password handling on edit
- verify hashed_password is actually written to DB

5. Admin auth verification path
- artifacts/api-server/src/services/admin-auth.service.ts
- password hashing + comparison logic
- make sure the exact same verification path works for Main Admin and newly created Team members

6. Placeholder usage across the whole repo
Audit all relevant frontend code in:
- artifacts/crm/src
- artifacts/website/src
Find all placeholder= usages and remove them carefully without damaging field labels or usability.

==================================================
PHASE 2 — IMPLEMENT THE FIXES
==================================================

Implement only the necessary fixes, preserving the current Replit-built project logic.

--------------------------------------
A. LOGIN REDIRECT FIX
--------------------------------------

Fix the CRM login redirect so it always lands on /crm/dashboard correctly.

Requirements:
- no brittle string concatenation
- no /crmdashboard bug
- use a safe route/base-path join strategy
- preserve the current router/navigation pattern already used in the project where possible

If the current implementation mixes window.location.href and router navigation, simplify it only as much as needed for correctness.

--------------------------------------
B. SESSION / COOKIE STABILIZATION
--------------------------------------

Make session persistence stable for the actual deployed environment:
- CRM under /crm
- API behind Nginx reverse proxy
- session-based auth

Requirements:
- login works
- refresh keeps session
- logout works
- login again works
- do not blindly hardcode insecure settings if a proxy-aware correct solution already exists
- preserve production-safe behavior where possible, but make the real deployed flow function reliably

--------------------------------------
C. TEAM MEMBER PASSWORD CREATE/UPDATE FIX
--------------------------------------

Fix Team member create/update so newly created accounts can authenticate.

Required behavior:

On create member:
- password is required
- password reaches backend correctly
- password is hashed before insert
- hashed_password is stored correctly
- created account is login-compatible immediately

On edit member:
- if password field is empty/blank, keep old password unchanged
- if password field is provided, hash and replace stored password
- do not null out or corrupt hashed_password

Make sure frontend payload keys and backend expected fields match exactly.

--------------------------------------
D. LOGIN COMPATIBILITY CONSISTENCY
--------------------------------------

Ensure all admin/team accounts use one consistent auth path:
- same login endpoint
- same hash verification format
- same current-user/session logic

Main Admin and newly created team members must both authenticate through the same reliable path.

--------------------------------------
E. FRONTEND AUTH STATE FIX
--------------------------------------

After successful login:
- current user state must refresh correctly
- protected routes must recognize auth immediately
- user must land in CRM correctly

After logout:
- auth state/cache must clear correctly
- user must return to login state correctly
- logging back in must work immediately

Use the project’s existing state/query approach; do not replace it with a new architecture.

--------------------------------------
F. REMOVE ALL PLACEHOLDERS PROJECT-WIDE
--------------------------------------

Remove placeholder text from all applicable fields across the whole project.

Scope includes:
- CRM login fields
- Team member forms
- booking/admin forms
- search fields
- filters
- website/public forms
- any other text inputs, textareas, or similar controls

Rules:
- remove placeholder props/values entirely where safe
- if a component requires a placeholder prop for some reason, pass an empty string only if necessary
- preserve visible labels, helper text, and accessibility
- do not replace placeholders with other faint in-field example text
- final UI should show no sample hint text inside fields anywhere

==================================================
PHASE 3 — VALIDATION / SAFETY
==================================================

Add or preserve these safety rules:

1. Team member creation validation
- missing password on create must be rejected
- keep unique-email validation
- preserve any existing sensible minimum password validation if already present

2. Logging
Add concise, useful debug-safe logs only where needed:
- team member created successfully
- hashed password stored
- login success/failure
Do NOT log plaintext passwords, secrets, or sensitive session data.

3. Security
- never store plaintext passwords
- never return password values in API responses
- do not weaken auth design beyond what is necessary to make the deployed proxy/session flow work correctly

==================================================
PHASE 4 — VERIFY WITH REAL FLOWS
==================================================

After implementing, verify these exact cases in code:

1. Main Admin
- login
- redirect to /crm/dashboard
- refresh
- logout
- login again

2. New Team Member
- create member from Team page with password
- log out
- log in with exact new member credentials
- refresh
- logout
- log in again

3. Edit existing Team Member
- edit with blank password → old password still works
- edit with new password → new password works, old password no longer works

4. Placeholder removal
- search repo for placeholder usage and ensure no user-facing placeholder text remains in CRM or website fields

==================================================
FILES MOST LIKELY TO TOUCH
==================================================

Only touch what is necessary, most likely including:
- artifacts/crm/src/pages/Login.tsx
- artifacts/crm/src/hooks/use-auth.tsx
- artifacts/crm/src/App.tsx
- artifacts/crm/src/pages/Team.tsx
- artifacts/api-server/src/app.ts
- artifacts/api-server/src/services/admin-auth.service.ts
- artifacts/api-server/src/services/admin-team.service.ts
- artifacts/api-server/src/routes/admin-team.ts
- any shared admin auth utility already used
- frontend files across artifacts/crm/src and artifacts/website/src where placeholder props are present

==================================================
IMPORTANT NON-GOALS
==================================================

Do NOT:
- refactor the whole project
- replace session auth with JWT
- redesign Team/roles architecture
- remove roles/permissions work
- touch website business logic unrelated to placeholder removal
- make unrelated visual redesigns
- change deployment/infrastructure architecture unless absolutely required for auth correctness

==================================================
OUTPUT
==================================================

Implement the fixes directly.

At the end, return:
1. What was broken
2. What files were changed
3. What exactly was fixed in:
   - login
   - logout
   - session persistence
   - Team member creation/login
   - redirect behavior
   - placeholder removal
4. Any deployment/build steps required after the changes