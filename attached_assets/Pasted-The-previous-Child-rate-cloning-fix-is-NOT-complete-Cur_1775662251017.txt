The previous Child-rate cloning fix is NOT complete.

Current observed result:
- Parent WEB rate opens in the correct ModelPricingGrid matrix layout
- Child rate created from that Parent still opens in the flat "Pricing Tiers" list view
- This is unacceptable

Required outcome:
A Child created from a Parent must open in the EXACT SAME layout and structure as the Parent.

This is not just a data-copy issue.
This is a strict UI + data parity requirement.

Business rule:
When a Child is created from a Parent, the Child must behave as a structural clone of the Parent at creation time.

That means the Child must have:
- the same day-range buckets
- the same model rows
- the same grid structure
- the same matrix-style editing layout
- the same visual rendering mode (ModelPricingGrid, not RateTiers)

CRITICAL:
Do NOT assume the previous fix worked just because day-ranges were PUT to the child.
The task is only complete if the Child actually renders in the same ModelPricingGrid layout as the Parent in the CRM UI.

What to investigate:
1. Why the Child still falls back to Pricing Tiers list view after creation
2. Whether day-ranges are truly being persisted on the child
3. Whether renderExpandedContent is still routing the child into RateTiers because of stale/incomplete data
4. Whether the expanded child detail payload differs from the parent in a way that prevents ModelPricingGrid rendering
5. Whether a refresh/reload is required to see day-ranges and if so, fix that behavior

Required fix:
1. Ensure the Child really persists the Parent's day-ranges on creation
2. Ensure the Child detail/query data used by the expanded view immediately reflects those day-ranges
3. Ensure renderExpandedContent uses the same ModelPricingGrid path for the Child as for the Parent
4. Ensure the Child opens in the SAME matrix/grid layout as the Parent immediately after creation, without requiring manual hacks
5. Preserve the same model ordering, column/day-range ordering, and editing style as the Parent

Important:
- Do NOT redesign the Rates page
- Do NOT change DB schema
- Do NOT change pricing engine
- Do NOT change public website logic
- Do NOT add new features
- Do NOT expand scope into deactivation or pricing fallback
- Fix only the exact reason why the Child still renders differently from the Parent

Done looks like:
- Create Parent with day-range grid
- Create Child from that Parent
- Expand Child
- Child opens in the same ModelPricingGrid layout as Parent
- No flat Pricing Tiers fallback for that Child
- Same columns, same row structure, same edit behavior
- Parent remains unaffected

Return:
1. Root cause
2. Exact file(s) changed
3. Why the previous fix was insufficient
4. Exact reason Child was still rendering in RateTiers
5. Confirmation that the Child now opens in the same ModelPricingGrid layout as the Parent