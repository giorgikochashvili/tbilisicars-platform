Execute a SAFE, MINIMAL, PRODUCTION-GRADE fix for Tbilisicars CRM & WEB.

PROJECT CONTEXT
- This is a LIVE production monorepo already built in Replit.
- The system is largely working now.
- DO NOT refactor architecture.
- DO NOT rewrite working flows.
- DO NOT touch auth, logout, CORS, env, deploy, or unrelated modules.
- DO NOT change database schema unless absolutely unavoidable. Prefer no schema changes.
- DO NOT break Create Booking, payment creation, vehicle assignment, pickup, dropoff, dashboard, or parking pages.
- All changes must be minimal, additive, and targeted.

PRIMARY GOAL
Improve operational consistency between:
1) manual DELIVERED bookings
2) vehicle live status / rented state
3) TBS AIR PARKING behavior
4) Drop Off workflow at Tbilisi International Airport

IMPORTANT BUSINESS RULES
- DELIVERED booking with an assigned vehicle must be treated operationally as a rented / out vehicle.
- Booking status and payment status are related but NOT the same thing.
- DELIVERED must NOT automatically force PAID.
- PAID must NOT automatically force DELIVERED.
- If a manual booking is created as DELIVERED and payment information is entered, both booking and payment must continue to work normally exactly as before.
- Existing stable behavior must be preserved.

STRICT SAFETY RULES
- First inspect existing code paths before changing anything.
- Reuse existing functions and lifecycle logic wherever possible.
- Do NOT invent parallel logic if an existing helper already handles status side-effects safely.
- Do NOT apply side-effects in a way that breaks Create Booking.
- Do NOT touch any logic unrelated to the scoped tasks below.
- If a requested behavior is already correctly implemented in one flow, reuse that exact logic path.

SCOPED TASKS ONLY

==================================================
TASK 1 — KEEP CREATE BOOKING STABLE
==================================================

Create Booking must remain fully functional.

That means after this task:
- booking creation must work
- payment creation/registration must work
- assigned vehicle must still save correctly
- no internal server error
- no regression in manual booking flow

Before changing any create flow:
- inspect how payment creation currently works for manual bookings
- inspect how assigned vehicle is currently saved
- inspect how DELIVERED status is currently handled in create vs update vs pickup/dropoff flows

DO NOT introduce any change that can break booking creation.

==================================================
TASK 2 — MANUAL DELIVERED BOOKING MUST COUNT AS ACTIVE RENTAL
==================================================

Problem to solve:
If staff manually creates a booking that is already operationally delivered / handed over, the assigned vehicle must be reflected as rented / out in operational views.

Required outcome:
- A manually created booking with status DELIVERED and an assigned vehicle must result in that vehicle being treated operationally as RENTED / out
- Fleet Live Status must reflect that correctly
- The vehicle should not continue to appear operationally available if it is already delivered

Implementation guidance:
- Inspect the existing logic already used when a booking becomes operationally active through normal successful flows
- Reuse the existing side-effect path that safely synchronizes vehicle status
- Do NOT duplicate or re-implement business logic if an existing helper already does it correctly
- Apply the synchronization in the create path ONLY in the safe case where:
  - booking is successfully created
  - status is DELIVERED
  - assigned vehicle exists
- If there is no assigned vehicle, do not force vehicle-side effects

IMPORTANT:
- Do NOT broaden this into unrelated statuses unless existing code already safely handles them and it is necessary
- Be conservative and safe
- Prevent breaking Create Booking

==================================================
TASK 3 — PICK UP MUST REMOVE VEHICLE FROM TBS AIR PARKING
==================================================

Required behavior:
When Pick Up is successfully completed for a vehicle that is currently in TBS AIR PARKING, that vehicle must automatically be removed from TBS AIR PARKING.

Applies only to:
- TBS AIR PARKING
- zones TERMINAL / OUT / FREE

Requirements:
- Reuse existing parking removal logic if it already exists
- Do NOT create a second parking-removal implementation if one already exists
- Confirm pickup flow remains unchanged except for guaranteed synchronization
- Do NOT affect any future/non-Tbilisi parking systems

If this behavior is already correctly implemented in pickup flow:
- preserve it
- do not rewrite it
- only close any missing gap if one exists

==================================================
TASK 4 — ADD PARKING ZONE FIELD DIRECTLY INSIDE DROP OFF FORM
==================================================

This is the main UI enhancement.

Current desired UX:
When staff completes Drop Off and the return location is Tbilisi International Airport, the same Drop Off form/modal should include a parking zone selection field there in the form itself.

Do NOT implement this as a separate late popup if it can be safely implemented inside the existing Drop Off form.

Required behavior:
- In the Drop Off form/modal, detect when return/dropoff location is Tbilisi International Airport
- In that case, show an optional parking zone field
- Options:
  - TERMINAL
  - OUT
  - FREE
  - Skip / No parking assignment
- If the staff selects a zone and completes Drop Off successfully:
  - vehicle must be added automatically to the selected TBS AIR PARKING zone
- If staff chooses Skip / no parking assignment:
  - Drop Off must still complete normally
  - no parking assignment should be made
- This field must appear only for Tbilisi International Airport return cases
- For all other dropoff locations, Drop Off flow must remain unchanged

UX / implementation rules:
- Keep the UI minimal and consistent with the existing form
- Do not redesign the modal
- Do not break mobile layout
- The parking selection must not block Drop Off if skipped
- If parking assignment fails after successful Drop Off:
  - show a toast or error message
  - do NOT undo the Drop Off
  - do NOT corrupt booking state

==================================================
TASK 5 — PAYMENT LOGIC MUST REMAIN CORRECT
==================================================

This is critical.

Manual DELIVERED booking creation often includes payment information.
Do NOT break this.

Required behavior:
- If payment status/amount is entered during manual booking creation, the existing payment flow must continue to register it correctly
- Payment summary/history must remain correct
- Do not remove, bypass, or corrupt payment creation logic
- Do not create fake coupling where DELIVERED automatically means PAID
- Do not create fake coupling where PAID automatically means DELIVERED

If the current payment logic already works:
- preserve it exactly
- only ensure your changes do not interfere with it

==================================================
TASK 6 — DO NOT TOUCH THESE AREAS
==================================================

Forbidden areas:
- auth / logout
- CORS
- env handling
- deploy configuration
- nginx
- PM2
- pricing engine unrelated to this task
- voucher import unrelated to this task
- unrelated dashboard widgets
- unrelated parking systems
- database schema changes unless absolutely unavoidable

==================================================
IMPLEMENTATION APPROACH
==================================================

Before editing:
1. Inspect the existing Create Booking flow
2. Inspect existing payment creation flow inside booking creation
3. Inspect existing helper(s) used for booking status side-effects
4. Inspect existing pickup parking-removal flow
5. Inspect existing dropoff form and submission path
6. Find the safest existing integration point for:
   - manual DELIVERED vehicle sync
   - optional Tbilisi Airport parking zone assignment inside Drop Off form

Then implement only minimal changes.

==================================================
EXPECTED RESULT
==================================================

After the fix:
- Create Booking works exactly as before without internal server errors
- Manual DELIVERED booking with assigned vehicle is reflected operationally as RENTED / out
- Fleet Live Status becomes accurate for those cases
- Pick Up removes vehicle from TBS AIR PARKING
- Drop Off form at Tbilisi International Airport includes optional parking zone selection directly inside the same form
- Selecting TERMINAL / OUT / FREE adds the vehicle to that TBS AIR PARKING zone
- Selecting Skip does nothing and still completes Drop Off
- Payment logic remains intact
- No regressions in existing booking/payment/vehicle workflows

==================================================
FINAL OUTPUT REQUIRED
==================================================

After implementation, show:
1. The exact files changed
2. A concise explanation of each change
3. Confirmation that:
   - Create Booking still works
   - payment logic was preserved
   - no unrelated files were modified
   - no TypeScript/build errors remain

If any part of the requested change would risk breaking stable flows, choose the safest minimal implementation and explicitly say what you kept conservative.