Targeted customer-facing visual correction only.

We need to fix the customer confirmation email and PDF voucher visual polish after live testing.

Observed live issues:
1) Customer confirmation email header is too red and visually too heavy.
2) The logo in the customer email is too large and centered too dominantly.
3) The email should feel premium/dark navy, not red-heavy.
4) Pricing rows in the email are too tight/connected visually, for example labels and values look stuck together:
   Base rate (7 days)245.00 EUR
   Booster Seat35.00 EUR
   Discount-49.00 EUR
   Total Amount231.00 EUR
5) PDF voucher logo is too large and interferes with the header/design balance.

Important rules:
- Do not change booking logic.
- Do not change pricing calculations.
- Do not change database schema.
- Do not change API payloads.
- Do not change reference generation.
- Do not change email sending flow.
- Do not change PDF data/content logic.
- Do not touch website booking flow, CRM workflow, manual booking, Delivery Service, payment logic, availability, or vehicle assignment.
- No new packages.
- Keep this to email.service.ts and pdf-voucher.service.ts unless absolutely necessary.

Files to inspect/change:
- artifacts/api-server/src/services/email.service.ts
- artifacts/api-server/src/services/pdf-voucher.service.ts

Task A — Customer confirmation email header polish:
- Make dark navy the dominant header/background tone.
- Reduce the large red area significantly.
- Red should be used only as a small accent, not a big background block.
- Shrink the logo noticeably.
- Position the logo cleanly near the top-left area of the email header, not huge and centered.
- Keep the visible brand text “Tbilisicars” and “Car Rental Georgia” balanced near the logo.
- The first visible part of the email should look premium, clean, dark navy, and compact.
- Keep email HTML Gmail/Outlook safe with simple tables/divs and inline-safe CSS.

Task B — Customer confirmation email body spacing:
- Improve spacing and alignment in the pricing summary rows.
- Pricing rows must use a clear two-column layout:
  left label, right amount.
- Labels and values must not touch each other.
- Amounts should align consistently on the right.
- Discount line should remain green.
- Total amount should remain visually important but professional, not red/alarming.
- Keep current content and sections, just polish spacing/alignment.

Task C — PDF voucher logo size:
- Reduce the PDF voucher logo size substantially.
- The logo should not push, overlap, or visually disturb the header layout.
- Keep voucher header clean and balanced.
- If logo embedding fails, existing text fallback must continue to work.
- Do not remove important voucher content.

Task D — PDF voucher layout safety:
- After shrinking the logo, make sure the header text and booking reference/status block still align cleanly.
- Keep the PDF one page if it was one page.
- Do not change pricing calculations or booking data.

Validation:
- Run API build after changes.
- Confirm no TypeScript/build errors from changed files.
- Report exact files changed.
- Report exact visual values adjusted, especially:
  email header background/color,
  email logo size/position,
  email pricing row layout,
  PDF logo width/height.