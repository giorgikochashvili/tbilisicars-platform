You are working on the existing Tbilisicars CRM project.

Important:
Do NOT redesign the CRM.
Do NOT add new features right now.
Do NOT rewrite unrelated modules.
This is a focused bug-fix and integration-stabilization task.

Recent feature work introduced regressions across connected vehicle-based workflows. Your task is to inspect the real current implementation, find the actual root causes, and fix them cleanly.

==================================================
PRIMARY GOAL
==================================================

Stabilize and correctly reconnect the following broken flows:

1. New Booking:
- Brand / Model / Specific Vehicle cascade is broken or inconsistent
- submitting a valid booking can fail with HTTP 400 Bad Request: Invalid request parameters

2. TBS AIR PARKING:
- Enter Car in Parking modal does not correctly show/select real vehicles
- brand/model/vehicle options are not loading correctly
- staff cannot clearly see which actual vehicle is being assigned

3. Pick Up / Drop Off:
- modal fields such as date, mileage, fuel level, or similar inputs reset or behave as if the form refreshes while typing
- form state is unstable

This is a bug-fix pass only.

==================================================
BUG FIX BLOCK A — NEW BOOKING SUBMIT + VEHICLE CASCADE
==================================================

Current reported problems:
- Brand may appear but Model does not populate correctly
- Specific Vehicle may not populate correctly
- validation can behave inconsistently
- booking submit fails with HTTP 400 Bad Request: Invalid request parameters

Audit the full New Booking chain:

1. UI selects
2. react-hook-form state (or equivalent form state)
3. payload mapping
4. request payload sent to API
5. API validation schema
6. backend booking creation service

Required correct behavior:
- Brand selection works as a UI helper
- Model select always loads only models for selected brand
- selecting Brand resets Model and Specific Vehicle
- selecting Model resets Specific Vehicle
- Specific Vehicle remains optional if current business logic supports assign-later behavior
- form must always maintain a valid modelId when a model is selected
- submit payload must match the exact API schema
- valid completed form must create a booking successfully
- no false HTTP 400 error on valid input

Focus on likely root causes:
- wrong option value types
- id vs label mismatches
- stale modelId not reaching payload
- specific vehicle sending wrong value type
- brand changes silently invalidating model state
- API schema expecting a field that frontend no longer maps correctly
- date/time composition issues
- select visually updates but does not sync to form state

Do NOT weaken backend validation unless the schema itself is truly incorrect.
Prefer fixing the frontend/backend mapping mismatch properly.

==================================================
BUG FIX BLOCK B — TBS AIR PARKING VEHICLE ASSIGNMENT MODAL
==================================================

Current reported problems:
- staff cannot properly choose a real vehicle to assign
- modal may show only placeholder values like "Any brand"
- model and vehicle options do not load correctly
- user cannot clearly see which actual vehicle is being assigned

Required correct behavior:
- Brand select must load real brands from current fleet data
- Model select must load real models filtered by selected brand
- Vehicle select must load real vehicles filtered by selected model
- Vehicle labels must be clear and readable, for example:
  Toyota Corolla — GG-001-AA
  BMW X5 — GG-005-EE
- if there are no matching vehicles, show a meaningful empty state
- modal must never appear broken with placeholders only and no real choices
- assign action must send the correct vehicle_id

Also verify:
- already parked vehicles are excluded correctly
- invalid duplicate assignment is blocked correctly
- capacity logic still works
- no unrelated vehicles appear in the final selector

Important:
Use the same consistent relation chain:
Brand -> Vehicle Model -> Vehicle

Do not mix:
- brand names in one place and brand ids in another
- model names in one place and model ids in another
- labels as values

==================================================
BUG FIX BLOCK C — PICK UP / DROP OFF MODAL STATE STABILITY
==================================================

Current reported problems:
- typing into date/time, mileage, fuel, or similar fields causes the form to reset or behave like it refreshes
- one typed value can disappear or be overwritten
- modal form is unstable while editing

Find the real root cause and fix it.

Audit for issues such as:
- modal or inner form component remounting on every render
- changing component key
- defaultValues recreated on each render
- form reset() called too often
- parent rerender wiping inner state
- controlled/uncontrolled input mismatch
- numeric/date coercion overwriting partially typed values
- query invalidation or effect dependency loop resetting form state

Required correct behavior:
- date/time fields remain stable while editing
- mileage input remains stable while typing
- fuel level input remains stable while typing
- form preserves local state until submit/cancel
- modal does not remount/reinitialize on every keystroke
- save flow still works and updates status/history correctly

If there is an inner component defined inside BookingDetail or a similar parent that causes remounting, refactor only as much as needed to stabilize the form.

==================================================
BUG FIX BLOCK D — SHARED DATA / SELECT CONSISTENCY
==================================================

Perform a focused consistency pass across all affected components:

Check:
- query hook response shapes
- select option creation
- option value types
- id/name mismatches
- null/undefined handling
- parent-child select reset logic
- validation schema expectations
- payload builder correctness

If multiple components are now implementing Brand -> Model -> Vehicle logic differently and inconsistently, lightly refactor into a shared helper approach ONLY if that reduces bugs without broad rewrite.

Do NOT over-engineer.
Do NOT build a new abstraction layer unless truly helpful.

==================================================
EXPECTED RESULTS
==================================================

After the fix:

1. New Booking
- Brand -> Model -> Specific Vehicle works correctly
- valid form submits successfully
- no HTTP 400 on correct input
- no false "Please select a vehicle model" type issues

2. TBS AIR PARKING
- Enter Car in Parking modal shows real brands, models, and vehicles
- staff can clearly choose the actual vehicle
- assignment works correctly

3. Pick Up / Drop Off
- typing into date/time, mileage, and fuel fields no longer resets the form
- modal remains stable until submit/cancel
- save still works correctly

4. Existing recent features remain intact
- parking widget on dashboard still works
- fleet status click-through still works
- parking auto-removal on DELIVERED still works

==================================================
DEBUGGING REQUIREMENT
==================================================

Before changing code:
- inspect the exact request payload for New Booking
- inspect the exact API validation schema for booking creation
- identify the exact invalid/missing parameter causing HTTP 400
- identify the actual root cause of the Pick Up / Drop Off modal reset
- identify the actual root cause of broken parking vehicle selection

Do not guess.
Do not blindly rewrite.

==================================================
OUTPUT FORMAT
==================================================

When finished:
1. explain the exact root causes found
2. summarize exactly what was fixed
3. list files changed
4. confirm the project builds successfully
5. confirm these three flows were manually verified:
   - new booking creation
   - parking vehicle assignment
   - pickup/dropoff modal typing stability