Tbilisicars CRM&WEB CONTINUE

Important context:
- The project is already in a good and working state
- Previous Fleet CRM UI changes have already been made in this same project context
- Do NOT broadly refactor
- Do NOT touch website/public pages
- Do NOT touch auth
- Do NOT touch upload/storage logic
- Do NOT break existing create/edit/delete flows
- Keep edits minimal, localized, and safe

Current focus:
CRM -> Fleet page only

Very important observed behavior:
- In Fleet Calendar / Fleet Timeline, all uploaded vehicles are visible
- In Fleet -> Vehicles tab, not all uploaded vehicles are visible
- This strongly suggests the vehicles exist correctly in the system, and the main issue is likely a Vehicles-tab-specific fetch/render/query limit (around 20 items), not missing vehicle records
- This may also explain why some linked vehicles appear in grouped model expansion and some do not

Issues to fix:

1) Vehicles tab currently shows only about 20 vehicles
Need to find the exact source of this limitation and fix it safely.
Possible sources to audit:
- backend query limit
- frontend query params
- table pagination defaults
- frontend state slicing
- grouped rendering limit
- API hook / request limit specific to Fleet Vehicles tab

2) In grouped model expansion / expandable model rows, some linked vehicles appear and some do not
This likely relates to the same data-limit issue.
Fix so each model/group correctly shows all linked vehicles that belong to it under the active filters, instead of only a partial subset.

3) Remove brand logo/emblem thumbnails from Fleet CRM display UI
We no longer want brand logo thumbnails visually cluttering Fleet CRM.
Remove them from the relevant Fleet vehicle/model display areas.
Do NOT remove image upload support — only stop rendering brand logos in Fleet CRM UI.

4) In the Models tab, show the uploaded MODEL IMAGE before the model name
Important:
- show MODEL image
- do NOT show BRAND logo/emblem
The Models tab should visually use the model image thumbnail, but Fleet vehicle listing UI should stay clean.

Goal:
- Fleet Vehicles tab must show all vehicles reliably, not silently stop around 20
- If the list is long, implement safe proper pagination or another consistent visible navigation pattern already aligned with current CRM architecture
- Grouped model expansion must correctly show all linked vehicles
- Fleet Calendar/Timeline behavior must remain unchanged
- Brand logos must be removed from Fleet CRM display areas
- Models tab should show model image thumbnail before the model name
- No regressions

Implementation requirements:
1. First perform a careful audit and print a short summary of:
   - which exact files/components control Fleet Vehicles tab fetching and rendering
   - which exact files/components control Fleet model grouped expansion rendering
   - whether the 20-item cap comes from frontend or backend
   - why Calendar/Timeline can see all vehicles while Vehicles tab cannot
   - where brand logo thumbnails are currently rendered in Fleet CRM
   - where model image is available and should be rendered in Models tab

2. Then implement the minimum safe fix.

Preferred behavior for issue #1:
- If there is already an established CRM table pagination pattern, reuse it
- Otherwise implement the safest proper pagination for Fleet Vehicles tab
- Pagination must be visible and intuitive
- It must work together with search and filters
- Do NOT leave the list silently capped at 20 with no way to reach the rest
- If a simple fetch limit increase is not enough or would create hidden UX issues, implement proper page navigation

Preferred behavior for issue #2:
- Grouped/expanded model rows should show all vehicles linked to that model under the current filters
- The top-level Vehicles tab limitation must not truncate grouped children unexpectedly

Preferred behavior for issue #3:
- Remove brand logo thumbnails from Fleet CRM display areas relevant to vehicle/model listing UI
- Keep the admin UI text-first and cleaner
- Do NOT affect website branding or public pages

Preferred behavior for issue #4:
- In Models tab, show a small clean model image thumbnail before the model name
- Use the uploaded model image, not brand logo
- Keep alignment clean in the dark CRM table design
- If no model image exists, use a subtle fallback placeholder
- Do NOT reintroduce brand logos there

Scope guidance:
- Focus on artifacts/crm/src/pages/Fleet.tsx and any directly related CRM fleet data hook/query/API files only if needed
- Avoid unrelated refactors
- Avoid schema/database changes
- Avoid public website changes
- Avoid changing Fleet Calendar/Timeline logic unless absolutely necessary (they currently help confirm the vehicles exist)

Deliverables:
1. Audit summary
2. Exact files changed
3. Exact source of the ~20-item limitation
4. Exact solution implemented for full vehicle visibility (pagination or equivalent)
5. Confirmation that grouped model expansion now reflects all linked vehicles correctly
6. Exact Fleet CRM UI locations where brand logos were removed
7. Exact Models tab behavior for model image thumbnail
8. Confirmation that Calendar/Timeline behavior was preserved
9. Confirmation that website/upload/auth/business logic were untouched unless one minimal data-query fix was truly required