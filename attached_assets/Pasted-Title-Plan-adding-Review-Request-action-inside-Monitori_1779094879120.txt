Title: Plan adding Review Request action inside Monitoring booking detail view

Use PLAN MODE only. Do not edit files yet.

Context:
The Manual Send Review Request feature has already been implemented in:
- Booking Detail
- Monitoring row/action level

Now we need to add the same Review Request action inside the Monitoring module's booking/order detail area as well.

User requirement:
In the Monitoring module, when staff opens or views a specific booking/order details area, there should also be a “Review request” action/link/button available there, not only in the Monitoring row action.

Important:
Do not change backend logic if the existing Monitoring endpoint and dialog can be reused.
Do not duplicate business logic.
Prefer reusing the existing Monitoring SendMailDialog / Review Request modal and POST /admin/monitoring/:bookingId/send-review-request if possible.

Files to inspect:
- artifacts/crm/src/pages/Monitoring.tsx
- any child/detail components used by Monitoring if present

Current known Review Request behavior:
- Available only for RETURNED bookings
- Destination checkboxes:
  [ ] Trustpilot
  [ ] Google Tbilisi
  [ ] Google Kutaisi
- At least one destination required
- Uses existing Monitoring endpoint:
  POST /admin/monitoring/:bookingId/send-review-request
- Trustpilot BCC is server-side only
- No Trustpilot BCC value in frontend

Goal:
Add a Review Request action inside Monitoring's booking detail/expanded detail view using the same modal and same mutation flow.

Requirements:
- Use the existing SendMailDialog / Review Request dialog if possible.
- Do not create a second duplicate dialog unless there is no safe way to reuse it.
- The action inside the detail view must follow the same eligibility:
  - enabled only for RETURNED bookings with customer email
  - disabled or hidden for non-RETURNED bookings
- The label should be consistent:
  Review request
  or Send Review Request if there is enough space
- If disabled, show a helpful tooltip/title such as:
  Only available for RETURNED bookings
- Do not expose TRUSTPILOT_BCC_EMAIL in frontend.
- Do not add any new review links beyond the existing destination logic.
- Do not change API/backend unless you find that it is absolutely required.

Hard protections:
- Do NOT touch website files.
- Do NOT touch API/backend unless absolutely necessary and explain first.
- Do NOT touch database/schema/migrations.
- Do NOT touch pricing, payment, booking creation, public booking, PDF, email templates, sitemap, robots, SEO, or Google Ads.
- Do NOT change existing Review Request backend behavior.
- Do NOT add automatic sending.
- Do NOT allow non-RETURNED bookings.
- Do NOT add review counts, widgets, scripts, or external assets.

Plan Mode output required:

A. Current Monitoring structure
- Does Monitoring have an expanded row, detail panel, modal, drawer, or linked booking detail area?
- Where exactly is the existing row-level Review Request action?
- Where would the detail-level Review Request action fit best?

B. Reuse plan
- Can the existing SendMailDialog state and mailRow state be reused?
- Can clicking from the detail view set the same mailRow and open the same dialog?
- Or is a small helper function needed?

C. Exact proposed UI placement
- Exact area/component inside Monitoring.tsx
- Button/link label
- Enabled/disabled conditions

D. Files likely to change
- Prefer Monitoring.tsx only
- Confirm if any other file is needed

E. Risk assessment
- What could break?
- How to keep it safe and minimal?

F. Build recommendation
Say either:
- Ready for Build
or
- Needs clarification before Build