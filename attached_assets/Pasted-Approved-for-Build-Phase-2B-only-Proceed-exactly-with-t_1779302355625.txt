Approved for Build — Phase 2B only.

Proceed exactly with the approved Phase 2B plan.

Strict terminology:
- Extension Charge / Additional Amount Due is NOT a payment.
- Extra Days Payment IS a payment and is already implemented from Phase 2A.
- Do not confuse these two concepts.

Strict scope:
- Implement only Extension Charge / Additional Amount Due flow when staff changes dropoff date/time in Booking Detail edit mode.
- Do NOT implement automatic price recalculation.
- Do NOT create booking_payment rows from extension charge.
- Do NOT create accounting_entries rows from extension charge.
- Do NOT count extension charge as paid.
- Do NOT change public website.
- Do NOT change public booking flow.
- Do NOT change pricing engine.
- Do NOT implement Advance Payment / Pending Receivables.
- Do NOT implement Fixed Monthly Expenses.
- Do NOT change permissions or role guards.
- Do NOT deploy.
- Do NOT run production DB migrations.
- Do NOT perform Git Sync or Droplet build.

Allowed files:
1. lib/api-zod/src/generated/api.ts
2. lib/api-zod/src/generated/types/adminUpdateBookingBody.ts
3. artifacts/api-server/src/services/admin-bookings.service.ts
4. artifacts/api-server/src/routes/admin-bookings.ts
5. artifacts/crm/src/pages/BookingDetail.tsx

If a sixth file is not actually needed, do not create or edit it. Keep the implementation to the minimum required files.

Implementation rules:

1. API/Zod:
Add extensionChargeAmount to UpdateAdminBookingBody.
It must be optional and must accept only positive numbers.
Blank/zero/no value should be omitted by the CRM, not sent as 0.

2. CRM BookingDetail:
When staff enters Booking Overview edit mode, remember the original dropoff date/time.
Show Extension Charge field only when dropoff date or dropoff time differs from the original saved value.

Field label:
Extension Charge (Additional Amount Due)

Helper text:
Increases the booking total only — this is not a payment. Record the customer's payment separately using Extra Days Payment.

Currency:
Use booking.currency only. Do not add separate currency logic for extension charge.

PATCH behavior:
- If extensionChargeAmount > 0:
  - send extensionChargeAmount as a number
  - do NOT send totalAmount in the same PATCH request
- If extensionChargeAmount is blank/0:
  - do not send extensionChargeAmount
  - existing totalAmount update behavior remains unchanged

3. Backend:
Do not trust frontend-calculated totalAmount for extension charge.
If extensionChargeAmount > 0:
- Strip extensionChargeAmount out of normal updateData.
- Atomically increase booking.total_amount by extensionChargeAmount on the backend.
- Recalculate paymentStatus after the totalAmount increase using existing payment summary/status logic.
- Insert a booking_history row with:
  - actionType: EXTENSION_CHARGE
  - fieldName: total_amount
  - oldValue: old totalAmount
  - newValue: new totalAmount
  - description clearly mentioning extension charge amount, currency, and new dropoff date/time if available
  - changedById from the current admin/session if available

Do not create payment/accounting rows.

4. Route audit:
Enhance the existing booking PATCH audit log afterData only when extensionChargeAmount is present:
- extensionChargeAmount
- newTotalAmount if available

Do not redesign the audit system.

5. Payment Summary behavior:
After extension charge:
- booking.totalAmount increases
- totalPaid stays unchanged
- remainingBalance increases
- paymentStatus recalculates correctly
- Later, when staff adds Extra Days Payment, totalPaid increases and remainingBalance returns to zero

6. Do not touch:
- admin-booking-payments.service.ts, unless import-only usage is absolutely necessary and no logic is changed there
- admin-accounting.service.ts
- DB schema/migrations
- public website files
- pricing/public booking files
- permissions

After implementation:
1. Show exact changed files.
2. Confirm no DB migration was created.
3. Run relevant API and CRM typechecks/build checks if available.
4. Confirm no public website files changed.
5. Confirm no pricing/public booking flow changed.
6. Confirm no booking_payment row creation from extension charge.
7. Confirm no accounting_entries row creation from extension charge.
8. Confirm paymentStatus recalculates after extension charge.
9. Confirm Phase 2A Extra Days Payment remains unchanged.
10. Confirm Phase 3 Deposit metadata remains unchanged.
11. Commit with message:
Add extension charge flow for booking extensions