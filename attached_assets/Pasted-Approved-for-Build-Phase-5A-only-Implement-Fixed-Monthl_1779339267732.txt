Approved for Build — Phase 5A only.

Implement Fixed Monthly Expenses backend + migration only.

Do NOT implement the CRM UI yet.
Do NOT create FixedExpenses.tsx yet.
Do NOT modify Accounting.tsx for tabs yet.
Phase 5B will handle the UI separately after Phase 5A is verified.

Strict scope:
- Fixed Expense Templates backend/data model only.
- Template itself is NOT an accounting expense.
- Accounting expense is created only when staff manually posts a template for a specific month.
- No automatic cron/monthly posting.
- No booking_payment rows.
- No booking/payment logic changes.
- No public website changes.
- No pricing/public booking flow changes.
- No Georgia Bank / online payment.
- No permissions redesign.
- No production migration/deploy.
- No Git Sync or Droplet build.
- Do not touch unrelated SEO/noindex/sitemap tasks.

Allowed files for Phase 5A:
1. lib/db/migrations/0011_fixed_expense_templates.sql
2. lib/db/src/schema/accounting.ts
3. artifacts/api-server/src/services/admin-accounting.service.ts
4. artifacts/api-server/src/routes/admin-accounting.ts

Important correction:
The two backlink columns must be added to accounting_entries only:
- fixed_expense_template_id
- fixed_expense_month

Do NOT add these columns to booking_payment.
Do NOT modify booking_payment table in Phase 5A.

DB migration:
Create:
lib/db/migrations/0011_fixed_expense_templates.sql

Use additive/safe SQL:

CREATE TABLE IF NOT EXISTS fixed_expense_templates (
  id              serial        PRIMARY KEY,
  name            varchar(150)  NOT NULL,
  category        varchar(100)  NOT NULL,
  amount          numeric(12,2) NOT NULL,
  currency        accounting_currency_enum NOT NULL DEFAULT 'GEL',
  due_day         smallint      NOT NULL DEFAULT 1
                  CONSTRAINT chk_due_day CHECK (due_day BETWEEN 1 AND 28),
  is_active       boolean       NOT NULL DEFAULT true,
  notes           text,
  created_by_id   integer,
  created_at      timestamp     NOT NULL DEFAULT now(),
  updated_at      timestamp     NOT NULL DEFAULT now()
);

ALTER TABLE accounting_entries
  ADD COLUMN IF NOT EXISTS fixed_expense_template_id integer DEFAULT NULL
    REFERENCES fixed_expense_templates(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS fixed_expense_month varchar(7) DEFAULT NULL;

CREATE UNIQUE INDEX IF NOT EXISTS uq_fixed_expense_post
  ON accounting_entries(fixed_expense_template_id, fixed_expense_month)
  WHERE fixed_expense_template_id IS NOT NULL
    AND fixed_expense_month IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_fixed_expense_templates_is_active
  ON fixed_expense_templates(is_active);

CREATE INDEX IF NOT EXISTS idx_accounting_entries_template_id
  ON accounting_entries(fixed_expense_template_id)
  WHERE fixed_expense_template_id IS NOT NULL;

Schema:
In lib/db/src/schema/accounting.ts:
- Add fixedExpenseTemplatesTable.
- Add fixedExpenseTemplateId and fixedExpenseMonth to accountingEntriesTable only.
- Do not modify bookingPaymentTable.

Backend service:
Extend artifacts/api-server/src/services/admin-accounting.service.ts.

Add functions:
1. listFixedExpenseTemplates(activeOnly?: boolean)
2. createFixedExpenseTemplate(input)
3. updateFixedExpenseTemplate(id, input)
4. deleteFixedExpenseTemplate(id)
5. postFixedExpenseForMonth(templateId, month, adminId?)

Rules:
- Creating/updating/deleting templates must not create accounting_entries.
- postFixedExpenseForMonth creates exactly one accounting_entries EXPENSE row.
- postFixedExpenseForMonth must block duplicate posting for same templateId + YYYY-MM month.
- Use both application-level duplicate check and DB unique index.
- Use existing getExchangeRate / convertToGel pattern.
- entryDate should use the selected month and template due_day, clamped safely if needed.
- notes should clearly mention fixed expense template and month.
- deactivating template should not remove historical accounting entries.
- If deleting template with existing posts is unsafe, return 422 or require deactivate instead.
- Do not change existing accounting entry CRUD behavior.

Backend routes:
Extend artifacts/api-server/src/routes/admin-accounting.ts.

Add routes:
GET /admin/accounting/fixed-expenses
POST /admin/accounting/fixed-expenses
PATCH /admin/accounting/fixed-expenses/:id
DELETE /admin/accounting/fixed-expenses/:id
POST /admin/accounting/fixed-expenses/:id/post

Use existing accounting module permission pattern:
- view/list: canViewAccounting
- create/update/delete/post: canManageAccounting

This is not a permissions redesign; just reuse existing accounting route guards.

Audit logging:
Use existing logAudit pattern.
Log:
- fixed_expense_template.created
- fixed_expense_template.updated
- fixed_expense_template.deactivated or deleted
- fixed_expense_template.posted
- accounting_entry.created for the posted accounting entry if created through post route

actorId from req.session.adminId ?? null.

Validation:
- category must be an expense category if simple to validate safely.
- amount must be positive.
- currency must be GEL/USD/EUR.
- dueDay must be 1–28.
- month must match YYYY-MM.
- active template required for posting.

Out of scope:
- No CRM UI.
- No FixedExpenses.tsx.
- No Accounting.tsx tab.
- No automatic cron.
- No tax reporting.
- No supplier/vendor module.
- No receipt upload.
- No public website.
- No deployment.

After implementation:
1. Show exact changed files.
2. Show migration file content.
3. Confirm fixedExpenseTemplateId/fixedExpenseMonth were added to accounting_entries only.
4. Confirm booking_payment table was not modified.
5. Confirm template create/update/delete does not create accounting_entries.
6. Confirm post template creates exactly one accounting_entries EXPENSE row.
7. Confirm duplicate month posting is blocked.
8. Confirm existing Accounting CRUD still works.
9. Confirm no public website files changed.
10. Confirm no booking/payment logic changed.
11. Confirm no production migration/deploy/Git Sync happened.
12. Run relevant API typecheck/build checks if available.
13. Commit with message:
Add fixed expense templates backend