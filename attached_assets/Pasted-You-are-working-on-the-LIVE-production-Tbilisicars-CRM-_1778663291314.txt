You are working on the LIVE production Tbilisicars CRM&WEB monorepo.

IMPORTANT:
This is PLAN MODE ONLY.
Do NOT edit files.
Do NOT implement anything yet.

Context:
The SearchButton animation is already integrated and working correctly.
Current state:
- SearchButton.tsx exists
- Home.tsx uses <SearchButton onValidate={validateSearch} onSearch={navigateToBooking} />
- Validation runs before animation
- Invalid form shows errors with no animation
- Valid form animates the arrow and then navigates
- Mobile works
- Build is clean

However, the current animation is too simple. It only moves the arrow.
We want to explore a premium visual polish layer for the existing SearchButton animation.

Creative goal:
When the user clicks “Search Vehicles” with a valid form:
- The arrow should feel like it launches the trip.
- Add a subtle premium travel/airplane cue.
- The effect should feel like:
  arrow → motion trail / travel line → sleek airplane or jet-like launch cue
- It must look premium, elegant, minimal, and travel-tech.
- It must NOT look cartoonish, childish, cheap, casino-like, or over-animated.
- It must remain fast: around 0.6–0.9 seconds max.
- It must work well on desktop and mobile.
- It must not change the button size, page layout, validation, or navigation behavior.

Critical restrictions:
- Do NOT touch CRM.
- Do NOT touch backend/API.
- Do NOT touch booking logic.
- Do NOT touch pricing/payment logic.
- Do NOT touch email/PDF logic.
- Do NOT change routes.
- Do NOT change SEO metadata.
- Do NOT change homepage layout.
- Do NOT change form validation logic.
- Do NOT add new npm packages.
- Do NOT add video, Lottie, large images, or heavy assets.
- Do NOT make the animation fullscreen.
- Do NOT animate the button text.
- Keep this scoped to the existing SearchButton component unless there is a strong reason otherwise.

Please inspect:
1. Current SearchButton.tsx implementation
2. Current Home.tsx integration
3. Available icon libraries already used in the project
4. Current CSS/Tailwind/Framer Motion patterns

Analyze and propose:

1. Best visual approach
Compare:
- extending the existing ChevronRight animation only
- adding a subtle motion trail element
- adding a small inline SVG airplane/jet silhouette
- using Lucide Plane icon if available
- using CSS-only pseudo-elements
- using Framer Motion second animated element

2. Recommended implementation
Explain exactly which approach is safest and most premium.

3. Visual behavior
Describe:
- before click
- after valid click
- arrow movement
- trail behavior
- airplane/jet cue behavior
- how it exits
- when navigation fires
- what happens on mobile
- what happens with reduced-motion enabled

4. Design constraints
Define what must be avoided:
- no cartoon plane
- no emoji
- no bouncing
- no flashing
- no large object crossing the whole screen
- no layout shift
- no horizontal scroll
- no slow animation

5. Technical constraints
Explain:
- whether SearchButton.tsx alone is enough
- whether Home.tsx needs any changes
- whether onValidate/onSearch behavior remains unchanged
- whether timing/fallback logic remains unchanged
- whether TypeScript risk exists

6. Accessibility and mobile
Evaluate:
- prefers-reduced-motion
- keyboard click
- touch tap
- small screen layout
- low-end Android performance
- Safari/iOS behavior

7. Exact files likely affected
List expected files only.
Do not edit them.

8. Risk analysis
Include:
- visual quality risk
- mobile overflow risk
- animation delay risk
- booking flow risk
- regression risk

9. Done looks like
Describe the final expected premium result.

10. Final recommendation
At the end, state clearly:
- SAFE TO BUILD
or
- NEEDS REVISION

11. Future build prompt
Write the smallest safe implementation prompt for this visual polish only.
It must:
- modify only SearchButton.tsx if possible
- preserve validation-before-animation
- preserve onSearch timing/fallback
- preserve reduced-motion behavior
- not change Home.tsx unless absolutely necessary
- not add dependencies
- not touch any other project area

Again:
PLAN ONLY.
Do NOT edit files.
Return only the analysis and recommended implementation plan.