We are continuing under the strict Tbilisicars CRM business specification.

Current confirmed state:
- Fleet foundation works
- Locations foundation works
- Bookings creation/lifecycle/conflict logic works
- Vehicle status and location updates work
- Dashboard operational board works
- Customers works
- Extras works
- Rates works
- Promotions works
- Service / Maintenance works and is E2E verified
- Accounting works and is E2E verified
- Team Management works and is E2E verified
- Fleet Calendar / Timeline works and is E2E verified

Do NOT redesign working modules.
Do NOT touch unrelated modules.
Do NOT change booking logic.
Do NOT introduce permissions logic in this block.

Your task now is to implement ONLY the Reports / Exports module for Tbilisicars CRM operations.

==================================================
BLOCK 11 GOAL
==================================================

Create a practical Reports module that allows operations staff and management to view business statistics and export operational data.

This module must generate useful operational reports using existing CRM data.

Reports must be readable in the UI and exportable for analysis.

Do NOT build a complex analytics engine.
Do NOT build BI dashboards.
Keep the system operational and stable.

==================================================
A. MODULE PURPOSE
==================================================

The Reports module is used to analyze operational performance and financial activity.

It must support reporting for:

- bookings activity
- revenue and expenses
- fleet utilization
- service costs
- region performance

Reports must rely only on existing data structures already used in the CRM.

==================================================
B. REPORT TYPES
==================================================

Create the following report categories:

1. Booking Reports
2. Financial Reports
3. Fleet Utilization Reports
4. Service / Maintenance Reports
5. Region Activity Reports

Each category must produce a usable table of results.

==================================================
C. BOOKING REPORT
==================================================

Use booking data already stored in the system.

Required fields in the report:

- booking id
- pickup date
- dropoff date
- vehicle (brand + model + plate)
- region
- booking status
- customer name if available
- price if available

Required filters:

- date range
- region
- booking status
- vehicle search

==================================================
D. FINANCIAL REPORT
==================================================

Use the Accounting module as the source.

Display:

- income entries
- expense entries
- currency
- GEL equivalent
- category
- date

Required filters:

- date range
- entry type (income / expense)
- category
- currency

Display summary totals:

- total income (GEL equivalent)
- total expenses (GEL equivalent)
- net profit (GEL)

==================================================
E. FLEET UTILIZATION REPORT
==================================================

Analyze how often vehicles are used.

Report fields:

- vehicle (brand + model + plate)
- total bookings count
- total days booked
- total days available in period
- utilization percentage

Filters:

- date range
- region
- vehicle search

Keep calculations simple and stable.

==================================================
F. SERVICE / MAINTENANCE REPORT
==================================================

Use Service module data.

Fields:

- vehicle
- service category
- mileage
- cost
- currency
- date
- vendor if available

Filters:

- date range
- service category
- vehicle search

Show summary totals:

- total service cost (GEL equivalent if possible)

==================================================
G. REGION ACTIVITY REPORT
==================================================

Analyze operational activity per region.

Regions already exist:

- Tbilisi
- Kutaisi
- Batumi

Fields:

- region
- bookings count
- active vehicles
- revenue if simple to derive
- service cost if simple to derive

Keep calculations straightforward.

==================================================
H. EXPORT FUNCTIONALITY
==================================================

Each report table must support exporting the result set.

Minimum required export:

- CSV export

Optional if simple:

- Excel (.xlsx)

Export must respect current filters applied to the report.

Do NOT export entire database tables blindly.
Export only filtered results.

==================================================
I. UI STRUCTURE
==================================================

Create a dedicated Reports page.

Recommended structure:

Top section:
- report type selector

Second section:
- filters

Main section:
- report table

Actions:
- export button

Keep the layout simple and operational.

Do NOT redesign the dashboard or other modules.

==================================================
J. PERFORMANCE REQUIREMENT
==================================================

Reports must remain responsive.

Requirements:

- fetch only filtered data
- avoid extremely heavy full-table scans
- paginate if needed

Do not build a heavy analytics engine.

==================================================
K. REQUIRED VERIFICATION
==================================================

You must verify in the running UI:

1. Reports page loads
2. Booking report displays real data
3. Financial report displays accounting data
4. Fleet utilization report calculates correctly
5. Service report displays maintenance records
6. Region report displays activity
7. Filters work for each report
8. Export CSV works and downloads file
9. Export respects filters

Use realistic QA data only if needed.

==================================================
L. FINAL REPORT FORMAT
==================================================

At the end report only in this structure:

1. Reports module result
2. Booking report result
3. Financial report result
4. Fleet utilization result
5. Service report result
6. Region activity report result
7. Export functionality result
8. Verified UI flows
   - Reports page load: works / not
   - Filters: works / not
   - Booking report: works / not
   - Financial report: works / not
   - Fleet utilization: works / not
   - Service report: works / not
   - Region report: works / not
   - CSV export: works / not
9. Remaining gaps if any

Do not claim completion unless reports display real data and exports work.