Project context:
This is the live Tbilisicars CRM&WEB monorepo already fully built in Replit.
Do NOT refactor architecture.
Do NOT change accounting logic.
Do NOT auto-create payments.
Do NOT change booking creation flow, payment creation flow, or ledger source-of-truth.
We are only adding a controlled warning/indicator layer for delivered bookings that have no payment record.

Goal:
Add a unified red warning indicator with the exact text:

Record Payment

This should appear only when:

* booking status is DELIVERED
* the booking has zero payment history records

Important business rule:

* Payment History remains the source of truth for real financial records
* Booking payment status alone must NOT create accounting entries
* We are NOT creating automatic payments
* We are only surfacing missing-payment-record warnings

Scope:
Add this warning in exactly these places:

1. Booking Detail
2. Bookings List
3. Alerts module / alerts feed if such alert aggregation already exists in this codebase

Do not expand beyond this scope.

Implementation requirements:

1. First inspect existing booking detail data shape

* Find where booking detail page/modal loads:

  * booking status
  * payment summary
  * payment history / payments array
* Confirm what field contains payment records:

  * e.g. paymentHistory, payments, bookingPayments, or similar
* Reuse existing fetched data if already available
* Do NOT introduce unnecessary new endpoints if current detail payload already includes payment history

2. Add reusable warning condition
   Create a small derived boolean in the relevant UI layers, based on existing data:

hasPaymentRecordWarning =
booking.status === "DELIVERED" &&
paymentHistory.length === 0

Use exact DELIVERED status comparison consistent with the existing enum/constants in the codebase.
Do not guess status strings if a shared constant/type already exists.

3. Booking Detail UI
   In the booking detail modal/page:

* Show a red warning banner/card near the Payment Summary / Payment History area
* Exact main text:
  Record Payment
* Optional supporting text:
  This booking is marked as delivered but has no payment records.
* Use existing destructive/error UI styles already used elsewhere in the CRM
* If there is already an Alert, Banner, Callout, or destructive Card pattern in the project, reuse it
* Place it where staff will definitely see it before or near Payment History
* Show it only when the condition is true
* Do not affect any existing payment summary calculations

4. Bookings List UI
   In the main CRM Bookings list/table:

* Add a visible red badge/indicator on rows where the condition is true
* Exact text:
  Record Payment
* Prefer placing it in an existing status/info column if possible
* If no safe place exists, add it as a compact red badge near the booking status or amount area
* Keep layout stable and mobile-safe
* Do not clutter rows that do not meet the condition

Important:
If the bookings list currently does NOT include payment history counts or payment-record existence, inspect whether:

* it already receives enough fields to know payment entry existence, or
* there is already a derived field in API response, or
* a minimal safe backend addition is required

If a backend addition is required, keep it minimal:

* do NOT change accounting logic
* do NOT create payment rows
* only expose a boolean or count needed for UI warning state, such as:
  paymentRecordCount
  or
  hasPaymentRecords
  This must be derived from existing payment data only.

5. Alerts module
   If the project already has a central Alerts page/module or alert aggregation service:

* add a warning item/category for:
  Delivered bookings without payment record
* This alert should be based on the same condition
* Reuse existing alert card/list structure
* Keep wording short and operationally clear

Do NOT build a brand new alerting architecture if one does not already exist.
If Alerts module is not yet wired for this kind of operational item, document that clearly in your response and stop at Booking Detail + Bookings List.

6. Styling
   Use existing dark-theme CRM styles.
   Warning indicator must be clearly red/destructive.
   Text must be exactly:
   Record Payment

For alerts module label/title use exactly:
Delivered bookings without payment record

7. Safety rules

* No schema rewrite
* No accounting logic changes
* No payment auto-generation
* No booking status logic rewrite
* No silent data mutation
* No refactor outside touched files
* No breaking existing list performance
* No duplicate warning rendering in the same place

8. Output required from you
   Before coding, give me:
   A. exact files you plan to touch
   B. whether bookings list already has enough data or needs a minimal backend boolean/count
   C. whether Alerts module can support this with existing structure or not

Then implement only after that analysis.

Definition of done:

* Delivered booking with zero payment records shows red “Record Payment” warning in Booking Detail
* Same booking shows red “Record Payment” badge in Bookings list
* Alerts module includes “Delivered bookings without payment record” only if existing alerts structure supports it cleanly
* No automatic payment record is created
* No accounting behavior changes
* No regression in booking details, bookings list, or payment summary
