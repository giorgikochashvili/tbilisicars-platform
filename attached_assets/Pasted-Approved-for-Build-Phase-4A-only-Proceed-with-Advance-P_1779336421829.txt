Approved for Build — Phase 4A only.

Proceed with Advance Payment / Pending Receivables implementation, but apply the corrections below.

Strict terminology:
- ADVANCE_PAYMENT / Pending Receivable is NOT real received income yet.
- Pending advance must NOT count toward Total Paid.
- Pending advance must NOT create accounting_entries.
- Pending advance must NOT affect paymentStatus as paid.
- Mark as Received is the only moment when accounting income is created and paymentStatus/Total Paid update.

Strict scope:
- Implement Phase 4A only.
- Do NOT implement Phase 4B global Pending Receivables dashboard/report.
- Do NOT implement broker reconciliation.
- Do NOT implement Georgia Bank / online payment.
- Do NOT implement Fixed Monthly Expenses.
- Do NOT change permissions/role guards.
- Do NOT touch public website.
- Do NOT touch public booking flow or pricing engine.
- Do NOT deploy.
- Do NOT run production DB migrations.
- Do NOT perform Git Sync or Droplet build.
- Do NOT do or close the unrelated SEO/noindex/sitemap task.

Important correction #1 — route file:
The plan mentioned artifacts/api-server/src/routes/admin-bookings.ts for payment routes, but previous code inspection showed booking payment routes likely live in:
artifacts/api-server/src/routes/admin-booking-payments.ts

Before editing, confirm where POST /admin/bookings/:id/payments currently lives.
Implement Phase 4 route changes in the actual payment route file.
Do not move routes.
Do not duplicate routes.

Important correction #2 — Accounting category arrays:
Add "Advance Payment" to INCOME_CATEGORIES in both places:
1. artifacts/api-server/src/services/admin-accounting.service.ts
2. artifacts/crm/src/pages/Accounting.tsx

Backend and CRM category arrays must stay in sync.

Important correction #3 — SEO/noindex task:
Ignore the SEO/noindex/sitemap task entirely.
Do not edit website files.
Do not mark that task complete.
Do not write a no-op SEO commit.
This build is only Phase 4A Advance Payment / Pending Receivables.

Allowed files:
1. lib/db/migrations/0010_advance_payment_receivables.sql
2. lib/db/src/schema/accounting.ts
3. artifacts/api-server/src/services/admin-booking-payments.service.ts
4. The actual route file that contains POST /admin/bookings/:id/payments, likely:
   artifacts/api-server/src/routes/admin-booking-payments.ts
5. lib/api-zod/src/generated/api.ts
6. lib/api-zod/src/generated/types/adminBookingPayment.ts
7. artifacts/crm/src/pages/BookingDetail.tsx
8. artifacts/api-server/src/services/admin-accounting.service.ts
9. artifacts/crm/src/pages/Accounting.tsx

Do not edit website files.

DB migration:
Create:
lib/db/migrations/0010_advance_payment_receivables.sql

Use safe/idempotent SQL:
ALTER TYPE booking_payment_type_enum ADD VALUE IF NOT EXISTS 'ADVANCE_PAYMENT';

ALTER TABLE booking_payment
  ADD COLUMN IF NOT EXISTS advance_status varchar(10) DEFAULT NULL,
  ADD COLUMN IF NOT EXISTS received_at timestamp DEFAULT NULL,
  ADD COLUMN IF NOT EXISTS received_by_id integer DEFAULT NULL;

CREATE INDEX IF NOT EXISTS idx_booking_payment_advance_status
  ON booking_payment(advance_status)
  WHERE advance_status IS NOT NULL;

Schema:
Update bookingPaymentTypeEnum to include:
- ADVANCE_PAYMENT

Add to bookingPaymentTable:
- advanceStatus
- receivedAt
- receivedById

Backend service:
Update PaymentType union to include ADVANCE_PAYMENT.

PAYMENT_TYPE_ACCOUNTING:
- ADVANCE_PAYMENT must be null at creation time.
- It must not create accounting entry while pending.

getBookingPaymentSummary:
- Exclude rows where advanceStatus = PENDING.
- Include ADVANCE_PAYMENT in totalPaid only when advanceStatus is RECEIVED or normal summary filter allows it.
- Keep existing BOOKING_PAYMENT / ADJUSTMENT historical compatibility / ADDITIONAL_PAYMENT / EXTRA_DAYS_PAYMENT behavior.

addBookingPayment:
- Accept advanceStatus: "PENDING" only for ADVANCE_PAYMENT.
- If paymentType = ADVANCE_PAYMENT, create booking_payment row with advanceStatus = PENDING.
- Do not create accounting entry for pending advance.
- Do not count it toward Total Paid/paymentStatus.
- Add booking_history row actionType ADVANCE_PENDING if appropriate and existing history schema supports it.

Mark as Received:
Add service function receiveAdvancePayment(paymentId, bookingId, adminId):
- Fetch payment.
- Validate it belongs to booking.
- Validate paymentType = ADVANCE_PAYMENT.
- Validate advanceStatus = PENDING.
- If not PENDING, reject to prevent duplicate accounting entries.
- In one transaction:
  1. Create accounting_entries INCOME row with category "Advance Payment".
  2. Update booking_payment:
     - advanceStatus = RECEIVED
     - accountingEntryId = created accounting entry id
     - receivedAt = now
     - receivedById = adminId
  3. Recalculate payment summary.
  4. Update paymentStatus.
  5. Insert booking_history row actionType ADVANCE_RECEIVED if appropriate.
- Return updated payment and summary.

Delete behavior:
- Existing deleteBookingPayment behavior should remain.
- Pending advance has null accountingEntryId, so delete removes only booking_payment.
- Received advance has accountingEntryId, so delete removes linked accounting entry and recalculates paymentStatus.
- Do not break existing delete behavior.

Routes:
In the actual payment route file:
- Allow new payment type ADVANCE_PAYMENT for creation.
- If paymentType is ADVANCE_PAYMENT, require/set advanceStatus = PENDING.
- Add POST route for Mark as Received, for example:
  POST /admin/bookings/:bookingId/payments/:paymentId/receive
- Add audit logging for Mark as Received.
- Keep existing add/delete payment audit logging.

API/Zod/types:
- Add ADVANCE_PAYMENT to add payment body validation.
- Add advanceStatus optional field where needed.
- Add advanceStatus, receivedAt, receivedById to AdminBookingPayment response type.
- Add receive response schema if needed.
- Rebuild api-zod only if typecheck requires it, but do not commit ignored dist files unless already tracked and necessary.

CRM BookingDetail:
Payment Type dropdown should include:
- Booking Payment
- Additional Payment
- Extra Days Payment
- Advance Payment / Pending Receivable

When Payment Type = ADVANCE_PAYMENT:
- Show clear warning:
  “This records a pending receivable — not received income yet. It will not count toward Total Paid until marked as received.”
- On add, send advanceStatus = PENDING.
- Show PENDING badge on pending advance rows.
- Show RECEIVED badge/date on received advance rows.
- Add Mark as Received button only for pending advance rows.
- On Mark as Received, show confirmation:
  “Mark this advance payment as received? This will create an accounting income entry.”
- On success, refresh payment history and summary.

Accounting categories:
Add "Advance Payment" to INCOME_CATEGORIES in:
- artifacts/api-server/src/services/admin-accounting.service.ts
- artifacts/crm/src/pages/Accounting.tsx

Do not implement a global Pending Receivables dashboard in Phase 4A.

After implementation:
1. Show exact changed files.
2. Show migration file content.
3. Confirm no website files changed.
4. Confirm no pricing/public booking flow changed.
5. Confirm no permissions changed.
6. Confirm no production migration/deploy/Git Sync happened.
7. Confirm pending advance does not create accounting entry.
8. Confirm pending advance does not count toward Total Paid.
9. Confirm Mark as Received creates exactly one accounting income entry.
10. Confirm Mark as Received recalculates paymentStatus.
11. Confirm duplicate Mark as Received is rejected.
12. Confirm delete behavior for pending and received advance.
13. Confirm Phase 2A Additional/Extra Days payments still work.
14. Confirm Phase 2B Extension Charge remains untouched.
15. Confirm Phase 3 Deposit metadata remains untouched.
16. Run relevant API and CRM typechecks/build checks if available.
17. Commit with message:
Add advance payment pending receivables flow