Tbilisicars CRM&WEB — STEP 1 HEADER/STEPPER POLISH (SURGICAL UI REFINEMENT ONLY)

Continue from the current working state.

IMPORTANT:
- Do NOT change backend logic
- Do NOT change booking logic
- Do NOT touch filters logic
- Do NOT touch pricing logic
- Do NOT touch selection logic
- Do NOT re-architect Step 1
- Do NOT modify unrelated modules
- Surgical layout refinement only

CURRENT STATE:
- Step 1 already has a separate full-width stepper container
- Left sidebar and right vehicle results layout already exist
- The page still has a separate heading block above the stepper:
  - "Book Your Car"
  - "Complete the steps below to submit your reservation request."

TARGET CHANGE:
Move the page heading content INTO the same full-width stepper container, so it becomes one unified header section.

DESIRED STRUCTURE:
Inside the existing top full-width header/stepper card:
1. Top area:
   - Title: "Book Your Car"
   - Subtitle: "Complete the steps below to submit your reservation request."
2. Bottom area:
   - Existing step progress row (1 / 2 / 3 / 4 / 5 / 6)

So the current standalone heading block above the stepper should be removed, and its content should be placed inside the stepper card itself.

LAYOUT REQUIREMENTS:
- Keep the current dark premium style
- Keep spacing balanced and more compact than before
- Title and subtitle should be centered horizontally within the header card
- Stepper row remains below the text content
- Preserve responsive behavior
- Do not make the header card excessively tall
- Keep it elegant and compact

VISUAL GOAL:
Instead of:
[ Book Your Car ]
[ subtitle ]
[ separate stepper card ]

Make it:
[ unified card:
    Book Your Car
    subtitle
    1-2-3-4-5-6 stepper
]

IMPLEMENTATION NOTES:
- Reuse the existing stepper container
- Move the existing heading JSX into that container
- Remove duplicate outer heading section
- Adjust padding/gap only as needed for a polished result
- Do not change step labels or step logic

LIKELY FILE:
- artifacts/website/src/pages/Booking.tsx

DELIVERABLE:
Return:
1. Exact file changed
2. Short summary of layout change
3. Confirmation that:
   - heading is now inside the stepper card
   - standalone heading block above it was removed
   - responsive layout remains intact

MANUAL TEST:
- Open booking page Step 1
- Confirm there is no separate large heading block above
- Confirm the title + subtitle are inside the same card as the stepper
- Confirm spacing is cleaner and more compact
- Confirm no regression in sidebar / filters / vehicle list