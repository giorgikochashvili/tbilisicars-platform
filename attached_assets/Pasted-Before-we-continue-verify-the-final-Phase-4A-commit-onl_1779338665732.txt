Before we continue, verify the final Phase 4A commit only.

Do not make any new changes.
Do not edit files.
Do not run new tasks.
Do not touch SEO/noindex/sitemap.

Please run and report:

1. git status --short
2. git show --name-only --oneline --stat HEAD
3. git diff --stat HEAD~1..HEAD
4. git show --name-status HEAD

I need exact confirmation:

A. Commit identity
- Commit hash
- Commit message
- Working tree clean or not

B. Changed files
List every changed file in the final Phase 4A commit.

Specifically confirm:
- Whether any website files changed.
- Whether Booking.tsx website page or sitemap.xml changed.
- Whether any attached_assets files were committed.
- Whether lib/db/migrations/0010_advance_payment_receivables.sql was created.
- Whether lib/db/dist files were committed or only rebuilt locally.
- Whether lib/api-zod/dist files were committed or only rebuilt locally.

C. Migration confirmation
Show exact content of:
lib/db/migrations/0010_advance_payment_receivables.sql

Confirm:
- 0010 migration was applied only to local/dev Replit database if it was run.
- Production database was not touched.
- No Droplet deploy happened.
- No Git Sync happened.

D. Route/file confirmation
Confirm from final diff:
- The actual payment route file was updated:
  artifacts/api-server/src/routes/admin-booking-payments.ts
- No duplicate payment route was added elsewhere.
- admin-bookings.ts was not incorrectly used for payment routes unless only imports/types required it.

E. Phase 4A logic confirmation
Confirm from final diff:
- ADVANCE_PAYMENT added to booking_payment_type_enum.
- advanceStatus / receivedAt / receivedById added to bookingPaymentTable schema.
- Pending ADVANCE_PAYMENT does not create accounting_entries.
- Pending ADVANCE_PAYMENT is excluded from getBookingPaymentSummary / Total Paid.
- Pending ADVANCE_PAYMENT does not reduce Remaining Balance.
- Mark as Received creates exactly one accounting income entry.
- Mark as Received sets advanceStatus = RECEIVED.
- Mark as Received sets accountingEntryId.
- Mark as Received sets receivedAt and receivedById.
- Mark as Received recalculates paymentStatus.
- Duplicate Mark as Received is rejected.
- deleteBookingPayment behavior for pending and received advance remains safe.
- Existing BOOKING_PAYMENT still works.
- Existing ADDITIONAL_PAYMENT still works.
- Existing EXTRA_DAYS_PAYMENT still works.
- Phase 2B Extension Charge is untouched.
- Phase 3 Deposit metadata is untouched.

F. Accounting categories
Confirm:
- "Advance Payment" was added to backend INCOME_CATEGORIES.
- "Advance Payment" was added to CRM Accounting.tsx INCOME_CATEGORIES.
- Backend and CRM category arrays are still in sync.

G. CRM confirmation
Confirm:
- BookingDetail payment dropdown includes Advance Payment / Pending Receivable.
- Warning banner appears when ADVANCE_PAYMENT is selected.
- handleAddPayment sends advanceStatus = PENDING for ADVANCE_PAYMENT.
- Payment History shows PENDING / RECEIVED badge.
- Mark as Received button appears only on pending advance rows.
- Mark as Received confirmation text is present.
- After success, payment history/summary refresh.

H. Typecheck
Confirm:
- API baseline remained 14 pre-existing errors only.
- CRM baseline remained 13 pre-existing errors only.
- No errors in changed files.

Verification only.