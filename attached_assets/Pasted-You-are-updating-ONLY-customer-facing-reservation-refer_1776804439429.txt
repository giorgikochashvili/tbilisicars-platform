You are updating ONLY customer-facing reservation reference presentation, booking email layout/copy, and voucher presentation.

STRICT RULES:
- Do NOT change booking creation logic
- Do NOT change core CRM workflows
- Do NOT change pricing calculation logic
- Do NOT change API contracts unless absolutely required for pure presentation formatting
- Do NOT rename unrelated functions or refactor architecture
- Keep all changes surgical and presentation-focused
- No breaking changes

GOAL:
Make reservation references, booking email, and voucher look cleaner, more professional, and more customer-friendly.

CONTEXT:
The system currently shows a customer-facing booking code like:
TC-00316

The desired customer-facing format is:
#316

This shorter format should be used consistently anywhere customers see the reservation code.

ALSO:
The booking confirmation email currently feels visually too narrow / cramped and exposes some ugly pricing/metadata presentation.
The attached voucher should also follow the cleaned-up presentation rules already established in the booking page.

CHECK THESE FILES FIRST:
- artifacts/api-server/src/services/email.service.ts
- artifacts/api-server/src/services/pdf-voucher.service.ts
- artifacts/crm/src/pages/BookingDocument.tsx
- artifacts/crm/src/pages/HandoverDocument.tsx
- artifacts/crm/src/pages/PaymentDocument.tsx
- any helper used specifically for customer booking email or voucher generation

IMPORTANT:
Only change files that are actually responsible for customer-facing booking confirmation email / voucher / reservation reference presentation.
Do NOT change unrelated CRM admin documents unless the same customer-facing formatting is reused there.

--------------------------------------------------
1. CUSTOMER-FACING RESERVATION CODE FORMAT
--------------------------------------------------

Find where customer-facing reservation/booking reference is formatted as something like:
TC-00316

Change ONLY customer-facing presentation so it displays as:
#316

Rules:
- Use the real system booking number/reference value
- Do NOT invent a new ID
- Do NOT break internal storage
- Do NOT change database values
- This is presentation formatting only

Apply consistently in:
- booking success/confirmation page if covered by current reusable formatting path
- booking confirmation email subject/body
- attached booking voucher PDF
- any customer-facing booking document where the current TC-00... format is shown

If some internal/admin-only CRM screen still requires the old format internally, leave it unchanged unless it is directly customer-facing.

--------------------------------------------------
2. EMAIL LAYOUT POLISH
--------------------------------------------------

Improve the customer booking confirmation email presentation so it feels:
- wider / more balanced
- less cramped
- more polished
- easier to scan

Keep the brand/design language intact.
Do NOT redesign from scratch.
Do NOT create a new architecture for templates.

Required improvements:
- cleaner spacing
- better width usage
- better visual grouping of booking details
- cleaner price summary presentation
- avoid ugly narrow compressed blocks
- preserve responsive email safety

--------------------------------------------------
3. EMAIL CONTENT CLEANUP
--------------------------------------------------

The email must follow the same customer-facing wording rules already established for the booking page:

A)
Never show internal discount/campaign names.

B)
If discount is shown, keep it customer-friendly:
- base rental
- discount
- final rental total

C)
Use "pick-up" wording consistently in customer-facing copy.

D)
Use the cleaned insurance wording:
- "Full Insurance"
- no public-facing "excess" wording if currently exposed
- deposit wording aligned with booking page copy where appropriate

E)
Keep payment wording simple and customer-facing.

--------------------------------------------------
4. VOUCHER POLISH
--------------------------------------------------

Update the attached booking voucher so it feels cleaner and better aligned visually.

Required:
- customer-facing reservation code uses #316-style formatting
- booking status "Confirmed" should appear in green
- any pricing rows should follow the cleaned pricing presentation rules
- do not expose internal discount names
- use corrected customer-facing copy already established in booking page/email where relevant
- keep voucher professional and easy to read

Do NOT redesign the whole voucher engine.
Do only surgical presentation improvements.

--------------------------------------------------
5. REMOVE UGLY WEBSITE METADATA DUMP FROM CUSTOMER-FACING OUTPUT
--------------------------------------------------

Current issue:
A note-style block may expose raw website metadata such as:
[WEBSITE DATA] Nationality: Georgia Age: 31 WhatsApp: Yes ... Payment Method: Cash Insurance: Full

This is ugly and should not appear in customer-facing email/voucher output.

Required:
- Do NOT dump raw "[WEBSITE DATA] ..." strings into customer-facing note sections
- Cleanly omit these raw metadata strings from customer-facing presentation
- If some of the values are already presented elsewhere in structured form, keep them there
- Do NOT break internal storage in this task
- Presentation cleanup only

IMPORTANT:
This task is NOT asking for a full data-model migration.
It is only asking to stop exposing ugly raw metadata dumps in customer-facing output.

--------------------------------------------------
6. KEEP THIS TASK SAFE
--------------------------------------------------

Out of scope:
- CRM internal note storage redesign
- booking database schema changes
- booking creation logic changes
- promo logic changes
- deep refactor of email templating system
- unrelated document redesign

DONE LOOKS LIKE:
- customers see #316-style reservation code instead of TC-00316-style formatting
- booking confirmation email looks wider, cleaner, and more polished
- email no longer exposes internal discount names
- voucher shows Confirmed in green
- voucher/email wording matches booking-page wording improvements
- ugly raw [WEBSITE DATA] note dump is no longer exposed to customers
- no logic breaks
- no TypeScript/runtime errors

At the end, return:
1. exact files touched
2. what changed in each file
3. whether any customer-facing reference formatting remains intentionally unchanged anywhere