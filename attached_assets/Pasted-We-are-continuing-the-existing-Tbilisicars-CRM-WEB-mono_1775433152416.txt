We are continuing the existing Tbilisicars CRM&WEB monorepo.

Work ONLY on the Voucher Import flow. Do NOT redesign anything. Do NOT touch unrelated modules.

CURRENT CONFIRMED STATE
- AI voucher extraction is now working correctly
- The form auto-fills customer name, dates, locations, brand, model, amount, currency, external reservation code, etc.
- Current blocker is NOT AI
- Current blocker is that the import flow treats missing location reservation_code_prefix as a hard blocking error
- Example UI message:
  "The selected pickup location has no reservation code prefix. Configure a prefix in Locations settings before importing."
- Confirm Import stays disabled because of this
- In our business logic, prefix should NOT be mandatory for voucher import
- We want import to proceed even when the selected pickup/dropoff location has no prefix

GOAL
Make missing location reservation_code_prefix a NON-BLOCKING condition in Voucher Import.

REQUIRED BEHAVIOR
- If pickup/dropoff location has no prefix:
  - show only a soft warning or informational note if needed
  - DO NOT block the import
  - Confirm Import must remain enabled as long as the real required booking data is present
- Voucher Import must still create the booking successfully without reservation_code_prefix
- Do NOT require staff to edit Locations just to import a voucher
- Existing extracted data and manual editing flow must remain unchanged

STRICT RULES
- Do NOT redesign booking code architecture
- Do NOT modify unrelated CRM pages
- Do NOT modify AI extraction logic
- Do NOT change DB schema
- Do NOT introduce a large refactor
- Keep changes minimal and surgical

WHAT TO INSPECT
- artifacts/crm/src/pages/VoucherImportDialog.tsx
- artifacts/api-server/src/routes/admin-voucher-import.ts
- artifacts/api-server/src/services/admin-voucher-import.service.ts
- booking creation path used by voucher confirm/import

TASK
1. Find where missing reservation_code_prefix is currently treated as a blocking validation error
2. Change it so prefix is NOT part of the blocking conditions for enabling Confirm Import
3. If server-side confirm/import also blocks on missing prefix, remove that hard requirement there too
4. Preserve all truly required validations:
   - contactFullName
   - pickup/dropoff locations
   - pickup/dropoff datetime
   - brand/model if currently required by booking import
5. Missing prefix may produce only a non-blocking warning at most

DONE LOOKS LIKE
- Voucher Import auto-fills correctly
- Location may display "(no prefix)" but import is still allowed
- Confirm Import button becomes enabled when the actual required booking fields are present
- Booking is successfully created even if reservation_code_prefix is null/missing
- No unrelated regressions

RETURN FORMAT
Return a short exact report with:
1. where the hard block was
2. exact file(s) changed
3. minimal fix applied
4. why this is safe
5. how to test in CRM Preview