Task: Audit and stabilize booking confirmation email + PDF voucher chain

Context:
This is a live production-connected monorepo (Tbilisicars CRM & WEB), fully built in Replit. Website bookings are already working and correctly stored. Now we must verify and stabilize the confirmation email + PDF voucher flow to ensure correctness, consistency, and reliability.

CRITICAL RULES:
- Do NOT change architecture
- Do NOT refactor unrelated code
- Do NOT change pricing logic
- Do NOT introduce new services or providers
- Only inspect and fix issues if they exist
- Keep changes minimal and surgical

Goal:
Ensure that after a successful website booking:
1. Confirmation email is always sent
2. Sender is support@tbilisicars.com
3. PDF voucher is correctly generated and attached
4. All pricing values in email + PDF match backend calculations
5. No silent failures occur

Audit Scope:

1. Email sending flow
- Identify where the confirmation email is triggered (likely in public-bookings route or email.service)
- Confirm it uses RESEND (or current provider)
- Confirm sender is support@tbilisicars.com
- Confirm failure handling (logs vs silent fail)

2. PDF voucher generation
- Identify PDF generation logic and template
- Verify:
  - customer name
  - dates (pickup/dropoff)
  - locations
  - vehicle/model
  - extras
  - one-way fee
  - discount
  - total amount
- Ensure all values come from the same backend-calculated source

3. Pricing consistency
- Ensure email + PDF use the same:
  - extras calculation (with max_days cap)
  - discount (serverDiscountAmount)
  - one-way fee
- No duplicated or recomputed logic in templates

4. Attachment handling
- Confirm PDF is always attached to email
- Confirm correct file naming and format
- Confirm no race condition (email sent before PDF ready)

5. Failure safety
- If email fails:
  - confirm error is logged clearly
  - booking still succeeds
- Ensure no crash or blocked response

6. Generated password block
- Confirm:
  - generatedPassword is included only when present
  - appears correctly in email or confirmation page

Output format:
- Where email is triggered (file + line)
- Where PDF is generated
- Any inconsistencies found
- Exact minimal fixes (if needed)
- If everything is already correct, explicitly confirm "No fixes required"