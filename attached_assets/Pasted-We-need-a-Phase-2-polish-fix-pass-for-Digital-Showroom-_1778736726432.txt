We need a Phase 2 polish/fix pass for Digital Showroom.

IMPORTANT:
Do not proceed to Phase 3. Do not implement playlist fullscreen slideshow yet.
Do not modify backend, DB/schema, routes, permissions, booking, pricing, payment, website, upload logic, or fleet APIs.
Keep changes limited to artifacts/crm/src/pages/Showroom.tsx unless absolutely necessary.

Current issues to fix:

1. Vehicle Presentation Carousel is not visually working like a real carousel.
When clicking a vehicle card, the presentation viewer must feel like a premium carousel:
- very large vehicle image
- dark/premium Tbilisicars background
- clean left/right arrows
- previous/next must move through vehicles in the SAME category
- image should use the best available existing model image URL and display with object-contain, not small preview/card size
- avoid low-quality thumbnail styling
- no technical specs

2. Add a true fullscreen presentation mode for the opened vehicle/carousel.
This is NOT playlist slideshow.
Inside the vehicle presentation carousel, add a fullscreen/presentation button.
When activated:
- hide all admin UI, sidebar, tabs, buttons except minimal close/exit and navigation arrows
- show only the large vehicle image, premium background, and optional showroom text/price/badge
- make it suitable for showing directly to a customer on a tablet or screen
- Escape or close exits fullscreen presentation mode

3. Compare tray placement/design.
The compare tray must not appear on top of the vehicle image background in a way that ruins the presentation.
Make it a small premium floating tray in the lower corner or bottom edge:
- glass/dark style
- small thumbnails
- remove buttons
- View Compare button
- Clear button
- must not cover the main vehicle image too much

4. Compare mode should also have carousel behavior.
When vehicles are added to compare:
- user can open compare view
- compare view should allow viewing each compared vehicle individually in a large presentation carousel
- previous/next moves only through vehicles added to compare
- still allow a simple side-by-side overview if already implemented, but add the individual compare carousel/presentation experience
- no booking, no quote, no technical specs

5. Hide or simplify sort_order from the normal slide editor UI.
The current “sort order” field is confusing for staff.
If the field must stay in data, hide it from the normal UI or move it to an advanced/collapsed area.
Playlist item order is what matters for slideshow order.

6. Preserve working behavior:
- Vehicles tab still opens
- categories remain collapsible and all open by default
- Prices tab still works: USD save, EUR preview, exchange rate save
- Slides create/edit/save still works
- Playlist create/edit/add items still works
- Compare add/remove still works

7. Build/check:
- run CRM build/typecheck
- report exact files changed
- confirm no forbidden files/logic were touched
- do not proceed to Phase 3