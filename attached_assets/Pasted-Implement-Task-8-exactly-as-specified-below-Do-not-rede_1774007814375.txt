Implement Task #8 exactly as specified below. Do not redesign pages, do not touch website booking flow, do not add external FX APIs, do not change unrelated business logic.

GOAL
Fix CRM currency handling so that:
1) New CRM bookings support GEL / USD / EUR selection and store the chosen currency
2) Bookings list shows the correct symbol per booking
3) Dashboard revenue always returns a single GEL total using fixed conversion rules:
   - GEL => GEL
   - USD => amount * 2.7
   - EUR => amount * 2.9
4) Rate tier form becomes EUR-only in the UI and backend
5) Existing CRM-created bookings that were implicitly defaulted to USD are backfilled to GEL

REQUIRED CHANGES

1. DB DEFAULT + BACKFILL
File: lib/db/src/schema/bookings.ts
- Change booking currency default from 'USD' to 'GEL'
- Apply schema update with db push / force push as appropriate
- Then run a SQL backfill:
  UPDATE bookings
  SET currency = 'GEL'
  WHERE currency = 'USD';
This backfill is intentional for old implicit-default CRM bookings only.

2. DASHBOARD REVENUE IN GEL
File: artifacts/api-server/src/services/admin-dashboard.service.ts
- Replace simple sum(totalAmount) logic with currency-aware aggregation
- Add a small helper like toGelEquivalent(amount, currency)
- Convert before summing:
  GEL => amount
  USD => amount * 2.7
  EUR => amount * 2.9
- The returned totalRevenue must always represent GEL only

3. FORMATTERS
File: artifacts/crm/src/lib/utils.ts
- Update formatMoney to format GEL only, using the ₾ symbol
- Add a separate helper:
  formatBookingAmount(amount, currency)
  that returns:
  GEL => ₾
  USD => $
  EUR => €
- Do not keep any hardcoded dollar-sign behavior for generic money formatting

4. BOOKINGS LIST SYMBOLS
File: artifacts/crm/src/pages/Bookings.tsx
- Update the bookings list amount cell to use:
  formatBookingAmount(b.totalAmount, b.currency)
  instead of formatMoney(...)
- Ensure no hardcoded $ remains in booking amount rendering

5. NEW BOOKING FORM CURRENCY PICKER
File: artifacts/crm/src/pages/Bookings.tsx
- Add currency: "GEL" to EMPTY_BOOKING state
- In the create form, replace the current single "Total Amount (optional)" input with a paired UI:
  - amount input
  - currency Select with options GEL / USD / EUR
- Keep existing two-column layout style consistent with the rest of the form
- Pass currency in the create mutation payload
- Do not break existing create-booking behavior

6. RATES TIER FORM EUR-ONLY
File: artifacts/crm/src/pages/Rates.tsx
- Remove the tier currency Select from create/edit form
- Replace it with a static label like:
  Price per day (€)
- Keep currency: "EUR" in component state and payload
- Do not expose currency choice in the tier UI anymore

7. BACKEND ENFORCEMENT FOR RATE TIERS
File: artifacts/api-server/src/services/admin-rates.service.ts
- Force tier currency = 'EUR' on create/upsert regardless of incoming client value
- Backend must not trust the client here

IMPORTANT CONSTRAINTS
- Do not touch website quote flow
- Do not add exchange-rate APIs
- Do not redesign unrelated UI
- Do not modify accounting module
- Do not change unrelated booking logic

AFTER IMPLEMENTATION, VERIFY AND REPORT
Return a concise implementation report with:
1) files changed
2) migration / db push executed
3) SQL backfill executed
4) dashboard revenue conversion logic confirmed
5) booking create form currency picker confirmed
6) bookings list symbols confirmed
7) rates tier EUR-only UI + backend enforcement confirmed
8) any edge cases or follow-up notes