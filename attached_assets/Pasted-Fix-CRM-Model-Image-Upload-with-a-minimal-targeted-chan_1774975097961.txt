Fix CRM Model Image Upload with a minimal targeted change only.

Context
Tbilisicars CRM&WEB is already largely working. Do NOT refactor broadly. Do NOT redesign UI. Do NOT invent a new upload system. Reuse the existing working Brand logo upload flow exactly.

Confirmed symptom
In CRM → Fleet → Models → Edit Model, clicking "Upload Image" and selecting a file does not trigger a real upload request. The UI ends with "Failed to fetch". Brand logo upload already works and should be the reference implementation.

Goal
Make Model Image upload work exactly like Brand Logo upload.

Required approach
1. Find the exact working Brand logo upload implementation in the CRM.
2. Reuse that same upload logic for Model Image upload.
3. Do NOT create a separate upload architecture.
4. Do NOT touch unrelated Fleet logic.

Tasks
1. In the Models form/modal:
- trace the current "Upload Image" button handler
- confirm why selecting a file does not produce a proper upload request
- replace/fix the handler to use the same upload flow as Brand logo upload

2. Upload request requirements:
- use the same working upload endpoint as Brand logo upload
- use FormData correctly
- do NOT manually set Content-Type when sending FormData
- ensure file selection immediately triggers the upload request

3. Success handling:
- after successful upload, set imageUrl in the model form state
- show the uploaded image preview
- preserve existing save flow so model save stores the returned imageUrl

4. Error handling:
- if upload fails, show a useful toast/error message from the real response
- avoid generic silent failure

5. Verification
Confirm all of the following:
- selecting a file in Model Image triggers a real network request
- upload succeeds using the existing working backend upload system
- imageUrl is written into the model form state
- preview appears
- saving the model persists the imageUrl
- Brand logo upload still works
- no TypeScript/build errors

Constraints
- Do NOT change backend storage architecture
- Do NOT change Brand upload behavior except if a shared utility must be reused
- Do NOT refactor unrelated form code
- Keep the fix minimal and production-safe

Required output
Return:
1. exact root cause
2. exact files changed
3. exact minimal fix implemented
4. confirmation of verification results