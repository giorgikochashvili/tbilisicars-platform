Good plan, but revise it before build with these constraints:

1) PROMO CODES are NOT out of scope.
They are required in this task, but only with minimal reuse of existing promo logic.
You must inspect the current codebase and determine whether promo domain / routes / CRM module already exist.
- If promo management already exists in CRM, reuse it and only connect website flow correctly.
- If promo validation already exists on website/backend, reuse it and do not rewrite pricing logic.
- Only add the minimal CRM admin UI/form needed if creation/editing is missing.
- Do NOT create a new promo architecture if one already exists.
Update the plan to include this work.

2) For Step 2 extras:
Do not assume no data changes are needed.
Inspect whether Step 2 already shows CRM-managed extras with sufficient info.
- If Step 2 already renders CRM extras correctly, keep logic unchanged and only rename labels.
- If CRM extras exist but website Step 2 is not rendering enough metadata already available in current API/domain, make only the minimal extension needed.
Update the plan accordingly.

3) For filters:
- Keep client-side filtering only.
- Do NOT invent hardcoded seat buckets unless derived safely from existing result data.
- If using seat buckets, make sure vehicles with values like 6 or 8 are still handled correctly.
- Do NOT break On Request cards or existing editSearch behavior.

4) Drive type:
- Explicitly mark drive type as deferred because the field does not exist in the current schema/payload.
- Do NOT replace it silently with a different business meaning.
- Fuel Type may remain as an additional filter only if it already exists in current website data and requires no schema change.

Now return the revised implementation plan including:
- Homepage auto-open dropoff
- Step 1 sidebar + filters + compact layout
- EUR total vs daily display
- Step 2 rename + CRM extras inspection/reuse
- CRM promo creation/management + website promo application using existing promo logic only