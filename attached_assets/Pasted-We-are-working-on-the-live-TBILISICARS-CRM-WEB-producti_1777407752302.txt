We are working on the live TBILISICARS CRM&WEB production project.

Goal:
Fix only the confirmed low-risk issues from the Claude audit:
1) Parameterize the raw SQL in /api/public/booking-config.
2) Add robots/noindex protection for /login, /cabinet, and /crm.

STRICT RULES:
- Do NOT change pricing logic.
- Do NOT change booking flow.
- Do NOT change API response shapes.
- Do NOT change database schema.
- Do NOT refactor unrelated code.
- Do NOT touch CRM logic.
- Do NOT touch email/PDF logic.
- Keep this surgical and production-safe.

Task 1 — Parameterize booking-config SQL

File likely involved:
artifacts/api-server/src/routes/public-bookings.ts

Problem:
In /api/public/booking-config, rateCheckDate and daysInt are interpolated into a raw SQL template string used by pool.query(). Even if currently sanitized, this is unsafe architecture.

Required:
- Replace direct SQL interpolation with bind parameters.
- Use pool.query(sql, values).
- Keep the same pricing behavior and same response format.
- Keep CURRENT_DATE behavior when pickup/dropoff dates are not provided.
- Do not modify rate selection priority or lateral query logic beyond parameterization.

Done looks like:
- No user-derived values are inserted directly into the SQL string.
- booking-config works with:
  - no query params
  - pickup_datetime + dropoff_datetime
  - pickup_location_id / location filters if supported
  - all existing booking search params
- Step 1 vehicle prices remain identical to before.
- Step 2 quote still matches Step 1.

Task 2 — Add noindex/robots protection

Files likely involved:
artifacts/website/public/robots.txt
artifacts/website/src/pages/Login.tsx
artifacts/website/src/pages/Cabinet.tsx
CRM public route if applicable only if safely accessible in website app.

Required:
- Update robots.txt to disallow:
  /login
  /cabinet
  /crm/
- Add noindex,nofollow meta to Login and Cabinet pages.
- If react-helmet-async is already installed, use it.
- If not installed, do NOT introduce full SEO architecture in this task unless absolutely necessary. Use the safest minimal method available in the current codebase.
- Do not build full per-page SEO metadata yet. That is a separate later task.

Testing checklist:
- Public booking page loads.
- Vehicle list/prices load correctly.
- Booking quote still works.
- Login and Cabinet pages still render.
- robots.txt contains Disallow rules.
- No TypeScript/build errors.

After implementation:
- Summarize exact files changed.
- Confirm what was not touched.
- Tell me the exact commands to test locally in Replit.