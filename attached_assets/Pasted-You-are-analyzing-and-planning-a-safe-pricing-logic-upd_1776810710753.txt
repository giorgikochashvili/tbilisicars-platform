You are analyzing and planning a safe pricing logic update in the existing Tbilisicars monorepo.

STRICT RULES:

* This is a LIVE production-connected project
* Do NOT change architecture
* Do NOT refactor unrelated code
* Do NOT guess how pricing works — inspect first
* Do NOT implement yet if the exact current calculation path is not confirmed
* Keep all proposed changes surgical and minimal
* Preserve backward compatibility
* No breaking changes

GOAL:
Allow promo codes to work on vehicle models that already have an active website discount, while keeping pricing correct, transparent, and consistent across:

* booking page UI
* promo validation flow
* quote calculation
* final booking creation data

CURRENT BUSINESS REQUIREMENT:
A discounted vehicle should still accept a promo code.

DESIRED CUSTOMER-FACING RESULT:
If both a website discount and a promo code apply, the pricing should be shown clearly and cleanly.
Customers should never see internal discount names.
The final total must be correct everywhere.

IMPORTANT:
First inspect the real current behavior and identify exactly where discounted vehicles are blocked from promo usage.
Do NOT assume whether the block is:

* frontend-only
* promo validation endpoint
* quote/pricing logic
* booking creation validation
* or a combination

CHECK THESE AREAS FIRST:

* artifacts/website/src/pages/Booking.tsx
* artifacts/api-server/src/routes/public-bookings.ts
* artifacts/api-server/src/services/public-bookings.service.ts (if present)
* pricing/quote helpers used by the website booking flow
* promo validation endpoint logic
* final booking creation price calculation path
* any shared pricing helpers used by email/PDF generation if relevant
* any types/interfaces that already expose:

  * original rental
  * discounted rental
  * promo discount
  * final total

LOOK SPECIFICALLY FOR:

* where the UI says promo codes cannot be combined with active discounts
* where promo validation rejects discounted vehicles
* where website discount is applied
* whether promo discount currently applies before or after website discount
* whether one-way fee and extras are already excluded from promo logic
* whether there is already a field for websiteDiscountAmount / discountedRentalPrice / promoDiscountAmount / finalRentalPrice

TASK BREAKDOWN:

---

1. TRACE THE CURRENT PRICING FLOW

---

Map the real order of operations today.

Identify:

* base rental source
* website discount application point
* promo code validation point
* promo discount application point
* extras calculation
* one-way fee calculation
* final total assembly
* booking persistence fields affected

Return the exact file/function path for each step.

---

2. FIND THE EXACT BLOCKER

---

Identify exactly why promo codes currently do not work on discounted vehicles.

Examples of possible blockers:

* frontend message/guard only
* validation endpoint rejects when website discount exists
* quote builder short-circuits promo logic
* booking creation rejects mixed discounts

Return the exact condition(s) causing the block.

---

3. PROPOSE THE SAFEST BUSINESS RULE

---

Recommend the safest exact order of operations for this system.

The plan should explicitly confirm whether the correct order is:

A)
base rental
→ website discount
→ promo discount
→ final rental total

or something else based on the actual current code.

Important:

* promo should apply only to rental price, not one-way fee
* extras/per-trip/per-day logic must remain unchanged unless current code already supports promo on them (most likely it should NOT)
* do NOT broaden promo scope accidentally

---

4. UI / PRICE BREAKDOWN REQUIREMENTS

---

If both discounts are active, the customer-facing pricing should clearly show:

* Base rental
* Website discount
* Promo discount
* Final rental total

Rules:

* no internal campaign names
* clean customer-facing labels only
* consistent output in all booking summary areas
* if email/PDF pricing already reuses the same pricing fields, note whether they also need follow-up after this task

Do NOT implement email/PDF changes in this plan unless the same fields are directly affected.

---

5. BACKWARD COMPATIBILITY / RISK CHECK

---

The plan must explicitly state:

* whether existing non-discounted promo flow stays unchanged
* whether discounted-no-promo flow stays unchanged
* whether old bookings remain unaffected
* whether persistence fields need extension or only existing fields need better use
* whether there is any risk of double-discounting bugs

---

6. MINIMAL SAFE IMPLEMENTATION STRATEGY

---

The plan should prefer the smallest safe implementation.

Examples:

* remove the specific validation block
* update quote calculation order
* extend returned quote fields if needed
* update UI breakdown rendering

Avoid:

* architecture rewrites
* pricing engine rewrite
* unrelated refactors

---

7. DONE LOOKS LIKE

---

* promo codes can be applied to discounted vehicles
* pricing order is explicit and correct
* one-way fee behavior remains unchanged
* extras behavior remains unchanged unless explicitly confirmed otherwise
* UI shows clean breakdown with both discounts when applicable
* no internal discount names exposed
* booking creation persists the correct final values
* no TypeScript/runtime errors
* no regressions for existing bookings

---

8. OUTPUT FORMAT

---

Return:

1. exact current pricing flow
2. exact blocker preventing promo+discount today
3. safest recommended order of operations
4. exact files to change
5. whether existing fields are sufficient or new fields are needed
6. regression risks to watch
7. whether this should go directly to Build or needs a Revise step
