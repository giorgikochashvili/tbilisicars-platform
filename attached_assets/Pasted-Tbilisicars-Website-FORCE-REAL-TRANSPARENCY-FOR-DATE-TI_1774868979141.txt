Tbilisicars Website — FORCE REAL TRANSPARENCY FOR DATE/TIME INPUTS (NO SOLID BACKGROUNDS)

Continue from current working state.

IMPORTANT:
- This is a STRICT visual fix
- Do NOT change layout
- Do NOT change form logic
- Do NOT change spacing or structure
- ONLY fix the background styling of input fields

==================================================
PROBLEM
==================================================

The date/time input fields still appear fully solid.

They are currently using a solid color instead of real transparency.

This is incorrect.

==================================================
TASK — APPLY REAL TRANSPARENCY (MANDATORY)
==================================================

Update the following fields:

- Pickup Date & Time
- Return Date & Time

AND (for consistency if needed):
- Location select field

==================================================
REQUIRED IMPLEMENTATION (DO NOT IGNORE)
==================================================

You MUST use actual transparency via RGBA or equivalent.

Example direction (do NOT leave solid colors):

- background: rgba(255, 255, 255, 0.05)   ← preferred light glass
OR
- background: rgba(15, 23, 42, 0.4)       ← dark glass version

Additionally:
- Add subtle backdrop blur:
  backdrop-filter: blur(6px);

- Keep border subtle:
  border: 1px solid rgba(255,255,255,0.1);

==================================================
VISUAL RESULT REQUIRED
==================================================

- Background behind the inputs must be slightly visible through them
- Inputs should look like glass (NOT solid boxes)
- Text and icons must remain fully readable
- Do NOT make them too transparent
- Do NOT keep them opaque

If background behind is NOT visible → task is NOT complete

==================================================
DO NOT DO
==================================================

- Do NOT use solid hex colors (like #1e293b)
- Do NOT fake transparency with gradients only
- Do NOT ignore backdrop blur
- Do NOT leave current styling unchanged

==================================================
DELIVERABLE
==================================================

Return:
1. File updated
2. Exact CSS applied to inputs
3. Confirmation that RGBA transparency is used
4. Confirmation that background is now slightly visible through inputs

==================================================
MANUAL TEST
==================================================

Homepage:
- Look at Pickup Date & Time field
- Look at Return Date & Time field

You MUST see:
→ slight visibility of background behind input
→ soft glass effect

If inputs still look solid → FIX NOT ACCEPTED