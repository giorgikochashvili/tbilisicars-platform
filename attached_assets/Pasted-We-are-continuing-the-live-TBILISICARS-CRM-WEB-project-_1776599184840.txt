We are continuing the live TBILISICARS CRM&WEB project already fully built in Replit.

Important:
- This task is ONLY for the remaining agreed UI/copy polish items
- Do NOT touch booking status logic again
- Do NOT touch booking time logic again
- Do NOT touch scroll behavior again
- Do NOT touch Step 4 checkbox/modal behavior again
- Do NOT touch success-page account block again
- Do NOT touch email redesign/PDF logic again
- Do NOT refactor unrelated code
- Do NOT change architecture
- Keep this surgical and production-safe

This task covers ONLY these 4 agreed items:

1. Global footer compact fix
2. Booking Step 1 cleanup
3. Insurance step text fix
4. Success page copy cleanup

Scope details:

1) Global footer compact fix
Problem:
- On mobile, the footer feels too vertically stretched
- Section spacing is too large
- The bottom of pages feels too long and loose

Required behavior:
- Make the footer more compact on mobile
- Reduce excessive vertical gaps between footer sections and footer items
- Keep all existing footer content and links
- Do not redesign the footer from scratch
- Make sure the footer still works well on all relevant website pages

2) Booking Step 1 cleanup
Problem:
- There is a duplicate Booking Summary: one collapsible and one always-open version
- The heading “Book Your Car” is too generic and too large
- The Step 1 header area feels too tall / not informative enough
- Step progress should be clearer and more visual

Required behavior:
- Remove the duplicate always-open Booking Summary block
- Keep only the collapsible Booking Summary
- Change the Step 1 heading from:
  “Book Your Car”
  to:
  “Choose Your Perfect Car”
- Make the Step 1 header area more compact and better balanced
- Add a clearer 1–6 step progress visual
- The step progress should indicate the flow clearly using short labels, aligned with the booking journey, for example:
  Vehicle / Extras / Insurance / Your Info / Review / Confirm
- Keep all current Step 1 logic, filters, category layout, mobile carousel, and selection behavior unchanged
- This is a presentation/header cleanup only

3) Insurance step text fix
Problem:
- The current main insurance text is not the wording we want

Required behavior:
- Replace the current main insurance text with exactly:
  “Full Insurance is activated without any additional fee.”
- Keep the rest of the Insurance step unchanged unless a tiny wording/layout adjustment is strictly needed to make this text fit cleanly

4) Success page copy cleanup
Problem:
- The success/confirmation page still feels a bit text-heavy / overloaded
- We want cleaner copy and better hierarchy, but only lightly
- The account block is already removed and must stay removed

Required behavior:
- Lightly simplify and polish the customer-facing success page copy
- Improve hierarchy/readability where needed
- Keep all essential booking confirmation details
- Do not remove important confirmation information
- Do not redesign the entire success page
- Keep changes modest, copy-focused, and presentation-focused

Constraints:
- First inspect the exact files and components involved
- Apply the smallest safe set of changes
- Do NOT change booking logic, pricing, availability, API, CRM, DB, or schema
- Do NOT change Step 2/3/4 logic
- Do NOT change email or PDF behavior
- Do NOT touch unrelated pages/components unless required for the footer compact fix

What I need from PLAN:
1. Exact files that must change
2. Exact current duplicate Booking Summary block that will be removed
3. Exact Step 1 header/progress cleanup approach
4. Exact footer compacting approach
5. Exact Insurance text location to update
6. Exact success-page copy areas to simplify
7. Any risks or ambiguity found
8. Wait for approval before BUILD

This must be implemented as a minimal, precise final polish step. Precision matters more than speed.