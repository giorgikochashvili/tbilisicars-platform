We are working on an already fully built and functioning project: **Tbilisicars CRM & WEB platform**.

IMPORTANT CONTEXT:

* The entire project was originally generated and structured by Replit.
* This is NOT a new project.
* It has been moved to a new Replit account via GitHub.
* The system is already running in production on a server.
* We MUST NOT restructure, rewrite, or redesign anything.
* We ONLY fix current TypeScript build errors so deployment can succeed.

GOAL:
Fix all TypeScript errors so that `pnpm build` completes successfully WITHOUT breaking any existing functionality.

CRITICAL RULES:

* DO NOT change architecture
* DO NOT rename files or folders
* DO NOT remove features
* DO NOT refactor unrelated logic
* ONLY apply minimal, safe fixes required for type correctness

---

### ERRORS TO FIX (PRIORITY)

#### 1. Fix `req.params` type issues

Problem:
TypeScript error:
Argument of type 'string | string[]' is not assignable to parameter of type 'string'

Fix pattern:
Everywhere we use:

```ts
parseInt(req.params.id)
```

Replace with SAFE handling:

```ts
const id = parseInt(String(req.params.id), 10);
```

Apply this fix in ALL files:

* admin-accounting.ts
* admin-booking-payments.ts
* admin-bookings.ts
* admin-fleet.ts
* admin-service.ts

---

#### 2. Fix "Not all code paths return a value"

For ALL async route handlers like:

```ts
router.get(..., async (req, res) => {
```

Ensure:

* Every path returns a response
* Add fallback return:

```ts
return res.status(500).json({ error: "Unexpected error" });
```

---

#### 3. Fix `string | undefined` issues

Example:

```ts
paymentDate: string;
```

Fix:

```ts
paymentDate: paymentDate ?? new Date().toISOString(),
```

---

#### 4. Fix incorrect result typing (`result.id`)

Problem:

```ts
result.id does not exist
```

Fix:

```ts
result.payment.id
```

---

### FINAL STEP

After applying fixes:

1. Run:

```bash
pnpm build
```

2. Confirm:

* No TypeScript errors
* Build completes successfully

---

### IMPORTANT

* Keep all business logic unchanged
* Do NOT touch working modules (Customers, Rates, Extras, etc.)
* Only fix typing and compile errors

Return a summary of:

* Files modified
* What was fixed in each file
* Confirmation that build passes
