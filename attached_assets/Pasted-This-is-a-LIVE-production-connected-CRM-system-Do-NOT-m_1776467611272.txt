This is a LIVE production-connected CRM system. Do NOT modify production logic or real data.

We need to improve Preview/testing visibility because current data is insufficient to properly test UI, mobile layouts, and operational flows.

IMPORTANT:
This task is NOT about changing business logic.
This task is ONLY about safely introducing test data for development/preview purposes.

-----------------------------------
STEP 1 — ANALYZE CURRENT SETUP
-----------------------------------
- Identify how data is currently loaded in CRM pages:
  Bookings, Booking Detail, Dashboard (Operations), Customers, Monitoring
- Determine whether Preview is connected to real database or shared environment
- Identify safe insertion points for test data

-----------------------------------
STEP 2 — CHOOSE SAFE STRATEGY
-----------------------------------
You MUST choose ONLY ONE of these approaches:

A) Dev-only seed data (preferred)
- Data is inserted ONLY in development environment
- Controlled script or initialization logic

OR

B) Conditional fallback data
- If database returns empty → inject test data in frontend or API layer
- ONLY when NODE_ENV is development or preview

DO NOT:
- Write into production DB
- Override real data
- Mix fake and real data silently

-----------------------------------
STEP 3 — CREATE REALISTIC TEST DATA
-----------------------------------

Bookings:
- Multiple statuses: PENDING, CONFIRMED, DELIVERED, RETURNED, CANCELED
- Payment states: PAID, UNPAID, PREPAID
- Different cities: Tbilisi, Kutaisi, Batumi
- Various durations (1 day, multi-day, long rentals)
- Bookings with:
  - assigned vehicle
  - no vehicle (on request)
- Long customer names (to test UI overflow)
- Long vehicle names

Customers:
- Different name lengths and formats
- Different phone formats

Vehicles:
- Different models and plate numbers
- Different locations

Operations:
- Ensure there are ACTIVE pickups and dropoffs for today

Monitoring:
- Ensure there are records visible for filters and rendering

-----------------------------------
STEP 4 — SAFETY RULES (CRITICAL)
-----------------------------------
- MUST be environment-based (dev/preview only)
- MUST NOT affect production data
- MUST NOT change existing queries or logic
- MUST NOT break API contracts
- MUST NOT overwrite existing records

-----------------------------------
STEP 5 — OUTPUT PLAN
-----------------------------------
Before building, explain clearly:
1. Which approach you selected (A or B)
2. Where exactly you will implement it (files/modules)
3. How test data will be generated
4. Why it is safe and isolated
5. How it can be disabled later